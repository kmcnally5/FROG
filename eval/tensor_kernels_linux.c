/* tensor_kernels_linux.c — linux-only shim that pulls in the shared
 * kernel source. See tensor_kernels_darwin.c for the rationale; same
 * file, different OS gate.
 */
#include "tensor_kernels.inc"
