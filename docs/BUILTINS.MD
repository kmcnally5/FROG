# kLex Built-in Functions

<!-- generated: do not edit by hand. This file is regenerated from source by the maintainers' docs pipeline. Edit the doc comment above each builtin in eval/*.go instead. -->

<!-- last-generated: 2026-06-02 20:08:35 UTC -->

> ⚠️ **Auto-generated.** Do not edit this file by hand — your changes will be overwritten when it is regenerated from source. Edit the doc comment above the builtin's registration in `eval/builtins_*.go` instead.

This reference covers **619 builtins** organised into **49 categories**, derived directly from the source. Every signature, doc string, and source pointer is generated.

## Table of Contents

- [Core](#core) (52)
- [Console & Output](#console--output) (21)
- [Strings](#strings) (6)
- [Formatting](#formatting) (1)
- [Bytes](#bytes) (10)
- [Base64](#base64) (4)
- [Math](#math) (27)
- [Random](#random) (3)
- [Sort](#sort) (2)
- [Bitwise](#bitwise) (6)
- [Vector Math](#vector-math) (3)
- [Date & Time](#date--time) (5)
- [UUID](#uuid) (1)
- [Regular Expressions](#regular-expressions) (8)
- [Cryptography](#cryptography) (11)
- [Compression](#compression) (4)
- [CSV / TSV](#csv--tsv) (10)
- [PDF](#pdf) (3)
- [URL](#url) (2)
- [Path Manipulation](#path-manipulation) (8)
- [Operating System](#operating-system) (9)
- [Process Execution](#process-execution) (4)
- [Networking](#networking) (6)
- [HTTP](#http) (2)
- [HTTP Streaming](#http-streaming) (1)
- [Database](#database) (14)
- [MCP Client](#mcp-client) (6)
- [Native Bridges](#native-bridges) (9)
- [Native Bridge Pools](#native-bridge-pools) (6)
- [Parallel Processing](#parallel-processing) (3)
- [Atomic Arrays](#atomic-arrays) (7)
- [Concurrent Hash](#concurrent-hash) (5)
- [Graphics](#graphics) (58)
- [UI Widgets](#ui-widgets) (2)
- [Agent](#agent) (7)
- [Elapsed Time](#elapsed-time) (1)
- [Eval Wasm](#eval-wasm) (10)
- [Fs Dispatch Gen](#fs-dispatch-gen) (25)
- [Graphics Wasm](#graphics-wasm) (55)
- [Image Fx](#image-fx) (12)
- [Iter](#iter) (1)
- [Json](#json) (2)
- [Jsonl](#jsonl) (1)
- [Mcp Server](#mcp-server) (3)
- [Mtl Darwin](#mtl-darwin) (28)
- [Mtl Stub](#mtl-stub) (28)
- [Tensor](#tensor) (74)
- [Ui Lifecycle Wasm](#ui-lifecycle-wasm) (2)
- [Ui Widgets](#ui-widgets) (51)

## Core

### `__version__(…)` → `string`

__version__ returns the interpreter version string.


<sub>Source: [`eval/eval.go:241`](../eval/eval.go#L241)</sub>

### `appendFile(path, content)` → `null`

appendFile appends a string to a file, creating it if it does not exist. On failure returns a runtime error.


<sub>Source: [`eval/eval.go:885`](../eval/eval.go#L885)</sub>

### `apply(fn, args)`

apply(fn, args) — call fn with the elements of args as positional arguments.


This is the spread/variadic-call operator: where `fn(a, b, c)` is fixed at parse time, `apply(fn, [a, b, c])` lets you build the argument list at runtime. Indispensable for higher-order utilities like partial, flip, curry, and pipelines that hand off an arbitrary-arity call.


```lex
apply(fn(a, b) { a + b }, [3, 4])   → 7
apply(println, ["hello", "world"])  → prints "hello world"
```


Errors:

```lex
- fn must be a *Function or *Builtin
- args must be an *Array
- any error raised by fn itself is returned unchanged
```


<sub>Source: [`eval/eval.go:1175`](../eval/eval.go#L1175)</sub>

### `assert(condition, [message])` → `null`

assert checks that condition is true. assert(condition)          — fails with "assert: condition is false" assert(condition, message) — fails with the given message condition must be bool; a non-bool condition is a TypeError. On success returns null. On failure raises a RuntimeError that propagates normally — catchable with safe() like any other error.


<sub>Source: [`eval/eval.go:1056`](../eval/eval.go#L1056)</sub>

### `async(fn, ...args)`

async launches a function in a background goroutine and returns a Task immediately. Accepts both user-defined functions and builtins. Usage: task = async(fn, arg1, arg2, ...) The function runs in a snapshotted environment: it can read globals from the time the task was launched, but mutations are task-local and not visible to the caller. This eliminates mutex contention and prevents shared mutable state bugs. Note: do not call input() from async — it shares a global stdin reader. M4 (audit fix, 2026-05-22): async retains args[1:] as fnArgs inside the spawned goroutine that runs AFTER Fn returns. Mark so the VM's OpCallBuiltin allocates a fresh args slice rather than reusing a pooled buffer — otherwise the next caller's args would clobber what the goroutine still reads.


<sub>Source: [`eval/eval.go:1373`](../eval/eval.go#L1373)</sub>

### `await(task)`

await blocks until the given task completes and returns its result. If the task's function produced an error, await propagates it. Usage: result = await(task)


<sub>Source: [`eval/eval.go:1601`](../eval/eval.go#L1601)</sub>

### `cancel(ch)` → `null`

cancel signals that the consumer of a channel is done and no more values should be sent. Any blocked or future send() call on this channel returns false instead of blocking. cancel() is idempotent — calling it twice is safe. This is the explicit form; breaking out of a for-in loop over a channel also cancels it automatically.


<sub>Source: [`eval/eval.go:1024`](../eval/eval.go#L1024)</sub>

### `channel(…)` → `channel`

channel creates a new channel for passing values between async tasks. channel()    — unbuffered: send blocks until a receiver is ready. channel(n)   — buffered with capacity n: send blocks only when the buffer is full.


<sub>Source: [`eval/eval.go:963`](../eval/eval.go#L963)</sub>

### `close(ch)`

close signals that no more values will be sent on the channel. Receivers will drain any buffered values then get (null, false) from recv. Returns null on success. RuntimeError if the channel is already closed.


<sub>Source: [`eval/eval.go:1121`](../eval/eval.go#L1121)</sub>

### `concat(a, b)` → `array`

concat merges two arrays into a new array in a single allocation. Faster than looping push when combining two existing arrays. Usage: concat(arr1, arr2) -> new array containing all elements of arr1 followed by arr2.


ANTIPATTERN ALERT (OFI #10): `acc = concat(acc, batch)` in a loop is O(n²) — each call copies the growing accumulator. If you're merging more than two arrays, collect them into a single outer array first and call concatAll() — that's O(total) in one pass.


<sub>Source: [`eval/eval.go:548`](../eval/eval.go#L548)</sub>

### `concatAll(arrs)` → `array`

concatAll(arrs) — single-allocation flatten of an array of arrays. Replaces the O(n²) loop antipattern:


```lex
// BAD — O(n²)
acc = []
for batch in batches { acc = concat(acc, batch) }

// GOOD — O(total)
acc = concatAll(batches)
```


Computes total length first so the result is one allocation, then copies each sub-array into place. Empty input → empty array. Non-array elements at any depth fail with a typed error naming the offending index.


<sub>Source: [`eval/eval.go:576`](../eval/eval.go#L576)</sub>

### `delete(hash, key)` → `null`

delete removes a key from a hash in place (mutates the hash).


<sub>Source: [`eval/eval.go:357`](../eval/eval.go#L357)</sub>

### `endsWith(str, suffix)` → `bool`

endsWith returns true if str ends with the given suffix. endsWith("hello", "lo") → true


<sub>Source: [`eval/eval.go:817`](../eval/eval.go#L817)</sub>

### `env(name)` → `null`

env returns the value of an environment variable as a string, or null if unset.


<sub>Source: [`eval/eval.go:832`](../eval/eval.go#L832)</sub>

### `error(code, message)` → `error`

error creates a first-class Error value with a user-defined code and message. error("NOT_FOUND", "key was missing") The returned Error is NOT a propagation signal — it stays in the environment and can be inspected via .code, .message, and .is(code).


<sub>Source: [`eval/eval.go:1301`](../eval/eval.go#L1301)</sub>

### `exec(cmd, args)` → `string`

exec runs an external binary and returns its stdout as a string. The first argument is the command name or path; the second is an array of string arguments. On non-zero exit or any OS error, returns a runtime error — use safe(exec, cmd, args) to handle failures without crashing.


<sub>Source: [`eval/eval.go:911`](../eval/eval.go#L911)</sub>

### `filter(arr, fn)` → `array`

filter returns a new array containing only the elements for which the function returns true. Example: filter([1,2,3,4], fn(x) { x > 2 }) → [3, 4]


<sub>Source: [`eval/eval.go:1195`](../eval/eval.go#L1195)</sub>

### `float(val)` → `float`

float converts an integer or string to a float. float(3) → 3.0, float("2.5") → 2.5


<sub>Source: [`eval/eval.go:625`](../eval/eval.go#L625)</sub>

### `hasKey(hash, key)` → `bool`

hasKey returns true if the hash contains the given key, false otherwise. hasKey(h, "name") — avoids the null-check pattern of h["name"] == null.


<sub>Source: [`eval/eval.go:341`](../eval/eval.go#L341)</sub>

### `indexOf(str, substr)` → `int`

indexOf returns the index of the first occurrence of substr in str, or -1 if not found. Operates on Unicode code points, consistent with string indexing. indexOf("hello", "ll") → 2,  indexOf("hello", "x") → -1


<sub>Source: [`eval/eval.go:774`](../eval/eval.go#L774)</sub>

### `input(…)` → `string`

input prints an optional prompt and reads one line from stdin. The trailing newline is stripped. Returns the line as a string.


<sub>Source: [`eval/eval.go:939`](../eval/eval.go#L939)</sub>

### `int(val)` → `int`

int converts a string or float to an integer. int("42") → 42, int(3.9) → 3 (truncates toward zero)


<sub>Source: [`eval/eval.go:412`](../eval/eval.go#L412)</sub>

### `isError(val)` → `bool`

isError returns true if val is a RuntimeError or TypeError produced by the evaluator. Use this inside stream pipeline stages to detect errors returned by callback functions via safe().


<sub>Source: [`eval/eval.go:1042`](../eval/eval.go#L1042)</sub>

### `join(arr, sep)` → `string`

join concatenates an array of strings into a single string with a separator. join(["a", "b", "c"], ",") → "a,b,c" All array elements must be strings — mixing types is a TypeError.


<sub>Source: [`eval/eval.go:459`](../eval/eval.go#L459)</sub>

### `keys(hash)` → `array`

keys returns an array of all keys in a hash. Note: Go maps have no guaranteed order, so the key order is non-deterministic.


<sub>Source: [`eval/eval.go:280`](../eval/eval.go#L280)</sub>

### `len(val)` → `int`

len(val: array | string | hash) -> int


Return the length of an array, string, or hash.


<sub>Source: [`eval/eval.go:250`](../eval/eval.go#L250)</sub>

### `lower(str)` → `string`

lower returns a copy of a string with all letters converted to lowercase.


<sub>Source: [`eval/eval.go:613`](../eval/eval.go#L613)</sub>

### `makeArray(n, [default])` → `array`

makeArray allocates an array of n elements in a single allocation, all set to defaultVal. Use this instead of building with push() in a loop — push is O(n) per call (O(n²) total), makeArray is O(n) once. Fill elements with arr[i] = val afterwards. Usage: arr = makeArray(1000000, 0)   — 1M zeros, O(n)


<sub>Source: [`eval/eval.go:518`](../eval/eval.go#L518)</sub>

### `map(arr, fn)` → `array`

map(arr: array, fn: function) -> array


Apply a function to each element.


<sub>Source: [`eval/eval.go:1266`](../eval/eval.go#L1266)</sub>

### `pop(arr)` → `array`

pop returns a NEW array with the last element removed — it does not mutate. Consistent with push: both operations return new arrays rather than modifying in place. Calling pop on an empty array returns an empty array.


<sub>Source: [`eval/eval.go:484`](../eval/eval.go#L484)</sub>

### `print(…)` → `null`

print outputs a value without a trailing newline. Useful for building output on a single line across multiple calls.


<sub>Source: [`eval/eval.go:387`](../eval/eval.go#L387)</sub>

### `println(…)` → `null`

println(...vals: any) -> null


Print values to stdout with newline.


<sub>Source: [`eval/eval.go:244`](../eval/eval.go#L244)</sub>

### `push(arr, val)` → `array`

push returns a NEW array with the element appended — it does not mutate. This is intentional: immutable array operations are safer and more predictable.


<sub>Source: [`eval/eval.go:501`](../eval/eval.go#L501)</sub>

### `range(start, [stop], [step])`

range generates an array of integers. range(stop)             → [0, 1, ..., stop-1] range(start, stop)      → [start, start+1, ..., stop-1] range(start, stop, step)→ [start, start+step, ...] up to but not including stop A negative step counts down. Returns an empty array if the range is empty.


<sub>Source: [`eval/eval.go:649`](../eval/eval.go#L649)</sub>

### `readFile(path)` → `string`

readFile reads the entire contents of a file and returns it as a string. On failure (file not found, permission denied, etc.) it returns a runtime error. Use safe(readFile, path) to handle the error without crashing.


<sub>Source: [`eval/eval.go:849`](../eval/eval.go#L849)</sub>

### `recv(ch)` → `tuple`

recv receives the next value from a channel. Returns (value, true) when a value is available. Returns (null, false) when the channel is closed and empty. Blocks until a value is available or the channel is closed.


<sub>Source: [`eval/eval.go:1082`](../eval/eval.go#L1082)</sub>

### `recvNonBlock(ch)` → `null`

recvNonBlock attempts to receive from a channel without blocking. Returns the value if one is immediately available. Returns null if the channel is empty (no value available yet). Returns null if the channel is closed with no buffered values. Used for cooperative cancellation signaling in parallel workers.


<sub>Source: [`eval/eval.go:1102`](../eval/eval.go#L1102)</sub>

### `reduce(arr, fn, init)`

reduce folds an array into a single value by repeatedly applying a function. The function receives (accumulator, currentElement) and returns the new accumulator. Example: reduce([1,2,3], fn(acc, x) { acc + x }, 0) → 6


<sub>Source: [`eval/eval.go:1235`](../eval/eval.go#L1235)</sub>

### `replace(str, old, new)` → `string`

replace returns a copy of str with all occurrences of old replaced by new. replace("hello world", "world", "kLex") → "hello kLex" Replaces EVERY occurrence (it's a thin wrapper over Go's strings.ReplaceAll). `replaceAll` is a JS/Python-friendly alias for discoverability — same behaviour either way.


<sub>Source: [`eval/eval.go:731`](../eval/eval.go#L731)</sub>

### `replaceAll(str, old, new)` → `string`

replaceAll(s, old, new) — alias of replace. Lives under both names so people coming from JS / Python (where replace() is single-replace and replaceAll() is all-occurrences) find what they expect. Both call strings.ReplaceAll under the hood.


<sub>Source: [`eval/eval.go:753`](../eval/eval.go#L753)</sub>

### `safe(fn, ...args)` → `tuple`

safe calls a function (user-defined or builtin) and turns any runtime error into a (null, ErrorObject) tuple instead of letting the error propagate and crash the program. Usage: val, err = safe(fn, arg1, arg2, ...) On success returns (result, null); on error returns (null, Error). The Error carries a .code ("RUNTIME_ERROR" or "TYPE_ERROR") and .message. If the function already returns a tuple it is passed through unchanged.


<sub>Source: [`eval/eval.go:1323`](../eval/eval.go#L1323)</sub>

### `send(ch, val)`

send transmits a value to a channel. Blocks until a receiver is ready (unbuffered) or buffer has space (buffered). Returns null on success. Returns false (Boolean) if the channel has been cancelled via cancel() or by a consumer breaking out of a for-in loop — the caller should stop sending. Returns a RuntimeError if the channel's data side is already closed.


<sub>Source: [`eval/eval.go:986`](../eval/eval.go#L986)</sub>

### `sleep(ms)` → `null`

sleep pauses execution for the given number of milliseconds. sleep(500) waits half a second. Always returns null.


<sub>Source: [`eval/eval.go:1143`](../eval/eval.go#L1143)</sub>

### `split(str, sep)` → `array`

split breaks a string into an array of substrings on a separator. split("a,b,c", ",") → ["a", "b", "c"]


<sub>Source: [`eval/eval.go:437`](../eval/eval.go#L437)</sub>

### `sqrt(n)` → `float`

sqrt returns the square root of a number (integer or float). Usage: sqrt(16) → 4.0   sqrt(2) → 1.414...


<sub>Source: [`eval/eval.go:1579`](../eval/eval.go#L1579)</sub>

### `startsWith(str, prefix)` → `bool`

startsWith returns true if str begins with the given prefix. startsWith("hello", "he") → true


<sub>Source: [`eval/eval.go:801`](../eval/eval.go#L801)</sub>

### `str(val)` → `string`

str converts any value to its string representation. This is how you turn an integer into a string for building output. str(42) → "42", str(true) → "true", str(null) → "null"


<sub>Source: [`eval/eval.go:404`](../eval/eval.go#L404)</sub>

### `trim(str)` → `string`

trim removes leading and trailing whitespace from a string.


<sub>Source: [`eval/eval.go:716`](../eval/eval.go#L716)</sub>

### `type(val)` → `string`

type returns the runtime type name of any value as a string. Useful for debugging: type(42) → "INTEGER", type("hi") → "STRING"


<sub>Source: [`eval/eval.go:395`](../eval/eval.go#L395)</sub>

### `upper(str)` → `string`

upper returns a copy of a string with all letters converted to uppercase.


<sub>Source: [`eval/eval.go:602`](../eval/eval.go#L602)</sub>

### `values(hash)` → `array`

values returns an array of all values in a hash. Mirrors keys() — accepts both *Hash and *ConcurrentHash so the two builtins stay symmetric (calling code can iterate values(ch) the same way it iterates keys(ch)). Go map iteration is non-deterministic so do not rely on order across calls; ConcurrentHash uses sync.Map.Range whose ordering is also non-deterministic and may briefly include or exclude entries added/removed during iteration.


<sub>Source: [`eval/eval.go:314`](../eval/eval.go#L314)</sub>

### `writeFile(path, content)` → `null`

writeFile writes a string to a file, creating it if it does not exist and truncating it if it does. On failure returns a runtime error.


<sub>Source: [`eval/eval.go:865`](../eval/eval.go#L865)</sub>

## Console & Output

### `color_bg_black(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:85`](../eval/builtins_console.go#L85)</sub>

### `color_bg_blue(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:65`](../eval/builtins_console.go#L65)</sub>

### `color_bg_cyan(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:77`](../eval/builtins_console.go#L77)</sub>

### `color_bg_green(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:61`](../eval/builtins_console.go#L61)</sub>

### `color_bg_magenta(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:73`](../eval/builtins_console.go#L73)</sub>

### `color_bg_red(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:57`](../eval/builtins_console.go#L57)</sub>

### `color_bg_white(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:81`](../eval/builtins_console.go#L81)</sub>

### `color_bg_yellow(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:69`](../eval/builtins_console.go#L69)</sub>

### `color_black(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:37`](../eval/builtins_console.go#L37)</sub>

### `color_blue(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:17`](../eval/builtins_console.go#L17)</sub>

### `color_bold(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:41`](../eval/builtins_console.go#L41)</sub>

### `color_cyan(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:29`](../eval/builtins_console.go#L29)</sub>

### `color_dim(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:45`](../eval/builtins_console.go#L45)</sub>

### `color_green(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:13`](../eval/builtins_console.go#L13)</sub>

### `color_magenta(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:25`](../eval/builtins_console.go#L25)</sub>

### `color_red(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:9`](../eval/builtins_console.go#L9)</sub>

### `color_reset(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:53`](../eval/builtins_console.go#L53)</sub>

### `color_underline(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:49`](../eval/builtins_console.go#L49)</sub>

### `color_white(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:33`](../eval/builtins_console.go#L33)</sub>

### `color_yellow(…)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:21`](../eval/builtins_console.go#L21)</sub>

### `colorize(arg1, arg2, ...rest)` → `string`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_console.go:88`](../eval/builtins_console.go#L88)</sub>

## Strings

### `chr(n)` → `string`

chr(n) → string


Returns a single-character string containing the rune for code point n. n must be a valid Unicode code point — negative values, values in the surrogate range (0xD800–0xDFFF), and values >= 0x110000 are RuntimeErrors.


Pairs with ord(): chr(ord(c)) == c for any single-rune string.


Examples:

```lex
chr(65)    → "A"
chr(9731)  → "☃"
```


<sub>Source: [`eval/builtins_strings.go:215`](../eval/builtins_strings.go#L215)</sub>

### `ord(c)` → `int`

ord(c) → int


Returns the Unicode code point of the first character of c. The argument must be a non-empty string; supplying an empty string is a RuntimeError. Multi-character inputs use only the first rune — `ord("Ab")` returns 65.


Pairs with chr(): chr(ord(c)) == c for any single-rune string. Use this in place of stdlib/encoding.lex's ord, which scanned a 95-char ASCII table per call (O(95) per char).


Examples:

```lex
ord("A")  → 65
ord(" ")  → 32
ord("☃")  → 9731
```


<sub>Source: [`eval/builtins_strings.go:186`](../eval/builtins_strings.go#L186)</sub>

### `parseFloat(str)` → `tuple`

parseFloat(str) → (float, err)


Safely parses a string as a float. Trims leading/trailing whitespace. Returns (value, null) on success, (null, error) on failure. Use this instead of float() when the input is untrusted (CSV, HTTP, database).


Example:

```lex
f, err = parseFloat("3.14")
if err != null { println("bad number: {err.message}")  return }
println(f * 2.0)   // 6.28
```


<sub>Source: [`eval/builtins_strings.go:152`](../eval/builtins_strings.go#L152)</sub>

### `parseInt(str)` → `tuple`

parseInt(str) → (int, err)


Safely parses a string as an integer. Trims leading/trailing whitespace. Returns (value, null) on success, (null, error) on failure. Use this instead of int() when the input is untrusted (CSV, HTTP, database).


Example:

```lex
n, err = parseInt("42")
if err != null { println("bad number: {err.message}")  return }
println(n + 1)   // 43
```


<sub>Source: [`eval/builtins_strings.go:115`](../eval/builtins_strings.go#L115)</sub>

### `slice(arr, start, [end])` → `array`

slice(arr, start) — returns a new array from start to the end of arr. slice(arr, start, end) — returns a new array from start up to (not including) end. Indices are 0-based. A RuntimeError is raised if start or end is out of bounds.


```lex
slice([1,2,3,4,5], 2)     → [3, 4, 5]
slice([1,2,3,4,5], 1, 4)  → [2, 3, 4]
```


<sub>Source: [`eval/builtins_strings.go:55`](../eval/builtins_strings.go#L55)</sub>

### `substr(str, start, [end])` → `string`

substr(str, start) — returns the substring from start to the end of str. substr(str, start, end) — returns the substring from start up to (not including) end. Indices are 0-based. A RuntimeError is raised if start or end is out of bounds.


```lex
substr("hello world", 6)     → "world"
substr("hello world", 0, 5)  → "hello"
```


<sub>Source: [`eval/builtins_strings.go:18`](../eval/builtins_strings.go#L18)</sub>

## Formatting

### `format(fmt, ...args)` → `string`

format(fmtStr, arg...) — printf-style string formatting.


Format verbs:

```lex
%d         integer decimal
%f %e %g   float (decimal / scientific / shortest); accepts INTEGER too
%s         string (strict — use %v for any type)
%t         boolean
%v         any value (calls Inspect())
%x %X      integer hex (lower / upper)
%o         integer octal
%b         integer binary
%%         literal percent sign
```


Width, precision, and flags (-+0 space) follow standard printf conventions. Too few or too many arguments is a RuntimeError. Wrong type for a verb is a RuntimeError.


<sub>Source: [`eval/builtins_fmt.go:26`](../eval/builtins_fmt.go#L26)</sub>

## Bytes

### `base64ToBytes(s)` → `tuple`

── base64ToBytes(s: string) → (bytes, error) ───────────────────────────


Decode standard base64. Returns (bytes, null) on success, (null, err) with code "BASE64_INVALID" on bad input.


<sub>Source: [`eval/builtins_bytes.go:126`](../eval/builtins_bytes.go#L126)</sub>

### `bytes(x)` → `bytes`

── bytes(x) → bytes ────────────────────────────────────────────────────


Construct a bytes value:

```lex
bytes("hello")           — utf-8 encode the string
bytes([72, 101, 108])    — pack an integer array (each 0..255)
bytes(5)                 — N zero bytes
bytes(b"...")            — defensive copy
```


<sub>Source: [`eval/builtins_bytes.go:32`](../eval/builtins_bytes.go#L32)</sub>

### `bytesConcat(arr)` → `bytes`

── bytesConcat(arr: array of bytes) → bytes ────────────────────────────


Merge an array of *Bytes into a single *Bytes via Go-level memcpy. The pre-existing workaround was: byte-index each into an int array then call `bytes(intArray)` — fine for small payloads but ~40ms per 393kB merge in the interpreter. Doing it here as a Go builtin is one allocation plus N copy() calls, microsecond-scale.


Empty array → empty bytes (0-length). Single-element array → a copy of that element (NOT a reference share). Mixed-type array → typeError identifying the first non-bytes index.


<sub>Source: [`eval/builtins_bytes.go:260`](../eval/builtins_bytes.go#L260)</sub>

### `bytesToBase64(b)` → `string`

── bytesToBase64(b: bytes) → string ────────────────────────────────────


Standard base64 encoding (RFC 4648, with padding). Reverse of base64ToBytes. Total-encoding wraps into a single string with no internal line breaks.


<sub>Source: [`eval/builtins_bytes.go:111`](../eval/builtins_bytes.go#L111)</sub>

### `bytesToFloats(b)` → `tuple`

── bytesToFloats(bs) → array ───────────────────────────────────────────


Unpack little-endian IEEE-754 float32 bytes into a kLex float array. Counterpart to floatsToBytes. len(bs) must be a multiple of 4. Returns (null, err) on size mismatch so callers can branch cleanly.


<sub>Source: [`eval/builtins_bytes.go:225`](../eval/builtins_bytes.go#L225)</sub>

### `bytesToHex(b)` → `string`

── bytesToHex(b: bytes) → string ───────────────────────────────────────


Lowercase hex encoding, two characters per byte, no separator. "deadbeef" style. Reverse of hexToBytes.


<sub>Source: [`eval/builtins_bytes.go:149`](../eval/builtins_bytes.go#L149)</sub>

### `bytesToStr(b)` → `tuple`

── bytesToStr(b: bytes) → (string, error) ──────────────────────────────


utf-8 decode bytes. Returns (str, null) on success, (null, err) when the bytes are not valid utf-8 — err.code is "BYTES_INVALID_UTF8". Use this when binary may be text; if the call site is sure the bytes are utf-8 (e.g. JSON response bodies) unwrap with `?`.


<sub>Source: [`eval/builtins_bytes.go:88`](../eval/builtins_bytes.go#L88)</sub>

### `floatsToBytes(arr)` → `bytes`

── floatsToBytes(arr) → bytes ──────────────────────────────────────────


Pack a kLex float array as little-endian IEEE-754 float32 bytes — the standard wire format for embeddings, scientific data, audio samples. Output length is 4 × len(arr). Integer elements are accepted and converted (so a literal `[1, 2, 3]` works).


Round-trips with bytesToFloats. Use for disk-resident vector indexes (one float32 per element matches Metal MTLBuffer<float> layout, so the same bytes go straight into _mtlBuffer without re-packing).


<sub>Source: [`eval/builtins_bytes.go:195`](../eval/builtins_bytes.go#L195)</sub>

### `hexToBytes(s)` → `tuple`

── hexToBytes(s: string) → (bytes, error) ──────────────────────────────


Decode a hex string. Accepts both upper and lower case. Returns (bytes, null) on success, (null, err) with code "HEX_INVALID" on bad input (odd length, non-hex character).


<sub>Source: [`eval/builtins_bytes.go:165`](../eval/builtins_bytes.go#L165)</sub>

### `strToBytes(s)` → `bytes`

── strToBytes(s: string) → bytes ───────────────────────────────────────


utf-8 encode a string. Strings are already utf-8 internally so this is effectively a typed copy — but having it as an explicit builtin keeps the conversion direction obvious at the call site.


<sub>Source: [`eval/builtins_bytes.go:71`](../eval/builtins_bytes.go#L71)</sub>

## Base64

### `_base64Decode(s)` → `tuple`

_base64Decode(s) → (decoded_string, err)  — standard base64


<sub>Source: [`eval/builtins_base64.go:23`](../eval/builtins_base64.go#L23)</sub>

### `_base64Encode(s)` → `string`

_base64Encode(s) → string  — standard base64 with padding


<sub>Source: [`eval/builtins_base64.go:11`](../eval/builtins_base64.go#L11)</sub>

### `_base64UrlDecode(s)` → `tuple`

_base64UrlDecode(s) → (decoded_string, err)  — URL-safe base64, no padding


<sub>Source: [`eval/builtins_base64.go:51`](../eval/builtins_base64.go#L51)</sub>

### `_base64UrlEncode(s)` → `string`

_base64UrlEncode(s) → string  — URL-safe base64, no padding


<sub>Source: [`eval/builtins_base64.go:39`](../eval/builtins_base64.go#L39)</sub>

## Math

### `abs(n)` → `int`

abs returns the absolute value of x. Returns the same type as input. abs(-3) → 3    abs(-1.5) → 1.5    abs(4) → 4


<sub>Source: [`eval/builtins_math.go:216`](../eval/builtins_math.go#L216)</sub>

### `acos(n)` → `float`

acos returns the arc cosine of x in radians. Always returns Float. x must be in [-1, 1]. acos(1) → 0.0    acos(0) → 1.5707963267948966 (π/2)


<sub>Source: [`eval/builtins_math.go:253`](../eval/builtins_math.go#L253)</sub>

### `asin(n)` → `float`

asin returns the arc sine of x in radians. Always returns Float. x must be in [-1, 1]. asin(1) → 1.5707963267948966 (π/2)


<sub>Source: [`eval/builtins_math.go:236`](../eval/builtins_math.go#L236)</sub>

### `atan(n)` → `float`

atan returns the arc tangent of x in radians. Always returns Float. atan(1) → 0.7853981633974483 (π/4)


<sub>Source: [`eval/builtins_math.go:269`](../eval/builtins_math.go#L269)</sub>

### `atan2(y, x)` → `float`

atan2 returns the arc tangent of y/x in radians, using the signs of both arguments to determine the correct quadrant. Always returns Float. atan2(1.0, 1.0) → 0.7853981633974483 (π/4)


<sub>Source: [`eval/builtins_math.go:282`](../eval/builtins_math.go#L282)</sub>

### `ceil(n)` → `int`

ceil returns the smallest integer greater than or equal to x. Accepts integer or float. Always returns Integer. ceil(3.2) → 4    ceil(-2.7) → -2    ceil(4) → 4


<sub>Source: [`eval/builtins_math.go:30`](../eval/builtins_math.go#L30)</sub>

### `constrain(val, lo, hi)` → `int`

constrain(val, lo, hi) → number Clamp val to [lo, hi]. Returns integer if val is integer, float otherwise.


<sub>Source: [`eval/builtins_math.go:386`](../eval/builtins_math.go#L386)</sub>

### `cos(n)` → `float`

cos returns the cosine of x (x in radians). Always returns Float.


<sub>Source: [`eval/builtins_math.go:95`](../eval/builtins_math.go#L95)</sub>

### `e(…)` → `float`

e returns Euler's number. Always returns Float. e() → 2.718281828459045


<sub>Source: [`eval/builtins_math.go:316`](../eval/builtins_math.go#L316)</sub>

### `exp(n)` → `float`

exp returns e raised to the power x. Always returns Float. exp(1) → 2.718281828459045    exp(0) → 1.0


<sub>Source: [`eval/builtins_math.go:295`](../eval/builtins_math.go#L295)</sub>

### `floor(n)` → `int`

floor returns the greatest integer less than or equal to x. Accepts integer or float. Always returns Integer. floor(3.7) → 3    floor(-2.3) → -3    floor(4) → 4


<sub>Source: [`eval/builtins_math.go:13`](../eval/builtins_math.go#L13)</sub>

### `fmod(a, b)` → `float`

fmod returns the floating-point remainder of a divided by b. Both arguments must be floats. fmod(10.5, 3.2) → 0.9000000000000004


<sub>Source: [`eval/builtins_math.go:345`](../eval/builtins_math.go#L345)</sub>

### `hsl(h, s, l, [a])` → `array`

hsl(h, s, l [, a]) → [r, g, b, a] Convert HSL colour to a float array compatible with fill(), gradient(), and theme slots. h, s, l, a all in [0.0, 1.0]. Alpha defaults to 1.0.


<sub>Source: [`eval/builtins_math.go:430`](../eval/builtins_math.go#L430)</sub>

### `lerp(a, b, t)` → `float`

lerp(a, b, t) → float Linear interpolation: a + (b-a)*t. Returns a at t=0, b at t=1. Not clamped.


<sub>Source: [`eval/builtins_math.go:412`](../eval/builtins_math.go#L412)</sub>

### `log(n)` → `float`

log returns the natural logarithm of x. Always returns Float. x must be positive. log(1) → 0.0    log(2.718281828) ≈ 1.0


<sub>Source: [`eval/builtins_math.go:153`](../eval/builtins_math.go#L153)</sub>

### `log10(n)` → `float`

log10 returns the base-10 logarithm of x. Always returns Float. x must be positive. log10(100) → 2.0    log10(1) → 0.0


<sub>Source: [`eval/builtins_math.go:187`](../eval/builtins_math.go#L187)</sub>

### `log2(n)` → `float`

log2 returns the base-2 logarithm of x. Always returns Float. x must be positive. log2(8) → 3.0    log2(1) → 0.0


<sub>Source: [`eval/builtins_math.go:170`](../eval/builtins_math.go#L170)</sub>

### `max(a, b)`

max returns the larger of two values. Accepts integers and floats. Mixed types are compared as floats; the original value (and its type) is returned. max(3, 7) → 7    max(1.5, 2.5) → 2.5    max(3, 2.5) → 3


<sub>Source: [`eval/builtins_math.go:136`](../eval/builtins_math.go#L136)</sub>

### `min(a, b)`

min returns the smaller of two values. Accepts integers and floats. Mixed types are compared as floats; the original value (and its type) is returned. min(3, 7) → 3    min(1.5, 2.5) → 1.5    min(3, 2.5) → 2.5


<sub>Source: [`eval/builtins_math.go:119`](../eval/builtins_math.go#L119)</sub>

### `mod(a, b)` → `int`

mod returns the integer remainder of a divided by b. Both arguments must be integers. mod(10, 3) → 1    mod(-10, 3) → -1


<sub>Source: [`eval/builtins_math.go:326`](../eval/builtins_math.go#L326)</sub>

### `pi(…)` → `float`

pi returns the mathematical constant π. Always returns Float. pi() → 3.141592653589793


<sub>Source: [`eval/builtins_math.go:307`](../eval/builtins_math.go#L307)</sub>

### `pow(base, exp)` → `float`

pow returns base raised to the power exp. Always returns Float. pow(2, 10) → 1024.0    pow(2.0, 0.5) → 1.4142135623730951


<sub>Source: [`eval/builtins_math.go:203`](../eval/builtins_math.go#L203)</sub>

### `remap(val, inLow, inHigh, outLow, outHigh)` → `float`

remap(val, inLow, inHigh, outLow, outHigh) → float Re-map val from [inLow, inHigh] to [outLow, outHigh]. Not clamped. Named remap (not map) to avoid collision with the higher-order map(arr, fn).


<sub>Source: [`eval/builtins_math.go:364`](../eval/builtins_math.go#L364)</sub>

### `round(n)` → `int`

round returns the nearest integer to x, rounding half away from zero. Accepts integer or float. Always returns Integer. round(3.5) → 4    round(3.4) → 3    round(-2.5) → -3


<sub>Source: [`eval/builtins_math.go:47`](../eval/builtins_math.go#L47)</sub>

### `sin(n)` → `float`

sin returns the sine of x (x in radians). Always returns Float.


<sub>Source: [`eval/builtins_math.go:84`](../eval/builtins_math.go#L84)</sub>

### `sqrt(n)` → `float`

sqrt returns the square root of x. Always returns Float. Accepts integer or float. x must be non-negative. sqrt(4) → 2.0    sqrt(2.0) → 1.4142135623730951


<sub>Source: [`eval/builtins_math.go:64`](../eval/builtins_math.go#L64)</sub>

### `tan(n)` → `float`

tan returns the tangent of x (x in radians). Always returns Float.


<sub>Source: [`eval/builtins_math.go:106`](../eval/builtins_math.go#L106)</sub>

## Random

### `rand(…)` → `float`

rand returns a random float in [0.0, 1.0). The global source is automatically seeded — no setup required. Usage: rand()  →  0.7341...


<sub>Source: [`eval/builtins_rand.go:13`](../eval/builtins_rand.go#L13)</sub>

### `randInt(min, max)` → `int`

randInt returns a random integer in the closed range [min, max]. Both endpoints are inclusive: randInt(1, 6) simulates a die roll. min must be <= max. Usage: randInt(1, 10)  →  7


<sub>Source: [`eval/builtins_rand.go:24`](../eval/builtins_rand.go#L24)</sub>

### `shuffle(arr)` → `array`

shuffle returns a new array with the elements in random order. The original array is not mutated (consistent with push, pop, concat). Usage: shuffle([1, 2, 3, 4, 5])  →  [3, 1, 5, 2, 4]


<sub>Source: [`eval/builtins_rand.go:45`](../eval/builtins_rand.go#L45)</sub>

## Sort

### `sort(arr)` → `array`

sort returns a new array sorted in ascending order. Works for arrays of integers, floats, or strings (not mixed element types). Uses a stable sort — equal elements keep their original relative order. Usage: sort([3, 1, 4, 1, 5]) → [1, 1, 3, 4, 5]


<sub>Source: [`eval/builtins_sort.go:14`](../eval/builtins_sort.go#L14)</sub>

### `sortBy(arr, fn)` → `array`

sortBy returns a new array sorted using a comparator function. compareFn(a, b) must return true when a should appear before b. Uses a stable sort — equal elements keep their original relative order. Usage: sortBy(people, fn(a, b) { return a["age"] < b["age"] })


<sub>Source: [`eval/builtins_sort.go:71`](../eval/builtins_sort.go#L71)</sub>

## Bitwise

### `bitAnd(a, b)` → `int`

bitAnd(a, b) → integer Bitwise AND of two integers. Both operands must be integer. bitAnd(0b1100, 0b1010) → 8  (0b1000) bitAnd(0xFF, 0x0F)     → 15 (0x0F)


<sub>Source: [`eval/builtins_bitwise.go:13`](../eval/builtins_bitwise.go#L13)</sub>

### `bitNot(x)` → `int`

bitNot(x) → integer Bitwise NOT (ones' complement) of an integer. bitNot(0)  → -1 bitNot(-1) → 0


<sub>Source: [`eval/builtins_bitwise.go:62`](../eval/builtins_bitwise.go#L62)</sub>

### `bitOr(a, b)` → `int`

bitOr(a, b) → integer Bitwise OR of two integers. Both operands must be integer. bitOr(0b1100, 0b0011) → 15 (0b1111)


<sub>Source: [`eval/builtins_bitwise.go:29`](../eval/builtins_bitwise.go#L29)</sub>

### `bitShiftLeft(x, n)` → `int`

bitShiftLeft(x, n) → integer Shift x left by n bits. Equivalent to x * 2^n for non-negative x. n must be a non-negative integer. bitShiftLeft(1, 4)  → 16 bitShiftLeft(3, 8)  → 768


<sub>Source: [`eval/builtins_bitwise.go:79`](../eval/builtins_bitwise.go#L79)</sub>

### `bitShiftRight(x, n)` → `int`

bitShiftRight(x, n) → integer Arithmetic right shift of x by n bits. Sign bit is preserved. Equivalent to x / 2^n for non-negative x. n must be a non-negative integer. bitShiftRight(16, 4) → 1 bitShiftRight(256, 3) → 32


<sub>Source: [`eval/builtins_bitwise.go:101`](../eval/builtins_bitwise.go#L101)</sub>

### `bitXor(a, b)` → `int`

bitXor(a, b) → integer Bitwise XOR of two integers. Both operands must be integer. bitXor(0b1100, 0b1010) → 6  (0b0110)


<sub>Source: [`eval/builtins_bitwise.go:45`](../eval/builtins_bitwise.go#L45)</sub>

## Vector Math

### `cosineSim(arg1, arg2)` → `float`

cosineSim(a, b) → float


Compute the cosine similarity of two equal-length numeric arrays. Range: -1.0 (opposite) … 0.0 (orthogonal) … 1.0 (identical direction). The standard similarity metric for sentence/document embeddings — k-nearest-neighbour searches in stdlib/ai/vector_store.lex use this to rank results.


Both arrays must have the same length and contain only numbers (Integer or Float; mixed is fine). Zero-magnitude vectors return 0.


```lex
v1 = [1.0, 0.0, 0.0]
v2 = [0.0, 1.0, 0.0]
cosineSim(v1, v2)  // 0.0
cosineSim(v1, v1)  // 1.0
```


<sub>Source: [`eval/builtins_vector.go:38`](../eval/builtins_vector.go#L38)</sub>

### `dotProduct(arg1, arg2)` → `float`

dotProduct(a, b) → float


Inner product of two equal-length numeric arrays. Faster than cosineSim (no normalisation step) — useful when your vectors are already normalised, or when you want raw similarity rather than cosine-corrected similarity.


<sub>Source: [`eval/builtins_vector.go:83`](../eval/builtins_vector.go#L83)</sub>

### `vecNorm(arg1)` → `float`

vecNorm(v) → float


L2 (Euclidean) norm of a numeric array — the geometric length of the vector. Useful for normalising vectors (`v / vecNorm(v)`) so dotProduct becomes equivalent to cosineSim.


<sub>Source: [`eval/builtins_vector.go:119`](../eval/builtins_vector.go#L119)</sub>

## Date & Time

### `_timeFields(unix)`

_timeFields(unix) → (year, month, day, hour, minute, second, weekday) Converts a unix timestamp (integer seconds) into local time fields.


<sub>Source: [`eval/builtins_datetime.go:57`](../eval/builtins_datetime.go#L57)</sub>

### `_timeFormat(unix, layout)` → `string`

_timeFormat(unix, layout) → string Formats a unix timestamp using Go's reference-time layout string. Named layout constants are provided in datetime.lex for convenience.


<sub>Source: [`eval/builtins_datetime.go:71`](../eval/builtins_datetime.go#L71)</sub>

### `_timeNanos(…)` → `int`

_timeNanos() → integer Returns the current time in nanoseconds since an arbitrary epoch. Suitable for high-resolution timing and benchmarking.


<sub>Source: [`eval/builtins_datetime.go:27`](../eval/builtins_datetime.go#L27)</sub>

### `_timeNow(…)` → `tuple`

_timeNow() → (year, month, day, hour, minute, second, unix, weekday) Returns the current local time as an 8-element tuple. unix is integer seconds since 1970-01-01 00:00:00 UTC. weekday is the full English name: "Monday", "Tuesday", etc.


<sub>Source: [`eval/builtins_datetime.go:38`](../eval/builtins_datetime.go#L38)</sub>

### `_timeParse(str, layout)` → `tuple`

_timeParse(str, layout) → (unix, err) Parses a time string using Go's reference-time layout. Returns the unix timestamp on success, or (0, err_message) on failure.


<sub>Source: [`eval/builtins_datetime.go:89`](../eval/builtins_datetime.go#L89)</sub>

## UUID

### `_uuid(…)` → `string`

_uuid() → string  — generates a random UUID v4. Uses crypto/rand for proper randomness, not math/rand. Format: xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx


<sub>Source: [`eval/builtins_uuid.go:13`](../eval/builtins_uuid.go#L13)</sub>

## Regular Expressions

### `_regexFind(pattern, str)` → `tuple`

_regexFind(pattern, str) → (string|null, err) Returns the first match, or null if none.


<sub>Source: [`eval/builtins_regex.go:33`](../eval/builtins_regex.go#L33)</sub>

### `_regexFindAll(pattern, str)` → `tuple`

_regexFindAll(pattern, str) → (array, err) Returns all non-overlapping matches.


<sub>Source: [`eval/builtins_regex.go:58`](../eval/builtins_regex.go#L58)</sub>

### `_regexGroups(pattern, str)` → `tuple`

_regexGroups(pattern, str) → (array|null, err) Returns capture groups of the first match. Index 0 is the full match, indexes 1+ are the capture groups. Returns null if no match.


<sub>Source: [`eval/builtins_regex.go:168`](../eval/builtins_regex.go#L168)</sub>

### `_regexGroupsAll(pattern, str)` → `tuple`

_regexGroupsAll(pattern, str) → (array_of_arrays, err) Returns capture groups for every match. Each element is an array where index 0 is the full match and indexes 1+ are the capture groups.


<sub>Source: [`eval/builtins_regex.go:198`](../eval/builtins_regex.go#L198)</sub>

### `_regexMatch(pattern, str)` → `tuple`

_regexMatch(pattern, str) → (bool, err) True if pattern matches anywhere in str.


<sub>Source: [`eval/builtins_regex.go:12`](../eval/builtins_regex.go#L12)</sub>

### `_regexReplace(pattern, str, repl)` → `tuple`

_regexReplace(pattern, str, repl) → (string, err) Replaces the first match with repl.


<sub>Source: [`eval/builtins_regex.go:84`](../eval/builtins_regex.go#L84)</sub>

### `_regexReplaceAll(pattern, str, repl)` → `tuple`

_regexReplaceAll(pattern, str, repl) → (string, err) Replaces all matches with repl.


<sub>Source: [`eval/builtins_regex.go:116`](../eval/builtins_regex.go#L116)</sub>

### `_regexSplit(pattern, str)` → `tuple`

_regexSplit(pattern, str) → (array, err) Splits str on every match of pattern.


<sub>Source: [`eval/builtins_regex.go:141`](../eval/builtins_regex.go#L141)</sub>

## Cryptography

### `_aesDecrypt(arg1, arg2)` → `tuple`

_aesDecrypt(ciphertext_hex, key) → (plaintext, error) Decrypts AES-256-GCM ciphertext (expects format from _aesEncrypt) Returns (plaintext, null) on success or (null, error) on failure


<sub>Source: [`eval/builtins_crypto.go:234`](../eval/builtins_crypto.go#L234)</sub>

### `_aesEncrypt(arg1, arg2)` → `tuple`

_aesEncrypt(plaintext, key) → (ciphertext_hex, error) Encrypts plaintext using AES-256-GCM (authenticated encryption) key: 32-byte key (will be used as-is; derive from password with pbkdf2 if needed) Returns: hex-encoded (nonce + ciphertext + tag) or error Format: 24 hex chars (12 bytes nonce) + ciphertext + tag (always 32 hex chars)


<sub>Source: [`eval/builtins_crypto.go:188`](../eval/builtins_crypto.go#L188)</sub>

### `_bcryptHash(password)` → `tuple`

_bcryptHash(password) → (hash, err) Generates bcrypt hash of password (cost=12, default)


<sub>Source: [`eval/builtins_crypto.go:101`](../eval/builtins_crypto.go#L101)</sub>

### `_bcryptVerify(password, hash)` → `tuple`

_bcryptVerify(password, hash) → (matches, err) Verifies password against bcrypt hash Returns (true, null) if password matches Returns (false, null) if password doesn't match Returns (false, error) on error


<sub>Source: [`eval/builtins_crypto.go:121`](../eval/builtins_crypto.go#L121)</sub>

### `_constantTimeEquals(a, b)` → `bool`

_constantTimeEquals(a, b) → boolean Compares two strings in constant time (resistant to timing attacks) Use this for comparing passwords, tokens, signatures


<sub>Source: [`eval/builtins_crypto.go:168`](../eval/builtins_crypto.go#L168)</sub>

### `_hmacSha256(key, data)` → `tuple`

_hmacSha256(key, data) → hex_string Returns HMAC-SHA256 of data with key as hex string


<sub>Source: [`eval/builtins_crypto.go:63`](../eval/builtins_crypto.go#L63)</sub>

### `_hmacSha512(key, data)` → `tuple`

_hmacSha512(key, data) → hex_string Returns HMAC-SHA512 of data with key as hex string


<sub>Source: [`eval/builtins_crypto.go:82`](../eval/builtins_crypto.go#L82)</sub>

### `_md5(data)` → `tuple`

_md5(data) → hex_string Returns MD5 hash of data as hex string (DEPRECATED: use SHA256) MD5 is cryptographically broken; use SHA256 for security


<sub>Source: [`eval/builtins_crypto.go:49`](../eval/builtins_crypto.go#L49)</sub>

### `_randomBytes(arg1)` → `tuple`

_randomBytes(length) → (random_hex_string, err) Generates cryptographically secure random bytes Returns as hex string


<sub>Source: [`eval/builtins_crypto.go:146`](../eval/builtins_crypto.go#L146)</sub>

### `_sha256(arg1)` → `tuple`

_sha256(data) → hex_string Returns SHA256 hash of data as hex string


<sub>Source: [`eval/builtins_crypto.go:20`](../eval/builtins_crypto.go#L20)</sub>

### `_sha512(arg1)` → `tuple`

_sha512(data) → hex_string Returns SHA512 hash of data as hex string


<sub>Source: [`eval/builtins_crypto.go:34`](../eval/builtins_crypto.go#L34)</sub>

## Compression

### `_deflateCompress(data)` → `tuple`

_deflateCompress(data) → (compressed, err) Compresses a string using deflate (no gzip header)


<sub>Source: [`eval/builtins_compress.go:63`](../eval/builtins_compress.go#L63)</sub>

### `_deflateDecompress(data)` → `tuple`

_deflateDecompress(data) → (decompressed, err) Decompresses deflate-compressed data


<sub>Source: [`eval/builtins_compress.go:90`](../eval/builtins_compress.go#L90)</sub>

### `_gzipCompress(data)` → `tuple`

_gzipCompress(data) → (compressed, err) Compresses a string using gzip (default level 6)


<sub>Source: [`eval/builtins_compress.go:14`](../eval/builtins_compress.go#L14)</sub>

### `_gzipDecompress(data)` → `tuple`

_gzipDecompress(data) → (decompressed, err) Decompresses gzip-compressed data


<sub>Source: [`eval/builtins_compress.go:38`](../eval/builtins_compress.go#L38)</sub>

## CSV / TSV

### `_csvFirstRowCols(data, delim)` → `tuple`

_csvFirstRowCols(data, delim) → (column_count, error) Optimized: Parses only first row and returns column count (early exit) Used by columnCount() to avoid parsing entire CSV


<sub>Source: [`eval/builtins_csv.go:390`](../eval/builtins_csv.go#L390)</sub>

### `_csvFormat(rows)` → `tuple`

_csvFormat(rows) → (string, error) Formats array of arrays to CSV (comma-delimited)


<sub>Source: [`eval/builtins_csv.go:144`](../eval/builtins_csv.go#L144)</sub>

### `_csvFormatDelim(rows, delim)` → `tuple`

_csvFormatDelim(rows, delim) → (string, error) Formats array of arrays with custom delimiter


<sub>Source: [`eval/builtins_csv.go:218`](../eval/builtins_csv.go#L218)</sub>

### `_csvHasRows(data, delim)` → `tuple`

_csvHasRows(data, delim) → (bool, error) Optimized: Checks if CSV has at least one row (early exit after first read) Used by isEmpty() to avoid parsing entire CSV


<sub>Source: [`eval/builtins_csv.go:433`](../eval/builtins_csv.go#L433)</sub>

### `_csvParse(data)` → `tuple`

_csvParse(data) → (array_of_arrays, error) Parses CSV with comma delimiter (RFC 4180)


<sub>Source: [`eval/builtins_csv.go:81`](../eval/builtins_csv.go#L81)</sub>

### `_csvParseDelim(data, delim)` → `tuple`

_csvParseDelim(data, delim) → (array_of_arrays, error) Parses CSV with custom delimiter (first rune of delim string)


<sub>Source: [`eval/builtins_csv.go:117`](../eval/builtins_csv.go#L117)</sub>

### `_csvParseHeaders(data)` → `tuple`

_csvParseHeaders(data) → (array_of_hashes, error) Parses CSV treating first row as column headers Returns array of hashes where each hash maps column name to value Uses lenient parsing (FieldsPerRecord=-1) to handle ragged rows


<sub>Source: [`eval/builtins_csv.go:266`](../eval/builtins_csv.go#L266)</sub>

### `_csvStream(data, delim)`

_csvStream(data, delim) → Channel Streams CSV rows as they are parsed. Each element sent to channel is an Array of Strings. Channel closes automatically when parsing completes or on error. Allows overlapping parsing and processing for better parallelization. Optimized: Large buffer + async parser prevent lock contention with multiple workers.


<sub>Source: [`eval/builtins_csv.go:315`](../eval/builtins_csv.go#L315)</sub>

### `_tsvFormat(arg1)` → `tuple`

_tsvFormat(rows) → (string, error) Formats array of arrays to TSV (tab-delimited)


<sub>Source: [`eval/builtins_csv.go:181`](../eval/builtins_csv.go#L181)</sub>

### `_tsvParse(arg1)` → `tuple`

_tsvParse(data) → (array_of_arrays, error) Parses TSV with tab delimiter


<sub>Source: [`eval/builtins_csv.go:99`](../eval/builtins_csv.go#L99)</sub>

## PDF

### `_pdfExtractByPage(arg1)` → `tuple`

_pdfExtractByPage(path) → (pages, err)


Returns an array of strings, one per page, in document order. Each element is that page's plain text (may be empty for image-only pages). Use this when you want to keep page-level source attribution in a RAG pipeline (cite "page N of foo.pdf").


Pages where the underlying parser fails fall back to empty strings — one bad page doesn't kill the whole extraction. The (err) return is reserved for fatal open/decode failures of the file itself.


<sub>Source: [`eval/builtins_pdf.go:88`](../eval/builtins_pdf.go#L88)</sub>

### `_pdfExtractText(arg1)` → `tuple`

_pdfExtractText(path) → (text, err)


Returns the concatenated plain text of every page in the PDF at `path`. Page boundaries are NOT marked in the output — pages are just streamed back-to-back. Use _pdfExtractByPage when you need per-page text (e.g. for source attribution).


<sub>Source: [`eval/builtins_pdf.go:30`](../eval/builtins_pdf.go#L30)</sub>

### `_pdfPageCount(arg1)` → `tuple`

_pdfPageCount(path) → (n, err)


Returns the number of pages in the PDF. Useful for progress reporting before extracting page-by-page.


<sub>Source: [`eval/builtins_pdf.go:60`](../eval/builtins_pdf.go#L60)</sub>

## URL

### `_urlDecode(s)` → `tuple`

_urlDecode(s) → (decoded_string, err)  — decodes a percent-encoded string.


<sub>Source: [`eval/builtins_url.go:24`](../eval/builtins_url.go#L24)</sub>

### `_urlEncode(s)` → `string`

_urlEncode(s) → string  — percent-encodes a query string component. Spaces become +, special characters become %XX.


<sub>Source: [`eval/builtins_url.go:12`](../eval/builtins_url.go#L12)</sub>

## Path Manipulation

### `beginPath(…)` → `null`

beginPath() → null Clear the current path and reset the pen. Must be called before building a new path.


<sub>Source: [`eval/builtins_path.go:18`](../eval/builtins_path.go#L18)</sub>

### `bezierTo(cp1x, cp1y, cp2x, cp2y, x, y)` → `null`

bezierTo(cp1x, cp1y, cp2x, cp2y, x, y) → null Add a cubic Bézier from the current pen to (x, y) via control points (cp1x,cp1y) and (cp2x,cp2y).


<sub>Source: [`eval/builtins_path.go:57`](../eval/builtins_path.go#L57)</sub>

### `closePath(…)` → `null`

closePath() → null Close the current contour by adding a line back to the last moveTo point.


<sub>Source: [`eval/builtins_path.go:96`](../eval/builtins_path.go#L96)</sub>

### `fillPath(…)` → `null`

fillPath() → null Fill the current path with the current fill colour. Uses GPU stencil even-odd rule — handles concave and complex shapes correctly.


<sub>Source: [`eval/builtins_path.go:108`](../eval/builtins_path.go#L108)</sub>

### `lineTo(x, y)` → `null`

lineTo(x, y) → null Add a straight line from the current pen to (x, y).


<sub>Source: [`eval/builtins_path.go:41`](../eval/builtins_path.go#L41)</sub>

### `moveTo(x, y)` → `null`

moveTo(x, y) → null Move the pen to (x, y) without drawing. Starts a new contour.


<sub>Source: [`eval/builtins_path.go:26`](../eval/builtins_path.go#L26)</sub>

### `quadTo(cpx, cpy, x, y)` → `null`

quadTo(cpx, cpy, x, y) → null Add a quadratic Bézier from the current pen to (x, y) via control point (cpx, cpy). Internally elevated to a cubic for uniform tessellation.


<sub>Source: [`eval/builtins_path.go:76`](../eval/builtins_path.go#L76)</sub>

### `strokePath(…)` → `null`

strokePath() → null Stroke the current path with the current stroke colour and strokeWeight.


<sub>Source: [`eval/builtins_path.go:117`](../eval/builtins_path.go#L117)</sub>

## Operating System

### `_osArgs(…)` → `array`

_osArgs() → array of strings


Returns command-line arguments AFTER kLex's flag parser has consumed its own options (--cpuprofile, --help, --version, etc.).


Shape (stable across flag-or-no-flag invocations):

```lex
args[0]    — the kLex binary path (os.Args[0])
args[1]    — the script path
args[2..N] — script arguments
```


Pre-fix behaviour returned raw os.Args, which meant any script using `args[2]` etc. broke if a kLex flag was present — the flag pair shifted every positional. OFI #13. The prepended binary path keeps `args[0]` meaningful for scripts that re-spawn klex on themselves (e.g. frogLight UI spawning the cataloger via _processSpawnDetached).


<sub>Source: [`eval/builtins_os.go:137`](../eval/builtins_os.go#L137)</sub>

### `_osCwd(…)` → `tuple`

_osCwd() → (path_string, err)


<sub>Source: [`eval/builtins_os.go:70`](../eval/builtins_os.go#L70)</sub>

### `_osExit(arg1)` → `null`

_osExit(code) — terminates the process with the given exit code. Does not return.


<sub>Source: [`eval/builtins_os.go:169`](../eval/builtins_os.go#L169)</sub>

### `_osGetenv(arg1)` → `null`

_osGetenv(key) → string or null Returns the value of the named environment variable, or null if unset.


<sub>Source: [`eval/builtins_os.go:35`](../eval/builtins_os.go#L35)</sub>

### `_osHostname(…)` → `tuple`

_osHostname() → (hostname_string, err)


<sub>Source: [`eval/builtins_os.go:82`](../eval/builtins_os.go#L82)</sub>

### `_osName(…)` → `string`

_osName() → string


Return the operating-system identifier — Go's runtime.GOOS verbatim. Common values: "darwin", "linux", "windows", "freebsd", "openbsd". Use for cross-platform branching when path-probing would be brittle:


```lex
name = _osName()
if name == "darwin"  { cmd = "open"     }
if name == "linux"   { cmd = "xdg-open" }
if name == "windows" { cmd = "cmd"      }
```


Replaces the historical pattern of probing `/usr/bin/open` etc., which fails on minimal images where the standard tools live elsewhere on PATH.


<sub>Source: [`eval/builtins_os.go:115`](../eval/builtins_os.go#L115)</sub>

### `_osPid(…)` → `int`

_osPid() → integer  — current process ID


<sub>Source: [`eval/builtins_os.go:94`](../eval/builtins_os.go#L94)</sub>

### `_osSetenv(arg1, arg2)` → `tuple`

_osSetenv(key, val) → (null, err)


<sub>Source: [`eval/builtins_os.go:51`](../eval/builtins_os.go#L51)</sub>

### `_scriptDir(…)` → `string`

_scriptDir() → string


Returns the directory of the .lex file whose code is currently running. Walks the enclosing function/module env chain — inside an imported module it returns THAT module's directory, not the entry script's. Empty string if no script context (REPL).


Use to locate sibling resources independent of CWD:

```lex
bridge = nativeBridge("python3", [_scriptDir() + "/helper.py"], opts)
font   = loadFont(_scriptDir() + "/fonts/Outfit.ttf", 32)
```


The actual scope-walking implementation lives in evalCall's special case (alongside async). This Fn is a placeholder that returns "" — only reached if someone calls _scriptDir indirectly (e.g. via safe()) and the special case is bypassed. Direct calls always go through evalCall and get the real value.


<sub>Source: [`eval/builtins_os.go:28`](../eval/builtins_os.go#L28)</sub>

## Process Execution

### `_processExec(cmd, args)` → `tuple`

_processExec(cmd, args) → (stdout, stderr, exitCode, err) Runs cmd with args, captures stdout and stderr separately. exitCode is the integer exit code (-1 if the process could not be started). err is non-null only when the process could not be started at all.


<sub>Source: [`eval/builtins_process.go:48`](../eval/builtins_process.go#L48)</sub>

### `_processRun(cmd, args)` → `tuple`

_processRun(cmd, args) → (stdout, err) Runs cmd with args, captures stdout. stderr is folded into err on failure. args must be an Array of strings (may be empty).


<sub>Source: [`eval/builtins_process.go:18`](../eval/builtins_process.go#L18)</sub>

### `_processShell(cmd)` → `tuple`

_processShell(cmd) → (stdout, err) Runs cmd as a shell command via /bin/sh -c. stderr is folded into err.


<sub>Source: [`eval/builtins_process.go:189`](../eval/builtins_process.go#L189)</sub>

### `_processSpawnDetached(cmd, args, [opts?])` → `tuple`

_processSpawnDetached(cmd, args, opts?) → (pid, err)


Start a child process detached from the parent. Returns the child's PID on success. The child survives the parent — typical use is "spawn a background daemon and forget it." Unlike _processExec / _processRun, this returns IMMEDIATELY after the OS reports the process as started; the parent does NOT block on the child's exit.


opts (hash, all optional):

```lex
logFile : string             — append child stdout AND stderr to this
                                path. Strongly recommended for daemons:
                                without it, writes block once the OS
                                pipe buffer fills (typically ~64KB).
env     : hash string→string — extra env vars added to the inherited env
dir     : string             — child's working directory (default: cwd)
```


Returns (pid, null) on success or (null, errMsg) if the OS could not start the process. The child is reaped in a background goroutine so callers don't leak zombies — there is no API to wait on the child; callers should detect liveness via the daemon's own heartbeat file.


<sub>Source: [`eval/builtins_process.go:106`](../eval/builtins_process.go#L106)</sub>

## Networking

### `dnsLookup(hostname)` → `tuple`

dnsLookup resolves a hostname to an array of IP address strings. Returns (addresses, null) on success, (null, error) on failure. Usage: addrs, err = dnsLookup("example.com")


<sub>Source: [`eval/builtins_net.go:157`](../eval/builtins_net.go#L157)</sub>

### `netClose(conn)` → `null`

netClose closes a NetConn. Usage: netClose(conn)


<sub>Source: [`eval/builtins_net.go:139`](../eval/builtins_net.go#L139)</sub>

### `netRead(conn, maxBytes)` → `tuple`

netRead reads up to maxBytes from conn. Returns (string, null) on success or EOF, (null, error) on failure. Usage: data, err = netRead(conn, 4096)


<sub>Source: [`eval/builtins_net.go:92`](../eval/builtins_net.go#L92)</sub>

### `netWrite(conn, data)` → `tuple`

netWrite sends data to conn. Returns (null, null) on success, (null, error) on failure. Usage: _, err = netWrite(conn, "hello\n")


<sub>Source: [`eval/builtins_net.go:118`](../eval/builtins_net.go#L118)</sub>

### `tcpDial(host, port)` → `tuple`

tcpDial(host: string, port: int) -> (conn, error)


Open a TCP connection to host:port. Returns (connection, null) on success or (null, error) on failure.


<sub>Source: [`eval/builtins_net.go:16`](../eval/builtins_net.go#L16)</sub>

### `tcpListen(host, port)`

tcpListen starts a TCP server on host:port and returns a Channel of NetConn. Each accepted connection is sent as a NetConn value. Cancelling the channel (break in a for-in loop) stops the server. Usage: for conn in tcpListen("0.0.0.0", 8080) { async(fn() { handle(conn) }) }


<sub>Source: [`eval/builtins_net.go:40`](../eval/builtins_net.go#L40)</sub>

## HTTP

### `_httpDo(arg1, arg2, arg3, arg4, [arg5])`

_httpDo(method, url, headers, body [, timeoutSec]) → (status, body, headers, err)


The single Go-level primitive that backs all kLex HTTP functions. method     — string: "GET", "POST", "PUT", "PATCH", "DELETE", etc. url        — string headers    — HASH of string → string, or null for no custom headers body       — string payload, or null for no body timeoutSec — optional number of seconds. Omitted/null/0 = use the

```lex
           shared 30s client (good for normal REST). Pass an int
           or float to override per-call — required for LLM calls
           with cold-start (Ollama, llama.cpp), large batch
           embeddings, or any endpoint that may take >30s.
```


Always returns a 4-element Tuple so the kLex caller can destructure:

```lex
status, body, headers, err = _httpDo(...)
```

On failure: status=0, body="", headers={}, err=<message string> On success: err=null


<sub>Source: [`eval/builtins_http.go:62`](../eval/builtins_http.go#L62)</sub>

### `_httpServe(arg1, arg2)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_http.go:180`](../eval/builtins_http.go#L180)</sub>

## HTTP Streaming

### `_httpStream(arg1, arg2, arg3, arg4, [arg5])`

_httpStream(method, url, headers, body [, mode]) → (channel, err)


Open an HTTP request and return a kLex channel that yields parsed streaming frames as hashes:


```lex
{ "event": <event-name>, "data": <raw-data-payload> }
```


`mode` selects the wire format (default "sse"):


```lex
"sse"   — Server-Sent Events. Each frame is one blank-line-delimited
          group of event:/data: lines. event is the SSE event name;
          data is the joined data payload. Default Accept header:
          text/event-stream. Used by Anthropic, OpenAI, etc.

"lines" — Newline-delimited JSON (NDJSON / JSON Lines). Each non-
          empty response line becomes one frame with event="" and
          data = the raw line text. Default Accept header:
          application/x-ndjson. Used by Ollama, llama.cpp's server,
          and similar local-runtime APIs.
```


In both modes kLex callers parse the JSON payload themselves — payload shapes are provider-specific so the primitive stays neutral.


Cancellation:

```lex
When the consumer breaks out of a `for evt in ch { ... }` loop the
evaluator closes ch.done; the producer goroutine sees that, aborts
the in-flight read, and closes the response body. No leaked
connections after a consumer walks away.
```


Return shape:

```lex
On connection-level error (DNS, refused, 4xx/5xx setup failure):
  (null, "error message string")
On success:
  (channel, null)
Mid-stream errors (connection dropped, scanner failure) are
delivered as a final frame {"event": "error", "data": "<message>"}
before the channel closes.
```


Defaults:

```lex
- Accept header set automatically based on mode if not supplied.
- No request timeout (streams can run indefinitely; rely on
  consumer cancellation via channel.done for shutdown).
```


<sub>Source: [`eval/builtins_http_stream.go:61`](../eval/builtins_http_stream.go#L61)</sub>

## Database

### `dbBegin(conn)`

dbBegin(conn) → (tx, err)


Starts a database transaction. Use dbCommit or dbRollback to finish it. Pass the returned tx to dbQuery, dbExec, dbCommit, and dbRollback.


Example:

```lex
tx, err = dbBegin(conn)
if err != null { return }
_, err = dbExec(tx, "INSERT INTO log VALUES (?, ?)", [42, "updated"])
_, err = dbExec(tx, "UPDATE accounts SET balance = ? WHERE id = ?", [1500, 42])
if err != null { dbRollback(tx)  return }
dbCommit(tx)
```


<sub>Source: [`eval/builtins_sql.go:744`](../eval/builtins_sql.go#L744)</sub>

### `dbBulkInsert(conn, table, columns, rows)`

dbBulkInsert(conn, table, columns, rows) → (n, err)


Inserts multiple rows in a single SQL statement for maximum throughput. One round trip per batch — far faster than calling dbExec in a loop.


```lex
table   — table name string (must be trusted — not parameterisable in SQL)
columns — array of column name strings
rows    — array of arrays, each sub-array is one row's values
```


Auto-batches to stay within driver parameter limits:

```lex
MSSQL/SQL Server — 2000 params per batch
PostgreSQL        — 60000 params per batch
```


Returns the total number of rows affected across all batches.


WARNING: table and column names are interpolated directly into the SQL string. Never pass user-controlled input as table or column names.


Example:

```lex
n, err = dbBulkInsert(conn, "users", ["id", "name", "age"], [
    [1, "Alice", 28],
    [2, "Bob",   35],
])
if err != null { println(err.message)  return }
println("{n} rows inserted")
```


<sub>Source: [`eval/builtins_sql.go:483`](../eval/builtins_sql.go#L483)</sub>

### `dbClose(conn)` → `null`

dbClose(conn) → null


Closes the connection pool. Best effort — call when done.


<sub>Source: [`eval/builtins_sql.go:216`](../eval/builtins_sql.go#L216)</sub>

### `dbCommit(tx)`

dbCommit(tx) → (null, err)


Commits a transaction started with dbBegin.


<sub>Source: [`eval/builtins_sql.go:764`](../eval/builtins_sql.go#L764)</sub>

### `dbExec(conn, sql, [args])`

dbExec(conn, sql, ?args) → (rowsAffected, err)


Executes an INSERT, UPDATE, DELETE, or DDL statement. Returns the number of rows affected as an integer (-1 if unavailable). Pass parameters as an array — never interpolate values into the SQL string. Works on both connections and transactions.


Example:

```lex
n, err = dbExec(conn, "UPDATE accounts SET balance = ? WHERE id = ?", [1500.0, 42])
if err != null { println(err.message)  return }
println("{n} row(s) updated")
```


<sub>Source: [`eval/builtins_sql.go:363`](../eval/builtins_sql.go#L363)</sub>

### `dbExecReturning(conn, sql, [args])`

dbExecReturning(conn, sql, ?args) → (rows, err)


Executes a DML statement that returns rows — for INSERT/UPDATE/DELETE with RETURNING (PostgreSQL) or OUTPUT (SQL Server) clauses. Returns an array of hashes, same format as dbQuery. Works on both connections and transactions.


Example (PostgreSQL):

```lex
rows, err = dbExecReturning(conn, "INSERT INTO users (name) VALUES (?) RETURNING id, name", ["Alice"])
if err != null { println(err.message)  return }
id = rows[0]["id"]
```


Example (SQL Server):

```lex
rows, err = dbExecReturning(conn, "INSERT INTO users (name) OUTPUT INSERTED.id, INSERTED.name VALUES (?)", ["Alice"])
if err != null { println(err.message)  return }
id = rows[0]["id"]
```


<sub>Source: [`eval/builtins_sql.go:701`](../eval/builtins_sql.go#L701)</sub>

### `dbOpen(driver, dsn)`

dbOpen(driver, dsn) → (conn, err)


Opens a database connection pool and verifies connectivity with a ping. The pool is safe for concurrent use — share one conn across goroutines.


Supported drivers:

```lex
"mssql"      — Microsoft SQL Server (also accepts "sqlserver")
"postgres"   — PostgreSQL via pgx
```


Connection strings:

```lex
MS SQL:   "server=host;database=mydb;user id=sa;password=Pass1!"
          "sqlserver://sa:Pass1!@host:1433?database=mydb"
Postgres: "host=host user=user password=pass dbname=mydb sslmode=disable"
          "postgres://user:pass@host:5432/mydb"
```


Example:

```lex
conn, err = dbOpen("mssql", "server=myserver;database=Sales;user id=sa;password=Pass1!")
if err != null { println(err.message)  return }
```


<sub>Source: [`eval/builtins_sql.go:189`](../eval/builtins_sql.go#L189)</sub>

### `dbOpenWithPool(driver, dsn, [options?])`

dbOpenWithPool(driver, dsn, options) → (conn, err)


Like dbOpen but with explicit connection pool configuration. Options is a hash with any of these keys (all optional):

```lex
maxIdle:     max idle connections kept in pool (default 2)
maxOpen:     max open connections (0 = unlimited, default 0)
idleTimeout: seconds before an idle connection is closed (default unlimited)
lifetime:    max seconds a connection may be reused (default unlimited)
```


Example:

```lex
conn, err = dbOpenWithPool("mssql", "server=myserver;...", {
    maxIdle: 5, maxOpen: 20, idleTimeout: 300, lifetime: 3600
})
if err != null { println(err.message)  return }
```


<sub>Source: [`eval/builtins_sql.go:407`](../eval/builtins_sql.go#L407)</sub>

### `dbPing(conn)`

dbPing(conn) → (null, err)


Verifies the connection is still alive. Useful for health checks.


<sub>Source: [`eval/builtins_sql.go:231`](../eval/builtins_sql.go#L231)</sub>

### `dbQuery(conn, sql, [args])`

dbQuery(conn, sql, ?args) → (rows, err)


Executes a SELECT and returns all rows as an array of hashes. Column names become hash keys. SQL NULLs become kLex null. Pass parameters as an array — never interpolate values into the SQL string. Works on both connections and transactions.


Example:

```lex
rows, err = dbQuery(conn, "SELECT id, name FROM users WHERE active = ?", [true])
if err != null { println(err.message)  return }
for row in rows {
    println("{row["id"]}  {row["name"]}")
}
```


<sub>Source: [`eval/builtins_sql.go:258`](../eval/builtins_sql.go#L258)</sub>

### `dbQueryOne(conn, sql, [args])`

dbQueryOne(conn, sql, ?args) → (row, err)


Executes a SELECT and returns the first row as a hash, or null if no rows. Use when you expect exactly one result (e.g. lookup by primary key). Works on both connections and transactions.


Example:

```lex
row, err = dbQueryOne(conn, "SELECT * FROM users WHERE id = ?", [42])
if err != null { println(err.message)  return }
if row == null { println("not found")  return }
println(row["name"])
```


<sub>Source: [`eval/builtins_sql.go:304`](../eval/builtins_sql.go#L304)</sub>

### `dbQueryStream(conn, sql, [args])`

dbQueryStream(conn, sql, ?args) → (channel, err)


Executes a SELECT and returns a channel that yields rows one at a time. Each value sent on the channel is a hash (same format as dbQuery). Suitable for large result sets where loading all rows into memory is undesirable. Works on both connections and transactions. Break out of the for-in loop to cancel the stream early.


Example:

```lex
stream, err = dbQueryStream(conn, "SELECT id, name FROM big_table", [])
if err != null { println(err.message)  return }
for row in stream {
    println("{row["id"]}  {row["name"]}")
}
```


<sub>Source: [`eval/builtins_sql.go:596`](../eval/builtins_sql.go#L596)</sub>

### `dbRollback(tx)` → `null`

dbRollback(tx) → (null, err)


Rolls back a transaction started with dbBegin. Safe to call even if the transaction has already been committed or rolled back.


<sub>Source: [`eval/builtins_sql.go:782`](../eval/builtins_sql.go#L782)</sub>

### `dbSetTimeout(conn, ms)` → `null`

dbSetTimeout(conn, ms) → null


Sets a per-operation timeout on a connection or transaction. All subsequent dbQuery, dbQueryOne, dbExec, dbBulkInsert calls on this conn/tx will fail with DB_TIMEOUT_ERROR if they exceed ms milliseconds. Pass 0 to remove the timeout and revert to no deadline. dbBegin propagates the conn's timeout to the new transaction automatically.


Example:

```lex
dbSetTimeout(conn, 5000)   // 5 second limit per query
rows, err = dbQuery(conn, "SELECT * FROM large_table", [])
// → DB_TIMEOUT_ERROR if the query takes more than 5s
```


<sub>Source: [`eval/builtins_sql.go:666`](../eval/builtins_sql.go#L666)</sub>

## MCP Client

### `_mcpCall(client, method, params, [timeoutSec])` → `tuple`

_mcpCall(client, method, params, timeoutSec?) → (result, err)


Issue a JSON-RPC request and wait for the response. params may be a hash, array, or null. timeoutSec defaults to 60. result is the parsed MCP "result" value mapped to a kLex object (typically a hash).


Error codes:

```lex
MCP_CALL_RPC      — server returned an error response (code/message in err)
MCP_CALL_TIMEOUT  — no response within timeoutSec
MCP_CALL_CLOSED   — connection was closed before/during the call
```


<sub>Source: [`eval/builtins_mcp.go:587`](../eval/builtins_mcp.go#L587)</sub>

### `_mcpClose(client)` → `null`

_mcpClose(client) → null Gracefully terminate the server: close stdin (server should exit on EOF), wait up to 2s, then SIGKILL. Idempotent.


<sub>Source: [`eval/builtins_mcp.go:664`](../eval/builtins_mcp.go#L664)</sub>

### `_mcpInfo(client)`

_mcpInfo(client) → hash {name, version, protocol} Server identity as reported in the initialize response.


<sub>Source: [`eval/builtins_mcp.go:678`](../eval/builtins_mcp.go#L678)</sub>

### `_mcpNotifications(client)`

_mcpNotifications(client) → channel Returns the notification channel for this client. Each value is a hash {"method": "...", "params": ...} matching the JSON-RPC envelope. The channel is closed automatically when the server exits.


<sub>Source: [`eval/builtins_mcp.go:703`](../eval/builtins_mcp.go#L703)</sub>

### `_mcpNotify(client, method, params)`

_mcpNotify(client, method, params) → (null, err) Fire-and-forget notification. Server's reply (if any) goes to the notification channel rather than back to the caller.


<sub>Source: [`eval/builtins_mcp.go:639`](../eval/builtins_mcp.go#L639)</sub>

### `_mcpSpawn(cmd, args, [opts])`

_mcpSpawn(cmd, args, opts?) → (mcpClient, err)


Spawn an MCP server subprocess and complete the initialize handshake. cmd  — string, executable name (resolved via PATH) args — array of strings opts — optional hash:

```lex
       env          : hash string→string of additional env vars
       timeout_sec  : initialize-handshake timeout (default 30s)
       notif_buffer : notification channel size (default 32)
```


On success returns the MCPClient ready for _mcpCall. On failure the subprocess is reaped and a typed error is returned. Error codes:

```lex
MCP_SPAWN_FAILED — exec.Start error
MCP_INIT_FAILED  — initialize RPC returned an error or timed out
```


<sub>Source: [`eval/builtins_mcp.go:454`](../eval/builtins_mcp.go#L454)</sub>

## Native Bridges

### `bridgeCall(bridge, fn, args)`

── bridgeCall(bridge, fn, args, timeoutSec?) → (result, err) ────────────


Concurrent-call safe: multiple async tasks may call the same bridge simultaneously. Writes are serialised (writeMu) but waits are per-call.


<sub>Source: [`eval/builtins_bridge.go:1356`](../eval/builtins_bridge.go#L1356)</sub>

### `bridgeClose(bridge)` → `null`

── bridgeClose(bridge) → null ────────────────────────────────────────────


<sub>Source: [`eval/builtins_bridge.go:1582`](../eval/builtins_bridge.go#L1582)</sub>

### `bridgeInfo(bridge)`

── bridgeInfo(bridge) → hash ────────────────────────────────────────────


Returns the protocol metadata negotiated during the __hello__ handshake: protocol version, the set of capabilities both sides agreed on, and optional helper identity fields. A bridge that didn't reply to __hello__ is reported as protocol 0 with an empty capabilities array — backward compatible by construction.


Shape:

```lex
{
  "protocol":         1,
  "capabilities":     ["schema", "binary"],
  "helper":           "klex_bridge.py/0.7.0",
  "language":         "python",
  "language_version": "3.12.4"
}
```


<sub>Source: [`eval/builtins_bridge.go:1741`](../eval/builtins_bridge.go#L1741)</sub>

### `bridgeMetrics(bridge)`

── bridgeMetrics(bridge) → hash ─────────────────────────────────────────


Snapshot the bridge's observability counters and per-function latency percentiles. Cheap to call (one mutex acquisition + one sort per function on the latency samples) so a dashboard polling every second adds no measurable overhead.


Shape:

```lex
{
  "calls_total":     N,
  "calls_inflight":  M,
  "calls_failed":    K,
  "streams_total":   S,
  "bytes_sent":      bytesOut,
  "bytes_received":  bytesIn,
  "errors_by_code":  {"BRIDGE_TIMEOUT": 4, ...},
  "per_function":    {
     "add":   {"count": 230, "errors": 2, "p50_ms": 12.0, "p95_ms": 84.0, "p99_ms": 230.0},
     ...
  }
}
```


Percentiles are computed from a 256-sample circular buffer of recent call latencies (in milliseconds). With fewer than 256 calls, the percentiles use what's available.


<sub>Source: [`eval/builtins_bridge.go:1811`](../eval/builtins_bridge.go#L1811)</sub>

### `bridgeNotifications(bridge)`

── bridgeNotifications(bridge) → channel ────────────────────────────────


Returns the notification channel for this bridge. The channel receives every {"notif": ...} message the bridge subprocess emits. The channel is closed when the bridge closes (clean EOF for for-in loops).


Call this before starting the long operation; notifications emitted before the first call are buffered (256 items, drop-newest).


Example:

```lex
notifCh = bridgeNotifications(bridge)
async(fn() {
    msg, ok = recv(notifCh)
    while ok {
        println(msg["done"])
        msg, ok = recv(notifCh)
    }
})
result, err = bridgeCall(bridge, "long_job", [arg])
```


<sub>Source: [`eval/builtins_bridge.go:1648`](../eval/builtins_bridge.go#L1648)</sub>

### `bridgeOpen(transport)`

── bridgeOpen(transport: hash) → (bridge, err) ──────────────────────────


Transport-polymorphic bridge constructor. Dispatches on transport.kind:

```lex
"subprocess" — spawn a subprocess and speak the kLex bridge protocol
               over stdin/stdout (the only kind implemented today)
"worker"     — Web Worker (browser only; Phase 3 — not yet implemented)
"remote"     — TCP/QUIC remote bridge (future)
```


Named `bridgeOpen` (not `bridge`) to match the bridgeXxx family (bridgeClose / bridgeCall / bridgeStream / …) and to avoid shadowing the natural variable name. `let bridge, err = bridgeOpen(...)` is the idiomatic call shape.


Subprocess transport hash schema:

```lex
kind:             "subprocess"  (required)
cmd:              string         (required — executable name or path)
args:             [string, ...]  (optional — argv tail)
timeout_seconds:  number         (optional — default per-call timeout)
max_response_mb:  int            (optional — wire-frame cap, 1..256)
stderr_log:       string         (optional — mirror bridge stderr here)
```


Per FROG decision #5, key validation is STRICT — any key not listed above yields BRIDGE_TRANSPORT_MISCONFIGURED naming the bad key, so typos like "tiemout_seconds" fail loud instead of silently disabling the option.


Example (kLex hash literals require quoted string keys):

```lex
let bridge, err = bridgeOpen({
    "kind": "subprocess",
    "cmd": "python3",
    "args": ["yara_bridge.py"],
    "timeout_seconds": 30
})
```


See docs/BRIDGE_API_DESIGN.MD for the full design rationale.


<sub>Source: [`eval/builtins_bridge.go:1341`](../eval/builtins_bridge.go#L1341)</sub>

### `bridgeSchema(bridge)` → `null`

── bridgeSchema(bridge, fn?) → hash | null ───────────────────────────────


Returns the schema map declared by the bridge (via __schema__) for introspection. With one argument, returns a hash of every handler keyed by name. With two arguments, returns the single handler's schema or null if it isn't declared. Returns null overall when the bridge doesn't expose schemas.


Each schema hash is shaped:

```lex
{ "args": [[name, type], ...], "returns": type }
```


<sub>Source: [`eval/builtins_bridge.go:1700`](../eval/builtins_bridge.go#L1700)</sub>

### `bridgeStderr(bridge)` → `array`

── bridgeStderr(bridge) → array of strings ───────────────────────────────


<sub>Source: [`eval/builtins_bridge.go:1667`](../eval/builtins_bridge.go#L1667)</sub>

### `bridgeStream(bridge, fn, args, [timeout])`

── bridgeStream(bridge, fn, args) → (channel, err) ─────────────────────


Calls a STREAMING handler on the bridge subprocess. Returns immediately with a kLex channel; the bridge produces items in the background and the reader goroutine delivers each to the channel.


Consumers drain via `for item in ch { ... }`. The channel closes on clean end-of-stream. Mid-stream errors are delivered as the final item — an *Error value the consumer can detect with `type(item) == "ERROR"`. Breaking out of the for-in loop cancels delivery (further items are dropped) but does not currently signal the bridge to stop producing — that's a Phase 3.5 follow-up.


The handler on the bridge side must be registered with @stream_handler (or register(..., stream=True)). Calling a non-streaming handler via bridgeStream returns a BRIDGE_ERROR with a clear message.


Errors returned in the second tuple element (before any items arrive):

```lex
BRIDGE_CLOSED   — bridge already closed or stdin write failed
BRIDGE_TAINTED  — bridge is unusable after a prior fatal error
BRIDGE_ERROR    — marshal failed
```


<sub>Source: [`eval/builtins_bridge.go:1931`](../eval/builtins_bridge.go#L1931)</sub>

## Native Bridge Pools

### `bridgePool(n, transport, [opts])`

── bridgePool(n, transport, opts?) → (pool, err) ────────────────────────


Starts `n` identical bridges from a transport hash and returns them as a pool. Transport hash takes the same shape as bridgeOpen() — kind, cmd, args, plus optional timeout_seconds / max_response_mb / stderr_log.


Optional opts hash holds pool-only configuration:

```lex
init  — callable run once on each bridge after spawn. Receives the
        bridge as its single argument. If it returns an Error (or a
        Tuple whose second element is an Error — the bridgeCall shape),
        that bridge is marked dead in the pool. Use for loading YARA
        rules, opening connections, etc.
```


Example:

```lex
bridgePool(16, {
    "kind": "subprocess",
    "cmd":  "python3",
    "args": ["yara_bridge.py"],
    "timeout_seconds": 30
}, {
    "init": fn(b) { return bridgeCall(b, "load", ["rules.yar"]) }
})
```


<sub>Source: [`eval/builtins_bridge_pool.go:127`](../eval/builtins_bridge_pool.go#L127)</sub>

### `bridgePoolCall(pool, fn, args)`

── bridgePoolCall(pool, fn, args, timeoutSec?) → (result, err) ──────────


<sub>Source: [`eval/builtins_bridge_pool.go:223`](../eval/builtins_bridge_pool.go#L223)</sub>

### `bridgePoolClose(pool)` → `null`

── bridgePoolClose(pool) → null ─────────────────────────────────────────


<sub>Source: [`eval/builtins_bridge_pool.go:265`](../eval/builtins_bridge_pool.go#L265)</sub>

### `bridgePoolHealth(pool)`

── bridgePoolHealth(pool) → {size, alive, dead} ─────────────────────────


<sub>Source: [`eval/builtins_bridge_pool.go:291`](../eval/builtins_bridge_pool.go#L291)</sub>

### `bridgePoolStderr(pool)` → `array`

── bridgePoolStderr(pool) → array ─────────────────────────────────────── Concatenates the stderr tail of every member, prefixed with "[N] " so you can tell which subprocess emitted which line.


<sub>Source: [`eval/builtins_bridge_pool.go:310`](../eval/builtins_bridge_pool.go#L310)</sub>

### `bridgePoolStream(pool, fn, args, [timeout])`

── bridgePoolStream(pool, fn, args, timeout?) → (channel, err) ──────────


<sub>Source: [`eval/builtins_bridge_pool.go:245`](../eval/builtins_bridge_pool.go#L245)</sub>

## Parallel Processing

### `parallelArrayMap(arr, fn)` → `array`

parallelArrayMap returns a new array where each element is fn(value, index) of the corresponding input element. Element order is preserved. Uses runtime.NumCPU() goroutines.


Use this when you need a transformed copy without mutating the input:

```lex
doubled = parallelArrayMap(nums, fn(v, i) { v * 2 })
```


<sub>Source: [`eval/builtins_parallel.go:157`](../eval/builtins_parallel.go#L157)</sub>

### `parallelArrayReduce(arr, fn, init)`

parallelArrayReduce performs a parallel reduction over the array.


CONSTRAINTS (both required for correct results):

```lex
1. fn(accumulator, element) MUST be ASSOCIATIVE
   (e.g. +, *, max — chunk order is not guaranteed).
2. `initial` MUST be the IDENTITY for fn
   (0 for +, 1 for *, "" for string concat, [] for array concat).
```


Why identity matters: each worker starts its own chunk from `initial`, and the final serial pass also folds `initial` in once more. If you pass a non-identity init like 100 for +, you get 100 applied (numWorkers + 1) times instead of once. The result will not equal a serial reduce.


For non-identity inits, use stdlib/parallel.lex's parallel_reduce, which takes a separate mergeFn and applies init exactly once.


```lex
total = parallelArrayReduce(nums, fn(a, b) { a + b }, 0)
```


<sub>Source: [`eval/builtins_parallel.go:237`](../eval/builtins_parallel.go#L237)</sub>

### `parallelArrayUpdate(arr, fn)`

parallelArrayUpdate mutates each element in-place using fn(value, index). The function's return value replaces the element at that index. Uses runtime.NumCPU() goroutines; each works on a contiguous chunk.


Use this for dense data-parallel updates (e.g. virus map decay):

```lex
parallelArrayUpdate(cells, fn(v, i) { v * 0.5 })
```


Returns the same array (mutated). On error, returns the first error encountered; other workers may have completed their chunks before the error is observed.


<sub>Source: [`eval/builtins_parallel.go:90`](../eval/builtins_parallel.go#L90)</sub>

## Atomic Arrays

### `atomicAdd(arr, idx, delta)` → `int`

atomicAdd(arr, idx, delta) -> new_value Atomically adds delta to the value at idx and returns the result. For AtomicFloatArray, uses a CAS retry loop (lock-free but may retry under contention).


<sub>Source: [`eval/builtins_atomic.go:167`](../eval/builtins_atomic.go#L167)</sub>

### `atomicCAS(arr, idx, old, new)` → `bool`

atomicCAS(arr, idx, old, new) -> bool Compare-and-swap. If the current value at idx equals old, replaces it with new and returns true. Otherwise returns false. Used for building custom lock-free algorithms.


<sub>Source: [`eval/builtins_atomic.go:219`](../eval/builtins_atomic.go#L219)</sub>

### `atomicFloatArray(size, [initial])`

atomicFloatArray(size, [initial]) -> AtomicFloatArray Creates a fixed-size lock-free float64 array. Optional initial value fills every slot (default 0.0).


<sub>Source: [`eval/builtins_atomic.go:62`](../eval/builtins_atomic.go#L62)</sub>

### `atomicIntArray(size, [initial])`

atomicIntArray(size, [initial]) -> AtomicIntArray Creates a fixed-size lock-free integer array. Optional initial value fills every slot (default 0).


<sub>Source: [`eval/builtins_atomic.go:31`](../eval/builtins_atomic.go#L31)</sub>

### `atomicLoad(arr, idx)` → `int`

atomicLoad(arr, idx) -> value Atomically reads and returns the value at the given index. Works on AtomicIntArray (returns Integer) or AtomicFloatArray (returns Float).


<sub>Source: [`eval/builtins_atomic.go:97`](../eval/builtins_atomic.go#L97)</sub>

### `atomicStore(arr, idx, value)` → `null`

atomicStore(arr, idx, value) -> null Atomically writes the value at the given index.


<sub>Source: [`eval/builtins_atomic.go:125`](../eval/builtins_atomic.go#L125)</sub>

### `parallelArrayForEach(arr, fn)` → `null`

parallelArrayForEach(arr, fn) -> null Like parallelArrayMap but discards return values. Use this when the callback's purpose is side effects (e.g. atomic updates to shared state) rather than producing a transformed array. Saves the allocation of a result array.


<sub>Source: [`eval/builtins_atomic.go:285`](../eval/builtins_atomic.go#L285)</sub>

## Concurrent Hash

### `atomicHashAdd(ch, key, delta)`

atomicHashAdd(ch, key, delta) -> new float value Same as atomicHashIncr but for floats. Stores Float values; if key doesn't exist, treats current as 0.0.


<sub>Source: [`eval/builtins_concurrent_hash.go:83`](../eval/builtins_concurrent_hash.go#L83)</sub>

### `atomicHashCAS(ch, key, old, new)` → `bool`

atomicHashCAS(ch, key, old, new) -> bool Compare-and-swap the value at key. Returns true if swap succeeded (current value was equal to old AND key existed), false otherwise. Equality is by structural value comparison via objectsEqual.


<sub>Source: [`eval/builtins_concurrent_hash.go:138`](../eval/builtins_concurrent_hash.go#L138)</sub>

### `atomicHashIncr(ch, key, delta)`

atomicHashIncr(ch, key, delta) -> new integer value Atomically increments the integer at key by delta. If the key doesn't exist, treats current value as 0. Uses sync.Map CAS-loop internally; safe under concurrent access from any number of goroutines.


<sub>Source: [`eval/builtins_concurrent_hash.go:40`](../eval/builtins_concurrent_hash.go#L40)</sub>

### `concurrentHash(…)`

concurrentHash() -> empty ConcurrentHash


<sub>Source: [`eval/builtins_concurrent_hash.go:29`](../eval/builtins_concurrent_hash.go#L29)</sub>

### `quiesceLen(ch)` → `int`

quiesceLen(ch) -> integer


Exact entry count for a ConcurrentHash. Unlike len(ch), which reads the atomic Cnt counter and can briefly diverge from reality by the number of in-flight Store/Delete operations, quiesceLen walks the entire sync.Map via Range and counts live entries. O(n) in the size of the map.


Use after a known quiescent point (all writer tasks awaited) when you need a guaranteed-accurate size — e.g. for invariants, assertions, or "did the pipeline emit exactly N items" checks. Prefer len(ch) for progress indicators and other use-cases where ~1 off is fine.


Concurrent writes during the Range are not blocked; an entry inserted or deleted mid-walk may or may not be counted (sync.Map.Range semantics). For a fully consistent snapshot, ensure no writers are active.


<sub>Source: [`eval/builtins_concurrent_hash.go:186`](../eval/builtins_concurrent_hash.go#L186)</sub>

## Graphics

### `arc(x, y, r, startAngle, endAngle)` → `null`

arc(x, y, r, startAngle, endAngle) → null Draws an arc centred at (x, y) with radius r from startAngle to endAngle (radians). Angles follow screen-space convention: 0 = right, π/2 = down (y-down). With fill: draws a filled sector (pie slice). With stroke: draws the arc line only.


<sub>Source: [`eval/builtins_graphics.go:1153`](../eval/builtins_graphics.go#L1153)</sub>

### `background(…)` → `null`

background(gray: float) | background(r, g, b: float) | background(r, g, b, a: float) -> null


Clear the screen with a colour. Values are in [0.0, 1.0]. Single argument sets a grey level.


<sub>Source: [`eval/builtins_graphics.go:776`](../eval/builtins_graphics.go#L776)</sub>

### `blendMode(mode)` → `null`

blendMode(mode) — set the OpenGL blend equation for subsequent draw calls. "normal"   — src*alpha + dst*(1-alpha)   (default) "add"      — src*alpha + dst             (fire, glow, light) "multiply" — dst * src                  (shadows) "screen"   — 1 - (1-src)*(1-dst)        (brightening)


<sub>Source: [`eval/builtins_graphics.go:865`](../eval/builtins_graphics.go#L865)</sub>

### `circle(x, y, radius)` → `null`

circle(x: float, y: float, radius: float) -> null


Draw a circle centred at (x, y). Approximated with 64 segments. Respects fill and stroke state.


<sub>Source: [`eval/builtins_graphics.go:998`](../eval/builtins_graphics.go#L998)</sub>

### `drawImage(img, x, y)` → `null`

drawImage(img: image, x: float, y: float) | drawImage(img: image, x, y, w, h: float) -> null


Draw an image at (x, y). Optional w and h scale the image; defaults to the image's natural size.


<sub>Source: [`eval/builtins_graphics.go:1455`](../eval/builtins_graphics.go#L1455)</sub>

### `drawParticles(xs, ys, rs, gs, bs, alphas, count, pointSize)` → `null`

drawParticles(xs, ys, rs, gs, bs, alphas, count, pointSize) Renders up to count particles in a SINGLE draw call using per-vertex colour. xs/ys/rs/gs/bs/alphas are kLex arrays (SoA layout — matches ParticlePool). Skips particles with alpha < 0.01 automatically.


<sub>Source: [`eval/builtins_graphics.go:925`](../eval/builtins_graphics.go#L925)</sub>

### `droppedFiles(…)` → `array`

droppedFiles() → array of strings Returns all file paths dropped onto the window since the last call, then clears the buffer. Returns an empty array if nothing was dropped.


<sub>Source: [`eval/builtins_graphics.go:1910`](../eval/builtins_graphics.go#L1910)</sub>

### `ellipse(x, y, rx, ry)` → `null`

ellipse(x: float, y: float, rx: float, ry: float) -> null


Draw an ellipse centred at (x, y) with horizontal radius rx and vertical radius ry. Respects fill and stroke state.


<sub>Source: [`eval/builtins_graphics.go:1118`](../eval/builtins_graphics.go#L1118)</sub>

### `fill(…)` → `null`

fill(gray: float) | fill(r, g, b: float) | fill(r, g, b, a: float) -> null


Set the fill colour for subsequent shapes. Values in [0.0, 1.0].


<sub>Source: [`eval/builtins_graphics.go:786`](../eval/builtins_graphics.go#L786)</sub>

### `fontCharHeight(…)` → `int`

fontCharHeight() -> int


Return the pixel height of one character in the embedded monospace font at scale 1. Multiply by scale to get the display height.


<sub>Source: [`eval/builtins_graphics.go:1742`](../eval/builtins_graphics.go#L1742)</sub>

### `fontCharWidth(…)` → `int`

fontCharWidth() -> int


Return the pixel width of one character in the embedded monospace font at scale 1. Multiply by scale to get the display width.


<sub>Source: [`eval/builtins_graphics.go:1738`](../eval/builtins_graphics.go#L1738)</sub>

### `frameCount(…)` → `int`

frameCount() -> int


Return the number of frames rendered since the window opened.


<sub>Source: [`eval/builtins_graphics.go:1636`](../eval/builtins_graphics.go#L1636)</sub>

### `frameRate(fps)` → `null`

frameRate(fps: float) -> null


Cap the render loop to fps frames per second. Pass 0 to revert to vsync. Safe to call before window() — the cap is applied when the window opens.\n\nExample: frameRate(30) limits to 30fps.


<sub>Source: [`eval/builtins_graphics.go:888`](../eval/builtins_graphics.go#L888)</sub>

### `gradient(x, y, w, h, color1, color2, dir)` → `null`

gradient(x, y, w, h, color1, color2, dir) → null Fills a rectangle with a two-color linear gradient. color1/color2 are [r,g,b,a] float arrays; dir is "h" (left→right) or "v" (top→bottom).


<sub>Source: [`eval/builtins_graphics.go:1041`](../eval/builtins_graphics.go#L1041)</sub>

### `imageFromRgba(bytes, width, height)`

imageFromRgba(bytes, width, height) → image


Wraps raw RGBA8 pixel bytes (row-major, exactly width*height*4 bytes) into a kLex Image handle without going through PNG/JPEG decode. The counterpart to loadImage(pngBytes) for sources that already have raw pixels — Metal surface readbacks, procedural generators, video frames, screen captures.


Pixels are copied into the Image's own buffer so the caller can mutate the source bytes afterwards without affecting the image.


<sub>Source: [`eval/builtins_graphics.go:1300`](../eval/builtins_graphics.go#L1300)</sub>

### `imageSize(img)` → `tuple`

imageSize(img) → (width, height)


Returns the pixel dimensions of a kLex Image as a two-element tuple. Cheap (struct field reads); safe to call before the image's first drawImage (no GPU upload required).


<sub>Source: [`eval/builtins_graphics.go:1377`](../eval/builtins_graphics.go#L1377)</sub>

### `imageToRgba(img)` → `bytes`

imageToRgba(img) → bytes


Returns the Image's raw RGBA8 pixels (row-major, width*height*4 bytes). The natural counterpart to imageFromRgba — round-tripping `imageFromRgba(imageToRgba(img), w, h)` produces an equivalent image.


Source priority:

```lex
1. img.pixels  — pre-GPU-upload CPU bytes (present after loadImage
                 until the first drawImage uploads to a texture).
2. img.TextureID — once uploaded, reads back from the GPU via
                   glGetTexImage. MUST be called from the GL
                   thread (i.e. inside a draw frame).
```


<sub>Source: [`eval/builtins_graphics.go:1343`](../eval/builtins_graphics.go#L1343)</sub>

### `keyDown(key)` → `bool`

keyDown(key: string) -> bool


Return true while the named key is physically held down.\n\nKey names: \


<sub>Source: [`eval/builtins_graphics.go:1598`](../eval/builtins_graphics.go#L1598)</sub>

### `keyPressed(key)` → `bool`

keyPressed(key: string) -> bool


Return true only on the single frame the key was first pressed (one-shot). Resets to false the next frame.


<sub>Source: [`eval/builtins_graphics.go:1616`](../eval/builtins_graphics.go#L1616)</sub>

### `line(x1, y1, x2, y2)` → `null`

line(x1: float, y1: float, x2: float, y2: float) -> null


Draw a line from (x1, y1) to (x2, y2) using the current stroke colour.


<sub>Source: [`eval/builtins_graphics.go:1103`](../eval/builtins_graphics.go#L1103)</sub>

### `loadFont(path, [ptSize])`

loadFont(path) or loadFont(path, ptSize) → Font Loads a TrueType or OpenType font from disk and builds a proportional SDF atlas. ptSize defaults to 16. Call before or inside window() — GPU upload is deferred to the first textFont() call.


<sub>Source: [`eval/builtins_graphics.go:1750`](../eval/builtins_graphics.go#L1750)</sub>

### `loadImage(path)`

loadImage(path: string) -> image


Load a PNG or JPEG from disk and return an image handle. Safe to call before window() — texture upload is deferred to the first drawImage() call.


<sub>Source: [`eval/builtins_graphics.go:1251`](../eval/builtins_graphics.go#L1251)</sub>

### `mouseClicked(…)` → `bool`

mouseClicked() -> bool


Return true only on the single frame the left mouse button was first pressed (one-shot). Resets to false the next frame.\n\nUse this for button clicks — mouseDown() fires every frame while held.


<sub>Source: [`eval/builtins_graphics.go:1903`](../eval/builtins_graphics.go#L1903)</sub>

### `mouseClicked(…)` → `bool`

mouseClicked() -> bool


Return true only on the single frame the left mouse button was first pressed (one-shot). Resets to false the next frame.\n\nUse this for button clicks — mouseDown() fires every frame while held.


<sub>Source: [`eval/builtins_graphics.go:1655`](../eval/builtins_graphics.go#L1655)</sub>

### `mouseDown(…)` → `bool`

mouseDown() -> bool


Return true if the left mouse button is currently pressed.


<sub>Source: [`eval/builtins_graphics.go:1648`](../eval/builtins_graphics.go#L1648)</sub>

### `mouseRightClicked(…)` → `bool`

mouseRightClicked() -> bool


Return true only on the single frame the right mouse button was first pressed (one-shot). Resets to false the next frame.


<sub>Source: [`eval/builtins_graphics.go:1662`](../eval/builtins_graphics.go#L1662)</sub>

### `mouseRightDown(…)` → `bool`

mouseRightDown() -> bool


Return true if the right mouse button is currently held down.


<sub>Source: [`eval/builtins_graphics.go:1669`](../eval/builtins_graphics.go#L1669)</sub>

### `mouseScrollX(…)` → `float`

mouseScrollX() -> float


Return the horizontal mouse wheel delta for this frame (trackpad two-finger swipe or horizontal scroll wheel). Resets to 0 each frame.


<sub>Source: [`eval/builtins_graphics.go:1680`](../eval/builtins_graphics.go#L1680)</sub>

### `mouseScrollY(…)` → `float`

mouseScrollY() -> float


Return the vertical mouse wheel delta for this frame. Positive = scrolled up/forward, negative = scrolled down/backward. Resets to 0 each frame.\n\nExample: scroll offset += mouseScrollY() * speed


<sub>Source: [`eval/builtins_graphics.go:1676`](../eval/builtins_graphics.go#L1676)</sub>

### `mouseX(…)` → `float`

mouseX() -> float


Return the current mouse X position in window pixels.


<sub>Source: [`eval/builtins_graphics.go:1640`](../eval/builtins_graphics.go#L1640)</sub>

### `mouseX(…)` → `float`

mouseX() -> float


Return the current mouse X position in window pixels.


<sub>Source: [`eval/builtins_graphics.go:1895`](../eval/builtins_graphics.go#L1895)</sub>

### `mouseY(…)` → `float`

mouseY() -> float


Return the current mouse Y position in window pixels.


<sub>Source: [`eval/builtins_graphics.go:1899`](../eval/builtins_graphics.go#L1899)</sub>

### `mouseY(…)` → `float`

mouseY() -> float


Return the current mouse Y position in window pixels.


<sub>Source: [`eval/builtins_graphics.go:1644`](../eval/builtins_graphics.go#L1644)</sub>

### `noFill(…)` → `null`

noFill() -> null


Disable fill so shapes are drawn outline-only.


<sub>Source: [`eval/builtins_graphics.go:796`](../eval/builtins_graphics.go#L796)</sub>

### `noShadow(…)` → `null`

noShadow() → null — disable drop shadows.


<sub>Source: [`eval/builtins_graphics.go:855`](../eval/builtins_graphics.go#L855)</sub>

### `noStroke(…)` → `null`

noStroke() -> null


Disable stroke so shapes are drawn fill-only.


<sub>Source: [`eval/builtins_graphics.go:811`](../eval/builtins_graphics.go#L811)</sub>

### `point(x, y)` → `null`

point(x: float, y: float) -> null


Draw a single pixel at (x, y) using the current stroke colour. Point size is controlled by strokeWeight().


<sub>Source: [`eval/builtins_graphics.go:909`](../eval/builtins_graphics.go#L909)</sub>

### `polygon(points)` → `null`

polygon(points: array) -> null


Draw an arbitrary polygon from a flat array of x,y pairs: [x1,y1, x2,y2, ...]. Must have at least 3 points (6 values). Respects fill and stroke state.


<sub>Source: [`eval/builtins_graphics.go:1190`](../eval/builtins_graphics.go#L1190)</sub>

### `popClip(…)` → `null`

popClip() — pop the top clip rect and restore the one below it (or disable clipping).


<sub>Source: [`eval/builtins_graphics.go:1718`](../eval/builtins_graphics.go#L1718)</sub>

### `popMatrix(…)` → `null`

popMatrix() -> null


Restore the transform matrix saved by the last pushMatrix() call.


<sub>Source: [`eval/builtins_graphics.go:1518`](../eval/builtins_graphics.go#L1518)</sub>

### `pushClip(x, y, w, h)` → `null`

pushClip(x, y, w, h) — enable OpenGL scissor rect (clips drawing to this region) pushClip(x, y, w, h) — push a clipping rectangle onto the scissor stack. If a clip is already active the new rect is intersected with it, so nested clips always stay within their parent. Pair every pushClip with a popClip.


<sub>Source: [`eval/builtins_graphics.go:1688`](../eval/builtins_graphics.go#L1688)</sub>

### `pushMatrix(…)` → `null`

pushMatrix() -> null


Save the current transform matrix onto the stack. Pair with popMatrix() to restore it.


<sub>Source: [`eval/builtins_graphics.go:1512`](../eval/builtins_graphics.go#L1512)</sub>

### `rect(x, y, w, h)` → `null`

rect(x: float, y: float, w: float, h: float) -> null


Draw a rectangle. Origin is the top-left corner. Respects fill and stroke state.


<sub>Source: [`eval/builtins_graphics.go:970`](../eval/builtins_graphics.go#L970)</sub>

### `rotate(angle)` → `null`

rotate(angle: float) -> null


Rotate the drawing axes by angle radians (clockwise, y-down). Affects all subsequent draw calls until popMatrix().


<sub>Source: [`eval/builtins_graphics.go:1491`](../eval/builtins_graphics.go#L1491)</sub>

### `roundedRect(x, y, w, h, r)` → `null`

roundedRect(x, y, w, h, r: float) -> null


Draw a rounded rectangle with corner radius r. Rendered using a signed distance field (SDF) shader — mathematically perfect anti-aliased edges at any size. Respects fill and stroke state.


<sub>Source: [`eval/builtins_graphics.go:1019`](../eval/builtins_graphics.go#L1019)</sub>

### `saveImage(img, path)` → `null`

saveImage(img, path) — encode an Image to disk. Format is chosen from the path extension: .png (default, lossless), .jpg/.jpeg (quality 92), .gif (palette-quantised). Returns null on success, runtime error on failure. The image's RGBA pixels are encoded directly — no separate readback path needed because loadImage already keeps them in memory.


<sub>Source: [`eval/builtins_graphics.go:1396`](../eval/builtins_graphics.go#L1396)</sub>

### `scale(sx, sy)` → `null`

scale(sx: float, sy: float) -> null


Scale the drawing axes by (sx, sy). Affects all subsequent draw calls until popMatrix().


<sub>Source: [`eval/builtins_graphics.go:1501`](../eval/builtins_graphics.go#L1501)</sub>

### `shadow(offsetX, offsetY, blur)` → `null`

shadow(offsetX, offsetY, blur) → null              — black @ 50% alpha shadow(offsetX, offsetY, blur, r, g, b, a) → null — explicit colour Enable drop shadows on rect(), circle(), and roundedRect() calls.


<sub>Source: [`eval/builtins_graphics.go:827`](../eval/builtins_graphics.go#L827)</sub>

### `stroke(…)` → `null`

stroke(gray: float) | stroke(r, g, b: float) | stroke(r, g, b, a: float) -> null


Set the stroke (outline) colour for subsequent shapes. Values in [0.0, 1.0].


<sub>Source: [`eval/builtins_graphics.go:801`](../eval/builtins_graphics.go#L801)</sub>

### `strokeWeight(w)` → `null`

strokeWeight(w: float) -> null


Set the stroke line width in pixels.


<sub>Source: [`eval/builtins_graphics.go:816`](../eval/builtins_graphics.go#L816)</sub>

### `text(str, x, y, [scale])`

text(str, x, y) — draw a string using the embedded 8x8 bitmap font. Optional 4th argument sets scale (default 1 = 8px per character).


<sub>Source: [`eval/builtins_graphics.go:1530`](../eval/builtins_graphics.go#L1530)</sub>

### `textFont(font, str, x, y, [scale])`

textFont(font, str, x, y) or textFont(font, str, x, y, scale) → null Draws a string using a font returned by loadFont(). Respects fill colour. scale defaults to 1. Defers GPU upload on first call.


<sub>Source: [`eval/builtins_graphics.go:1779`](../eval/builtins_graphics.go#L1779)</sub>

### `textWidth(font, str, [scale])` → `float`

textWidth(font, str) or textWidth(font, str, scale) → float Returns the pixel width of str rendered with font at the given scale (default 1). Use this to right-align or center text before calling textFont().


<sub>Source: [`eval/builtins_graphics.go:1865`](../eval/builtins_graphics.go#L1865)</sub>

### `translate(x, y)` → `null`

translate(x: float, y: float) -> null


Move the drawing origin by (x, y). Affects all subsequent draw calls until popMatrix().


<sub>Source: [`eval/builtins_graphics.go:1480`](../eval/builtins_graphics.go#L1480)</sub>

### `triangle(x1, y1, x2, y2, x3, y3)` → `null`

triangle(x1, y1, x2, y2, x3, y3: float) -> null


Draw a triangle. Respects fill and stroke state.


<sub>Source: [`eval/builtins_graphics.go:1228`](../eval/builtins_graphics.go#L1228)</sub>

### `winHeight(…)` → `int`

winHeight() -> int


Return the window height in pixels.


<sub>Source: [`eval/builtins_graphics.go:1734`](../eval/builtins_graphics.go#L1734)</sub>

### `winWidth(…)` → `int`

winWidth() -> int


Return the window width in pixels.


<sub>Source: [`eval/builtins_graphics.go:1730`](../eval/builtins_graphics.go#L1730)</sub>

### `window(width, height, title, drawFn)`

── window ────────────────────────────────────────────────────────────── window(width, height, title, drawFn) Opens an OpenGL window and calls drawFn(frameCount) every frame until closed.


<sub>Source: [`eval/builtins_graphics.go:451`](../eval/builtins_graphics.go#L451)</sub>

## UI Widgets

### `uiBegin(…)` → `null`

uiBegin() — reset UI state at the start of each draw loop


<sub>Source: [`eval/builtins_ui.go:51`](../eval/builtins_ui.go#L51)</sub>

### `uiEnd(…)`

uiEnd() — finalize UI frame, update hover state


<sub>Source: [`eval/builtins_ui.go:86`](../eval/builtins_ui.go#L86)</sub>

## Agent

### `_setAsyncDoneHook(…)`

_setAsyncDoneHook(hookFn: fn | null) -> null


Phase 2 agentic hook primitive. Registers `hookFn` (or null to clear) as the on_async_done handler. Invoked from the TASK'S OWN goroutine immediately after the body returns. Use task_id to pair with the earlier on_async_spawn event.\n\nEvent hash shape:\n  {\n    \


<sub>Source: [`eval/builtins_agent.go:512`](../eval/builtins_agent.go#L512)</sub>

### `_setAsyncSpawnHook(…)`

_setAsyncSpawnHook(hookFn: fn | null) -> null


Phase 2 agentic hook primitive. Registers `hookFn` (or null to clear) as the on_async_spawn handler. Invoked SYNCHRONOUSLY in the spawning goroutine BEFORE the task's goroutine starts running. Each spawn carries a monotonic task_id that pairs with the on_async_done event for the same task.\n\nEvent hash shape:\n  {\n    \


<sub>Source: [`eval/builtins_agent.go:509`](../eval/builtins_agent.go#L509)</sub>

### `_setBridgeCallHook(…)`

_setBridgeCallHook(hookFn: fn | null) -> null


Phase 3 agentic hook primitive. Registers `hookFn` (or null to clear) as the on_bridge_call handler. Fires once per `_bridgeCall` AFTER the round-trip completes (bridge calls are synchronous from kLex's view).\n\nEvent hash shape:\n  {\n    \


<sub>Source: [`eval/builtins_agent.go:518`](../eval/builtins_agent.go#L518)</sub>

### `_setErrorHook(…)`

_setErrorHook(hookFn: fn | null) -> null


Phase 1 agentic hook primitive. Registers `hookFn` (or null to clear) as the on_error_bubble handler. The hook fires once per internal *Error at the moment it begins propagating up the call stack — BEFORE any safe() / `?` / top-level handler sees it. Observer-only — returning a value from the hook or producing an error inside it does not alter the original error.\n\nEvent hash shape passed to the hook:\n  {\n    \


<sub>Source: [`eval/builtins_agent.go:506`](../eval/builtins_agent.go#L506)</sub>

### `_setUiEventHook(…)`

_setUiEventHook(hookFn: fn | null) -> null


Phase 3 agentic hook primitive. Registers `hookFn` (or null to clear) as the on_ui_event handler. Fired SYNCHRONOUSLY by interactive widgets in stdlib/ui.lex (button / slider / checkbox / dropdown / textField / toggle / radio) when the user causes a state change. Pure-display widgets do NOT fire. Hover events are not emitted.\n\nEvent hash shape:\n  {\n    \


<sub>Source: [`eval/builtins_agent.go:515`](../eval/builtins_agent.go#L515)</sub>

### `_uiEvent(kind, widget, label, value, x, y)` → `null`

_uiEvent(kind: string, widget: string, label: string,

```lex
       value: any, x: int, y: int) → null
```

Producer-side primitive. Called by instrumented widgets in stdlib/ui.lex when the user causes a state change. Builds the event hash and dispatches it to the registered on_ui_event hook (if any). When no hook is registered, returns immediately without building the hash. Match the stdlib/agent.lex doc for the kind / widget vocabulary.


<sub>Source: [`eval/builtins_agent.go:543`](../eval/builtins_agent.go#L543)</sub>

### `_uiEventActive(…)` → `bool`

_uiEventActive() → BOOLEAN — used by stdlib/ui.lex widget instrumentation to skip event-hash construction when no consumer is registered. Lock-free atomic pointer load on the no-hook path.


<sub>Source: [`eval/builtins_agent.go:525`](../eval/builtins_agent.go#L525)</sub>

## Elapsed Time

### `elapsedTime(…)` → `float`

elapsedTime() → float

```lex
Wall-clock seconds since the kLex interpreter started. Sub-second
precision. Use for benchmark/timing measurements:
  let start = elapsedTime()
  // ... work ...
  let secs = elapsedTime() - start
```


<sub>Source: [`eval/builtins_elapsed_time.go:22`](../eval/builtins_elapsed_time.go#L22)</sub>

## Eval Wasm

### `_wasmBase64Decode(arg1)` → `string`

_wasmBase64Decode(encoded: string) → string Decodes a URL-safe base64 string. Returns a RuntimeError on bad input (use safe() in kLex to catch it as a (result, err) pair).


<sub>Source: [`eval/builtins_eval_wasm.go:172`](../eval/builtins_eval_wasm.go#L172)</sub>

### `_wasmBase64Encode(arg1)` → `string`

_wasmBase64Encode(text: string) → string URL-safe base64-encodes text (handles full UTF-8).


<sub>Source: [`eval/builtins_eval_wasm.go:158`](../eval/builtins_eval_wasm.go#L158)</sub>

### `_wasmCheckRunFlag(…)` → `bool`

_wasmCheckRunFlag() → bool Reads and clears window.__klex_run_requested — set by the Cmd+Enter keydown handler in index.html. Returns true once per key press.


<sub>Source: [`eval/builtins_eval_wasm.go:214`](../eval/builtins_eval_wasm.go#L214)</sub>

### `_wasmCopyToClipboard(arg1)` → `null`

_wasmCopyToClipboard(text: string) → null Writes text to the clipboard via navigator.clipboard (fire-and-forget).


<sub>Source: [`eval/builtins_eval_wasm.go:141`](../eval/builtins_eval_wasm.go#L141)</sub>

### `_wasmGetHash(…)` → `string`

_wasmGetHash() → string Returns window.location.hash without the leading '#'.


<sub>Source: [`eval/builtins_eval_wasm.go:111`](../eval/builtins_eval_wasm.go#L111)</sub>

### `_wasmGetHref(…)` → `string`

_wasmGetHref() → string Returns window.location.href — the full current page URL including hash.


<sub>Source: [`eval/builtins_eval_wasm.go:135`](../eval/builtins_eval_wasm.go#L135)</sub>

### `_wasmRequestPaste(…)` → `null`

_wasmRequestPaste() → null Reads the clipboard asynchronously and injects the text into the UI paste buffer (gfxState.pasteBuf) so the focused widget picks it up on the next frame — identical to the Cmd+V flow in the keyboard handler.


<sub>Source: [`eval/builtins_eval_wasm.go:191`](../eval/builtins_eval_wasm.go#L191)</sub>

### `_wasmSetHash(arg1)` → `null`

_wasmSetHash(hash: string) → null Sets window.location.hash (without triggering a page navigation).


<sub>Source: [`eval/builtins_eval_wasm.go:121`](../eval/builtins_eval_wasm.go#L121)</sub>

### `openURL(url)` → `null`

openURL(url) → null Opens url in a new browser tab via window.open().


<sub>Source: [`eval/builtins_eval_wasm.go:97`](../eval/builtins_eval_wasm.go#L97)</sub>

### `runScript(src)`

runScript(src) → {output, error, isError} Evaluates src in a fresh Environment. Captures println output. Returns a hash so the caller can display both stdout and errors. Scripts that call window() are rejected with a clear message — the playground canvas belongs to the IDE itself.


<sub>Source: [`eval/builtins_eval_wasm.go:42`](../eval/builtins_eval_wasm.go#L42)</sub>

## Fs Dispatch Gen

### `_fsAppend(arg1, arg2)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:226`](../eval/builtins_fs_dispatch_gen.go#L226)</sub>

### `_fsAppendBytesSync(path, bytes)`

_fsAppendBytesSync(path: string, bytes: bytes) -> (null, error)


Append bytes to a file and fsync(2) before close. Bytes are durably on disk when this function returns — safe to lose at most one call's worth of work on a power-loss crash. Use for append-only logs, vector indexes, audit trails. Adds ~1-5 ms per call; batch small writes into one call where possible.


<sub>Source: [`eval/builtins_fs_dispatch_gen.go:244`](../eval/builtins_fs_dispatch_gen.go#L244)</sub>

### `_fsChmod(arg1, arg2)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:352`](../eval/builtins_fs_dispatch_gen.go#L352)</sub>

### `_fsCopy(arg1, arg2)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:404`](../eval/builtins_fs_dispatch_gen.go#L404)</sub>

### `_fsCountLines(arg1)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:84`](../eval/builtins_fs_dispatch_gen.go#L84)</sub>

### `_fsExists(arg1)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:102`](../eval/builtins_fs_dispatch_gen.go#L102)</sub>

### `_fsListDir(arg1)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:156`](../eval/builtins_fs_dispatch_gen.go#L156)</sub>

### `_fsLstat(arg1)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:138`](../eval/builtins_fs_dispatch_gen.go#L138)</sub>

### `_fsMap(path)`

_fsMap(path) -> (content, err)


Memory-maps a file and returns its content as a string.\nThe string is backed by the mmap'd region—no copying.\nPerfect for analyzing large files in parallel (each worker accesses different ranges).


<sub>Source: [`eval/builtins_fs_dispatch_gen.go:66`](../eval/builtins_fs_dispatch_gen.go#L66)</sub>

### `_fsMkdir(arg1)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:316`](../eval/builtins_fs_dispatch_gen.go#L316)</sub>

### `_fsMkdirAll(arg1)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:334`](../eval/builtins_fs_dispatch_gen.go#L334)</sub>

### `_fsRead(arg1)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:12`](../eval/builtins_fs_dispatch_gen.go#L12)</sub>

### `_fsReadBytes(path)`

_fsReadBytes(path: string) -> (bytes, error)


Read entire file as raw bytes. Required for binary data (float32 vectors, images, encrypted blobs) that would not survive UTF-8 string coercion through _fsRead. Returns (Bytes, null) on success or (null, error) on failure.


<sub>Source: [`eval/builtins_fs_dispatch_gen.go:30`](../eval/builtins_fs_dispatch_gen.go#L30)</sub>

### `_fsReadChunk(path, offset, byteCount)`

_fsReadChunk(path, offset, byteCount) -> (content, isEOF, err)


Reads up to byteCount bytes from file starting at offset.\nReturns tuple: (content_string, isEOF_bool, error_or_null)\nisEOF is true if the returned chunk reaches the end of the file.\nUseful for streaming large files without loading entirely into memory.


<sub>Source: [`eval/builtins_fs_dispatch_gen.go:48`](../eval/builtins_fs_dispatch_gen.go#L48)</sub>

### `_fsReadDir(path)`

_fsReadDir(path) -> (array_of_info_hashes, err)


Each hash has the same keys as _fsStat. Entries whose Info() fails are skipped.


<sub>Source: [`eval/builtins_fs_dispatch_gen.go:174`](../eval/builtins_fs_dispatch_gen.go#L174)</sub>

### `_fsReadlink(arg1)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:192`](../eval/builtins_fs_dispatch_gen.go#L192)</sub>

### `_fsRemove(arg1)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:280`](../eval/builtins_fs_dispatch_gen.go#L280)</sub>

### `_fsRemoveAll(arg1)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:298`](../eval/builtins_fs_dispatch_gen.go#L298)</sub>

### `_fsRename(arg1, arg2)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:430`](../eval/builtins_fs_dispatch_gen.go#L430)</sub>

### `_fsStat(path)`

_fsStat(path) -> (info_hash, err)


info_hash keys: \


<sub>Source: [`eval/builtins_fs_dispatch_gen.go:120`](../eval/builtins_fs_dispatch_gen.go#L120)</sub>

### `_fsSymlink(arg1, arg2)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:456`](../eval/builtins_fs_dispatch_gen.go#L456)</sub>

### `_fsTmpDir(dir, pattern)`

_fsTmpDir(dir, pattern) -> (path, err)


Creates a new temp directory in dir matching pattern, returns its path.\nPass \


<sub>Source: [`eval/builtins_fs_dispatch_gen.go:386`](../eval/builtins_fs_dispatch_gen.go#L386)</sub>

### `_fsTmpFile(dir, pattern)`

_fsTmpFile(dir, pattern) -> (path, err)


Creates a new temp file in dir matching pattern, closes it, returns its path.\nPass \


<sub>Source: [`eval/builtins_fs_dispatch_gen.go:368`](../eval/builtins_fs_dispatch_gen.go#L368)</sub>

### `_fsTruncate(arg1, arg2)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:262`](../eval/builtins_fs_dispatch_gen.go#L262)</sub>

### `_fsWrite(arg1, arg2)`

_No doc comment in source. Add a `// description` block above the registration._

<sub>Source: [`eval/builtins_fs_dispatch_gen.go:208`](../eval/builtins_fs_dispatch_gen.go#L208)</sub>

## Graphics Wasm

### `arc(x, y, r, startAngle, endAngle)` → `null`

arc(x, y, r, startAngle, endAngle) — sweep an arc in radians. Matches the desktop signature; respects fill + stroke state.


<sub>Source: [`eval/builtins_graphics_wasm.go:581`](../eval/builtins_graphics_wasm.go#L581)</sub>

### `background(…)` → `null`

background(gray: float) | background(r, g, b: float) | background(r, g, b, a: float) -> null


Clear the screen with a colour. Values are in [0.0, 1.0]. Single argument sets a grey level.


<sub>Source: [`eval/builtins_graphics_wasm.go:384`](../eval/builtins_graphics_wasm.go#L384)</sub>

### `blendMode(mode)` → `null`

── blendMode(mode) ────────────────────────────────────────────────


Maps kLex blend-mode names onto Canvas2D's globalCompositeOperation. The common subset that works the same on every backend.


<sub>Source: [`eval/builtins_graphics_wasm.go:835`](../eval/builtins_graphics_wasm.go#L835)</sub>

### `circle(x, y, radius)` → `null`

circle(x: float, y: float, radius: float) -> null


Draw a circle centred at (x, y). Approximated with 64 segments. Respects fill and stroke state.


<sub>Source: [`eval/builtins_graphics_wasm.go:493`](../eval/builtins_graphics_wasm.go#L493)</sub>

### `drawImage(img, x, y)` → `null`

drawImage(img, x, y) | drawImage(img, x, y, w, h)


<sub>Source: [`eval/builtins_graphics_wasm.go:1221`](../eval/builtins_graphics_wasm.go#L1221)</sub>

### `drawParticles(xs, ys, rs, gs, bs, alphas, count, pointSize)` → `null`

drawParticles(xs, ys, rs, gs, bs, alphas, count, pointSize) → null


Browser parallel to the desktop GPU particle batch. SoA layout: xs/ys are positions; rs/gs/bs/alphas are per-particle colour components (0–1). Particles with alpha < 0.01 are skipped. Each live particle is a filled dot of diameter pointSize. The active transform and clip apply automatically.


Perf note: Canvas2D has no batched point draw, so this is one fill per live particle — fine for hundreds/low-thousands; the desktop GL build (single draw call) will outrun it at very high counts.


<sub>Source: [`eval/builtins_graphics_wasm.go:645`](../eval/builtins_graphics_wasm.go#L645)</sub>

### `droppedFiles(…)` → `array`

droppedFiles() — drain and return the list of files dropped onto the canvas since the last call. The desktop backend returns OS paths; the browser has no concept of paths, so we return data: URLs instead. Those work directly with loadImage(), which is the canonical way to handle drag-and-drop images:


```lex
for url in droppedFiles() {
    let img = loadImage(url)
    if type(img) != "ERROR" { drawImage(img, x, y) }
}
```


FileReader.readAsDataURL is async, so dropped files land in the queue a few ms after the drop event fires; calling droppedFiles() on the same frame as the drop may miss them. Polling each frame (the typical pattern) drains them by the next frame at worst.


<sub>Source: [`eval/builtins_graphics_wasm.go:1079`](../eval/builtins_graphics_wasm.go#L1079)</sub>

### `ellipse(x, y, rx, ry)` → `null`

ellipse(x: float, y: float, rx: float, ry: float) -> null


Draw an ellipse centred at (x, y) with horizontal radius rx and vertical radius ry. Respects fill and stroke state.


<sub>Source: [`eval/builtins_graphics_wasm.go:512`](../eval/builtins_graphics_wasm.go#L512)</sub>

### `fill(…)` → `null`

── State: fill / stroke / strokeWeight / noFill / noStroke ────────


<sub>Source: [`eval/builtins_graphics_wasm.go:337`](../eval/builtins_graphics_wasm.go#L337)</sub>

### `fontCharHeight(…)` → `int`

fontCharHeight() -> int


Return the pixel height of one character in the embedded monospace font at scale 1. Multiply by scale to get the display height.


<sub>Source: [`eval/builtins_graphics_wasm.go:1057`](../eval/builtins_graphics_wasm.go#L1057)</sub>

### `fontCharWidth(…)` → `int`

fontCharWidth / fontCharHeight — embedded monospace metrics at scale 1. The desktop backend uses an 8×8 bitmap font; the WASM backend uses Canvas2D's "monospace" at 8px which is close enough for layout math. Returning the constant 8 matches the desktop contract so cross-platform .lex code computes the same coords.


<sub>Source: [`eval/builtins_graphics_wasm.go:1051`](../eval/builtins_graphics_wasm.go#L1051)</sub>

### `frameCount(…)`

frameCount() -> int


Return the number of frames rendered since the window opened.


<sub>Source: [`eval/builtins_graphics_wasm.go:330`](../eval/builtins_graphics_wasm.go#L330)</sub>

### `frameRate(fps)` → `null`

frameRate(fps) — cap the render loop to fps frames per second. 0 means uncapped (run at the browser's vsync rate, typically 60).


<sub>Source: [`eval/builtins_graphics_wasm.go:1096`](../eval/builtins_graphics_wasm.go#L1096)</sub>

### `gradient(x, y, w, h, color1, color2, dir)` → `null`

gradient fills a rectangle with a two-colour linear gradient — the Canvas2D parallel to the desktop GPU gradient in builtins_graphics.go. Same signature and semantics: color1→color2 runs left→right for dir "h" and top→bottom for "v". The active transform and clip apply automatically (Canvas gradient coords are in current user space).


<sub>Source: [`eval/builtins_graphics_wasm.go:405`](../eval/builtins_graphics_wasm.go#L405)</sub>

### `imageFromRgba(bytes, width, height)`

imageFromRgba(bytes, width, height) → image


Browser parallel to the desktop builtin. Wraps raw RGBA8 pixels (row-major, width*height*4 bytes) into a drawable kLex Image: the pixels are painted onto an offscreen <canvas> via putImageData, and that canvas is registered as the image's draw source so drawImage() can blit it. The pixels are also kept on the Image so imageToRgba() round-trips without a readback. Bytes are copied — later caller mutations don't affect the image.


<sub>Source: [`eval/builtins_graphics_wasm.go:1281`](../eval/builtins_graphics_wasm.go#L1281)</sub>

### `imageSize(img)` → `tuple`

imageSize(img) → (width, height)


<sub>Source: [`eval/builtins_graphics_wasm.go:1258`](../eval/builtins_graphics_wasm.go#L1258)</sub>

### `imageToRgba(img)` → `bytes`

imageToRgba(img) → bytes


Browser parallel to the desktop builtin. Returns the image's raw RGBA8 pixels (row-major, width*height*4 bytes). Source priority:

```lex
1. img.pixels — present for images made via imageFromRgba.
2. the registered draw source (HTMLImageElement from loadImage, or
   an offscreen canvas) — drawn to a scratch canvas and read back
   with getImageData.
```

getImageData throws on a cross-origin-tainted canvas; the recover surfaces that as a clean kLex RuntimeError instead of a WASM trap.


<sub>Source: [`eval/builtins_graphics_wasm.go:1339`](../eval/builtins_graphics_wasm.go#L1339)</sub>

### `keyDown(key)` → `bool`

keyDown(key: string) -> bool


Return true while the named key is physically held down.\n\nKey names: \


<sub>Source: [`eval/builtins_graphics_wasm.go:1447`](../eval/builtins_graphics_wasm.go#L1447)</sub>

### `keyPressed(key)` → `bool`

keyPressed(key: string) -> bool


Return true only on the single frame the key was first pressed (one-shot). Resets to false the next frame.


<sub>Source: [`eval/builtins_graphics_wasm.go:1462`](../eval/builtins_graphics_wasm.go#L1462)</sub>

### `line(x1, y1, x2, y2)` → `null`

line(x1: float, y1: float, x2: float, y2: float) -> null


Draw a line from (x1, y1) to (x2, y2) using the current stroke colour.


<sub>Source: [`eval/builtins_graphics_wasm.go:532`](../eval/builtins_graphics_wasm.go#L532)</sub>

### `loadFont(path, [ptSize])` → `error`

── Text (default canvas font; loadFont/SDF deferred) ────────────── loadFont(path[, ptSize]) — async font load via the FontFace API.


Same channel-block pattern as loadImage: build a FontFace, call its load() Promise, then/catch into a Go channel, eval goroutine resumes when the Promise settles. The browser-side family name is synthesised as "klex_font_<id>" so users can call loadFont repeatedly without family collisions.


Returns *Font on success or *Error on failure (matches desktop `loadFont(path) -> Font` shape; callers check `if type(font) == "ERROR"`).


<sub>Source: [`eval/builtins_graphics_wasm.go:864`](../eval/builtins_graphics_wasm.go#L864)</sub>

### `loadImage(path)` → `error`

── Image loading + drawing ────────────────────────────────────────


loadImage(path) blocks the eval goroutine on a channel until HTMLImageElement.onload (success) or onerror (failure) fires. Same cooperative-async pattern as worker bridges + the render loop — Go scheduler yields while we wait, JS event loop runs, the load completes, the callback wakes the channel, eval resumes.


The loaded js.Value HTMLImageElement is stored in jsImageRegistry keyed by a synthetic uint32 (held in *Image.TextureID). Under the GLFW backend TextureID is a real GL texture; here we just reuse the field as an opaque numeric handle. drawImage looks up the js.Value and hands it to ctx.drawImage().


On failure loadImage returns an *Error directly (not a tuple) — matches the desktop signature `loadImage(path) -> image`. Callers can check `if type(img) == "ERROR"` (the natural kLex pattern for fallible builtins that return a single value).


<sub>Source: [`eval/builtins_graphics_wasm.go:1166`](../eval/builtins_graphics_wasm.go#L1166)</sub>

### `mouseClicked(…)` → `bool`

mouseClicked() -> bool


Return true only on the single frame the left mouse button was first pressed (one-shot). Resets to false the next frame.\n\nUse this for button clicks — mouseDown() fires every frame while held.


<sub>Source: [`eval/builtins_graphics_wasm.go:1413`](../eval/builtins_graphics_wasm.go#L1413)</sub>

### `mouseDown(…)` → `bool`

mouseDown() -> bool


Return true if the left mouse button is currently pressed.


<sub>Source: [`eval/builtins_graphics_wasm.go:1405`](../eval/builtins_graphics_wasm.go#L1405)</sub>

### `mouseRightClicked(…)` → `bool`

mouseRightClicked() -> bool


Return true only on the single frame the right mouse button was first pressed (one-shot). Resets to false the next frame.


<sub>Source: [`eval/builtins_graphics_wasm.go:1429`](../eval/builtins_graphics_wasm.go#L1429)</sub>

### `mouseRightDown(…)` → `bool`

mouseRightDown() -> bool


Return true if the right mouse button is currently held down.


<sub>Source: [`eval/builtins_graphics_wasm.go:1421`](../eval/builtins_graphics_wasm.go#L1421)</sub>

### `mouseScrollX(…)` → `float`

mouseScrollX() -> float


Return the horizontal mouse wheel delta for this frame (trackpad two-finger swipe or horizontal scroll wheel). Resets to 0 each frame.


<sub>Source: [`eval/builtins_graphics_wasm.go:1442`](../eval/builtins_graphics_wasm.go#L1442)</sub>

### `mouseScrollY(…)` → `float`

mouseScrollY() -> float


Return the vertical mouse wheel delta for this frame. Positive = scrolled up/forward, negative = scrolled down/backward. Resets to 0 each frame.\n\nExample: scroll offset += mouseScrollY() * speed


<sub>Source: [`eval/builtins_graphics_wasm.go:1437`](../eval/builtins_graphics_wasm.go#L1437)</sub>

### `mouseX(…)` → `float`

── Input: mouse + keyboard ────────────────────────────────────────


<sub>Source: [`eval/builtins_graphics_wasm.go:1395`](../eval/builtins_graphics_wasm.go#L1395)</sub>

### `mouseY(…)` → `float`

mouseY() -> float


Return the current mouse Y position in window pixels.


<sub>Source: [`eval/builtins_graphics_wasm.go:1400`](../eval/builtins_graphics_wasm.go#L1400)</sub>

### `noFill(…)` → `null`

noFill() -> null


Disable fill so shapes are drawn outline-only.


<sub>Source: [`eval/builtins_graphics_wasm.go:372`](../eval/builtins_graphics_wasm.go#L372)</sub>

### `noShadow(…)` → `null`

noShadow() -> null


Disable drop shadows set by shadow(). Subsequent rect(), circle(), and roundedRect() calls will not draw a shadow.


<sub>Source: [`eval/builtins_graphics_wasm.go:773`](../eval/builtins_graphics_wasm.go#L773)</sub>

### `noStroke(…)` → `null`

noStroke() -> null


Disable stroke so shapes are drawn fill-only.


<sub>Source: [`eval/builtins_graphics_wasm.go:378`](../eval/builtins_graphics_wasm.go#L378)</sub>

### `point(x, y)` → `null`

point(x, y) — single pixel-ish dot. Uses current stroke colour at the current strokeWeight. Canvas2D has no native point primitive; drawing a 1×1 fillRect at the stroke colour matches the desktop backend's pixel-set behaviour.


<sub>Source: [`eval/builtins_graphics_wasm.go:607`](../eval/builtins_graphics_wasm.go#L607)</sub>

### `polygon(points)` → `null`

polygon(points) — points is a flat [x1, y1, x2, y2, ...] array. Requires at least 3 points (6 numbers). Respects fill + stroke.


<sub>Source: [`eval/builtins_graphics_wasm.go:705`](../eval/builtins_graphics_wasm.go#L705)</sub>

### `popClip(…)` → `null`

popClip() -> null


Pop the top clip rect from the stack and restore the one below it. If the stack is now empty, clipping is disabled entirely. Every pushClip must have exactly one matching popClip.


<sub>Source: [`eval/builtins_graphics_wasm.go:815`](../eval/builtins_graphics_wasm.go#L815)</sub>

### `popMatrix(…)`

popMatrix() -> null


Restore the transform matrix saved by the last pushMatrix() call.


<sub>Source: [`eval/builtins_graphics_wasm.go:460`](../eval/builtins_graphics_wasm.go#L460)</sub>

### `pushClip(x, y, w, h)` → `null`

── Clipping (Canvas2D save/clip/restore) ──────────────────────────


pushClip(x, y, w, h) saves the canvas state, sets a rect clip, and stays in effect until popClip() restores. Nesting is supported — each pushClip is a save, each popClip is a restore. Mismatched pop without a push is a no-op (canvas state stack is empty).


<sub>Source: [`eval/builtins_graphics_wasm.go:791`](../eval/builtins_graphics_wasm.go#L791)</sub>

### `pushMatrix(…)`

── Transform stack ────────────────────────────────────────────────


<sub>Source: [`eval/builtins_graphics_wasm.go:459`](../eval/builtins_graphics_wasm.go#L459)</sub>

### `rect(…)`

── Shapes ─────────────────────────────────────────────────────────


<sub>Source: [`eval/builtins_graphics_wasm.go:480`](../eval/builtins_graphics_wasm.go#L480)</sub>

### `rotate(angle)` → `null`

rotate(angle: float) -> null


Rotate the drawing axes by angle radians (clockwise, y-down). Affects all subsequent draw calls until popMatrix().


<sub>Source: [`eval/builtins_graphics_wasm.go:462`](../eval/builtins_graphics_wasm.go#L462)</sub>

### `roundedRect(x, y, w, h, r)`

roundedRect(x, y, w, h, r: float) -> null


Draw a rounded rectangle with corner radius r. Rendered using a signed distance field (SDF) shader — mathematically perfect anti-aliased edges at any size. Respects fill and stroke state.


<sub>Source: [`eval/builtins_graphics_wasm.go:483`](../eval/builtins_graphics_wasm.go#L483)</sub>

### `saveImage(…)`

saveImage(img, path) → null


Desktop writes the image to disk; browsers have no filesystem, so in the WASM build this is intentionally unsupported. It is registered (rather than left undefined) purely so the failure is a clear, actionable message instead of an "undefined name" error — the same pattern _httpServe uses for browser-impossible operations. Pixel export in the browser is out of scope by design; run on desktop to save image files.


<sub>Source: [`eval/builtins_graphics_wasm.go:1390`](../eval/builtins_graphics_wasm.go#L1390)</sub>

### `scale(…)`

scale(sx: float, sy: float) -> null


Scale the drawing axes by (sx, sy). Affects all subsequent draw calls until popMatrix().


<sub>Source: [`eval/builtins_graphics_wasm.go:477`](../eval/builtins_graphics_wasm.go#L477)</sub>

### `shadow(offsetX, offsetY, blur)` → `null`

── Shadows (Canvas2D shadow* properties) ──────────────────────────


shadow(ox, oy, blur)            — opaque-black shadow at default shadow(ox, oy, blur, r, g, b, a) — coloured shadow


Affects subsequent rect/circle/roundedRect/text draws until noShadow() is called. Matches the desktop signature.


<sub>Source: [`eval/builtins_graphics_wasm.go:745`](../eval/builtins_graphics_wasm.go#L745)</sub>

### `stroke(…)` → `null`

stroke(gray: float) | stroke(r, g, b: float) | stroke(r, g, b, a: float) -> null


Set the stroke (outline) colour for subsequent shapes. Values in [0.0, 1.0].


<sub>Source: [`eval/builtins_graphics_wasm.go:348`](../eval/builtins_graphics_wasm.go#L348)</sub>

### `strokeWeight(w)` → `null`

strokeWeight(w: float) -> null


Set the stroke line width in pixels.


<sub>Source: [`eval/builtins_graphics_wasm.go:359`](../eval/builtins_graphics_wasm.go#L359)</sub>

### `text(str, x, y)` → `null`

text(str: string, x: float, y: float, scale?: float) -> null


Draw a string using the embedded 8×8 monospace bitmap font. Optional scale multiplies character size (default 1 = 8px). Uses current fill colour as tint.


<sub>Source: [`eval/builtins_graphics_wasm.go:1110`](../eval/builtins_graphics_wasm.go#L1110)</sub>

### `textFont(font, str, x, y, [scale])` → `null`

textFont(font, str, x, y[, scale]) — draw with a Font from loadFont.


<sub>Source: [`eval/builtins_graphics_wasm.go:933`](../eval/builtins_graphics_wasm.go#L933)</sub>

### `textWidth(font, ...rest)` → `float`

textWidth — measure a string. Accepts either form:

```lex
textWidth(str [, scale])           default monospace
textWidth(font, str [, scale])     Font from loadFont
```

Dispatched on the first arg's type so existing string-only callers keep working.


<sub>Source: [`eval/builtins_graphics_wasm.go:982`](../eval/builtins_graphics_wasm.go#L982)</sub>

### `translate(…)`

translate(x: float, y: float) -> null


Move the drawing origin by (x, y). Affects all subsequent draw calls until popMatrix().


<sub>Source: [`eval/builtins_graphics_wasm.go:461`](../eval/builtins_graphics_wasm.go#L461)</sub>

### `triangle(x1, y1, x2, y2, x3, y3)` → `null`

triangle(x1, y1, x2, y2, x3, y3: float) -> null


Draw a triangle. Respects fill and stroke state.


<sub>Source: [`eval/builtins_graphics_wasm.go:554`](../eval/builtins_graphics_wasm.go#L554)</sub>

### `winHeight(…)`

winHeight() -> int


Return the window height in pixels.


<sub>Source: [`eval/builtins_graphics_wasm.go:325`](../eval/builtins_graphics_wasm.go#L325)</sub>

### `winWidth(…)`

winWidth() -> int


Return the window width in pixels.


<sub>Source: [`eval/builtins_graphics_wasm.go:320`](../eval/builtins_graphics_wasm.go#L320)</sub>

### `window(width, height, title, drawFn)`

── window(w, h, title, drawFn) ──────────────────────────────────────


Open the canvas (must exist in the host page with id "klex-canvas"), run drawFn each frame via requestAnimationFrame, block until the canvas is removed from the DOM or the script returns from drawFn with a sentinel false. drawFn receives frameCount as its single arg.


<sub>Source: [`eval/builtins_graphics_wasm.go:136`](../eval/builtins_graphics_wasm.go#L136)</sub>

## Image Fx

### `_imgBrightness(bytes, w, h, amount)` → `tuple`

_imgBrightness(bytes, w, h, amount) → (bytes, err)


Adds `amount` to RGB (each in [0,1] space). amount in [-1, +1]; 0 is no change. Additive, unlike exposure (which is multiplicative).


<sub>Source: [`eval/builtins_image_fx.go:203`](../eval/builtins_image_fx.go#L203)</sub>

### `_imgChannelMixer(bytes, w, h, matrix9)` → `tuple`

_imgChannelMixer(bytes, w, h, matrix9) → (bytes, err)


matrix9 is a flat 9-element array of floats representing a 3×3 row-major matrix applied to each pixel's RGB column:

```lex
[ rr rg rb
  gr gg gb
  br bg bb ]
```

out.r = rr*in.r + rg*in.g + rb*in.b   (and similarly for g, b) Identity matrix [1,0,0, 0,1,0, 0,0,1] = no change.


<sub>Source: [`eval/builtins_image_fx.go:409`](../eval/builtins_image_fx.go#L409)</sub>

### `_imgContrast(bytes, w, h, amount)` → `tuple`

_imgContrast(bytes, w, h, amount) → (bytes, err)


Scales each channel around 0.5 mid-gray: out = (in - 0.5)*(1+amount)+0.5. amount in [-1, +1]; 0 is no change, +1 doubles contrast, -1 flattens to mid-gray.


<sub>Source: [`eval/builtins_image_fx.go:229`](../eval/builtins_image_fx.go#L229)</sub>

### `_imgDesaturate(bytes, w, h)` → `tuple`

_imgDesaturate(bytes, w, h) → (bytes, err)


Convenience equivalent to saturation(-1) but cheaper — every channel becomes Rec.709 luma.


<sub>Source: [`eval/builtins_image_fx.go:469`](../eval/builtins_image_fx.go#L469)</sub>

### `_imgExposure(bytes, w, h, stops)` → `tuple`

_imgExposure(bytes, w, h, stops) → (bytes, err)


Multiplies RGB by 2^stops — i.e. one stop doubles or halves the linear-light intensity. Typical range -4..+4 stops.


<sub>Source: [`eval/builtins_image_fx.go:175`](../eval/builtins_image_fx.go#L175)</sub>

### `_imgGamma(bytes, w, h, gamma)` → `tuple`

_imgGamma(bytes, w, h, gamma) → (bytes, err)


Raises each channel to the power 1/gamma. gamma > 1 brightens midtones; gamma < 1 darkens them. Must be > 0.


<sub>Source: [`eval/builtins_image_fx.go:313`](../eval/builtins_image_fx.go#L313)</sub>

### `_imgHueShift(bytes, w, h, degrees)` → `tuple`

_imgHueShift(bytes, w, h, degrees) → (bytes, err)


Rotates hue by `degrees` (any real number; wraps modulo 360). Routes through HSV — saturation and value are unchanged.


<sub>Source: [`eval/builtins_image_fx.go:284`](../eval/builtins_image_fx.go#L284)</sub>

### `_imgInvert(bytes, w, h)` → `tuple`

_imgInvert(bytes, w, h) → (bytes, err)


Negative — 1 - c on each RGB channel.


<sub>Source: [`eval/builtins_image_fx.go:448`](../eval/builtins_image_fx.go#L448)</sub>

### `_imgLevels(bytes, w, h, inBlack, inWhite, gamma, outBlack, outWhite)` → `tuple`

_imgLevels(bytes, w, h, inBlack, inWhite, gamma, outBlack, outWhite) → (bytes, err)


Classic Photoshop-style levels: remap [inBlack, inWhite] of the input to [outBlack, outWhite] of the output with an intermediate gamma adjustment. All five sliders are floats in [0,1] (gamma is the usual "midtone" knob, > 0).


<sub>Source: [`eval/builtins_image_fx.go:344`](../eval/builtins_image_fx.go#L344)</sub>

### `_imgSaturation(bytes, w, h, amount)` → `tuple`

_imgSaturation(bytes, w, h, amount) → (bytes, err)


Pulls each pixel toward (amount < 0) or away from (amount > 0) its luma. amount in [-1, +1]: -1 fully desaturates, 0 no change, +1 doubles saturation (may clip on already-saturated pixels).


<sub>Source: [`eval/builtins_image_fx.go:256`](../eval/builtins_image_fx.go#L256)</sub>

### `_imgSepia(bytes, w, h, strength)` → `tuple`

_imgSepia(bytes, w, h, strength) → (bytes, err)


Mixes the original pixel with its sepia-toned counterpart by `strength` (0 = no change, 1 = full sepia). Sepia transform is the standard Photoshop matrix.


<sub>Source: [`eval/builtins_image_fx.go:492`](../eval/builtins_image_fx.go#L492)</sub>

### `_imgVignette(bytes, w, h, strength, radius)` → `tuple`

_imgVignette(bytes, w, h, strength, radius) → (bytes, err)


Darkens pixels away from the image centre. `strength` in [0,1] is how dark the corners get (0 = no change, 1 = corners → black). `radius` in (0, ∞) controls how quickly the falloff starts; 0.5 is a tight vignette, 1.0 is gentle, 1.5 only kisses the corners. Distance is normalised to half-diagonal so the effect is resolution-independent.


<sub>Source: [`eval/builtins_image_fx.go:531`](../eval/builtins_image_fx.go#L531)</sub>

## Iter

### `_iterPrep(coll, twoVar)` → `tuple`

_iterPrep(coll: array | hash | string | bytes | tuple | channel | concurrentHash, twoVar: bool) -> (iterArray, isPairs)


Internal helper used by the VM's compileForIn lowering. Normalises an iterable into a (iterArray, isPairs) tuple the VM's index-based loop can consume.\n\n  - For *Array / *String / *Bytes / *Tuple / *Channel / *ConcurrentHash: pass-through; isPairs is FALSE; the VM's existing index-based loop handles iteration.\n  - For *Hash with twoVar=true: returns ([[k, v], ...], TRUE). The VM sees isPairs=true and unpacks each iter element as a 2-tuple into the loop's two binding slots.\n  - For *Hash with twoVar=false: returns an Error (matches the tree-walker's single-var hash-iteration rejection).\n  - For any other type: returns an Error.\n\nNot intended for direct kLex use — emitted by the compiler.


<sub>Source: [`eval/builtins_iter.go:33`](../eval/builtins_iter.go#L33)</sub>

## Json

### `_jsonParse(arg1)` → `tuple`

── _jsonParse(s: string) → (value, errStr) ────────────────────────────


Decode a JSON document into kLex objects. Returns (value, null) on success, (null, "<error message>") on parse failure. Mirrors the historical stdlib/json.lex `parse` signature byte-for-byte so existing callers can switch over by changing the delegation site only — no upstream churn.


<sub>Source: [`eval/builtins_json.go:230`](../eval/builtins_json.go#L230)</sub>

### `_jsonStringify(arg1)` → `tuple`

── _jsonStringify(v: any) → (string, errStr) ──────────────────────────


Encode a kLex value as JSON. Returns (json, null) on success or (null, "<error message>") if some value in the tree isn't JSON-representable (Bytes, cycles, NaN/Inf floats). Hash keys are emitted sorted so output is deterministic.


Note: the historical stdlib/json.lex returned just a string (no error tuple), so the wrapper in stdlib/json.lex unpacks `(s, _)` and returns s only. New code may call _jsonStringify directly to surface the error.


<sub>Source: [`eval/builtins_json.go:267`](../eval/builtins_json.go#L267)</sub>

## Jsonl

### `_replaySeenFile(arg1)`

_replaySeenFile(path: string) -> hash


Reads a JSONL file where each line is a {"path": str, "mtime": int, ...} record. Returns a hash {path: mtime} keyed by path; latest line wins on duplicate paths (matching the "last write" semantics any in-flight crash-recovery would expect).


Missing file → empty hash, no error. Malformed lines are skipped silently (same best-effort policy stdlib/json.lex callers already expect when iterating a JSONL).


Used by store._replaySeen to populate s.seen from catalog.jsonl or library.json without the interpreted-JSON-parser bottleneck. The downstream isFileFresh(s, path, mtime) just compares scalars — O(1) hash lookup, no nested chunk-array iteration.


<sub>Source: [`eval/builtins_jsonl.go:42`](../eval/builtins_jsonl.go#L42)</sub>

## Mcp Server

### `_mcpServeHTTP(spec)` → `tuple`

_mcpServeHTTP(spec) → (server, err)


Validates spec, binds an HTTP listener, and starts background goroutines for SSE + JSON-RPC. Returns the MCPServer handle immediately — the kLex caller stays in control.


<sub>Source: [`eval/builtins_mcp_server.go:298`](../eval/builtins_mcp_server.go#L298)</sub>

### `_mcpServerInfo(server)`

_mcpServerInfo(server) → hash  — introspection helper, useful for tests


<sub>Source: [`eval/builtins_mcp_server.go:377`](../eval/builtins_mcp_server.go#L377)</sub>

### `_mcpStopServer(server)` → `null`

_mcpStopServer(server) → null


Graceful shutdown: closes the listener, waits up to 5s for in-flight handlers, then forces shutdown. Idempotent.


<sub>Source: [`eval/builtins_mcp_server.go:346`](../eval/builtins_mcp_server.go#L346)</sub>

## Mtl Darwin

### `_mtlAccel(vertex_buffer, vertex_count)` → `tuple`

_mtlAccel(vertex_buffer, vertex_count) → Channel of (handle, err)


Builds a ray-tracing acceleration structure (MTLPrimitiveAS) from a triangle-list vertex buffer. The buffer must hold `vertex_count` float3 positions (12 bytes each), and vertex_count must be a positive multiple of 3 — every three consecutive vertices forms one triangle.


Async because accel builds are GPU-side and non-trivially slow for any real scene. The returned Channel emits ONE message: (accel_handle: int, null) on success or (null, err) on failure.


The accel structure retains its source vertex buffer internally, so releasing the kLex buffer handle does NOT invalidate the accel. Conversely, releasing the accel does NOT release the buffer — kLex must release both when done.


<sub>Source: [`eval/builtins_mtl_darwin.go:1070`](../eval/builtins_mtl_darwin.go#L1070)</sub>

### `_mtlAccelIndexed(vertex_buffer, vertex_count, index_buffer, index_count)` → `tuple`

_mtlAccelIndexed(vertex_buffer, vertex_count, index_buffer, index_count)

```lex
                                               → Channel of (handle, err)
```


Indexed-geometry variant of _mtlAccel. The vertex buffer is a float32 buffer of N float3 positions. The index buffer is a UINT32 buffer of triangle indices — every 3 consecutive uint32s reference 3 vertex positions to form one triangle.


Use this instead of _mtlAccel whenever multiple triangles share EXACT vertex positions — unindexed lists silently degenerate the BVH in that case (see feedback-mtl-shared-verts-bvh memory).


<sub>Source: [`eval/builtins_mtl_darwin.go:1189`](../eval/builtins_mtl_darwin.go#L1189)</sub>

### `_mtlAccelRelease(handle)` → `null`

_mtlAccelRelease(accel) → null


Drops the bridge's retain on the acceleration structure. The underlying vertex buffer (passed in via _mtlAccel) is NOT released by this call — caller manages its own buffer handle.


<sub>Source: [`eval/builtins_mtl_darwin.go:1223`](../eval/builtins_mtl_darwin.go#L1223)</sub>

### `_mtlBatchBegin(…)` → `tuple`

_mtlBatchBegin() → (handle, err)


Opens a fresh batch command buffer. Subsequent _mtlBatchDispatch calls add compute encoders to the same buffer; _mtlBatchCommit closes the batch and submits. Use for tight loops that issue many dispatches.


<sub>Source: [`eval/builtins_mtl_darwin.go:916`](../eval/builtins_mtl_darwin.go#L916)</sub>

### `_mtlBatchCommit(batch)`

_mtlBatchCommit(batch) → Channel of (null, err)


Commits the batch's command buffer and returns a Channel that emits when the GPU finishes. Releases the batch handle.


<sub>Source: [`eval/builtins_mtl_darwin.go:1033`](../eval/builtins_mtl_darwin.go#L1033)</sub>

### `_mtlBatchDispatch(batch, kernel, bindings, grid)` → `tuple`

_mtlBatchDispatch(batch, kernel, bindings, grid) → (null, err)


Adds one compute dispatch to an open batch. Synchronous on the CPU (just encoding) — the GPU work doesn't run until commit.


<sub>Source: [`eval/builtins_mtl_darwin.go:933`](../eval/builtins_mtl_darwin.go#L933)</sub>

### `_mtlBatchRelease(batch)` → `null`

_mtlBatchRelease(batch) → null


Abandons a batch without committing — used for error-path cleanup when a mid-batch dispatch fails. Idempotent: releasing an unknown handle is a no-op. Always pair with _mtlBatchBegin if any dispatch returns an error, otherwise the command buffer leaks until process exit.


<sub>Source: [`eval/builtins_mtl_darwin.go:1017`](../eval/builtins_mtl_darwin.go#L1017)</sub>

### `_mtlBuffer(arr)` → `tuple`

_mtlBuffer(floats) → (handle, err)


Wraps an array of floats as a GPU-accessible MTLBuffer. The caller passes a kLex Array; each element must be a number (int or float — both are accepted and coerced to float32). The buffer's storage mode is Shared, so on Apple Silicon there is no host→device copy: the bytes are visible to both sides at the same address.


Sync because the underlying newBufferWithBytes call is a few-microsecond allocation; async would add complexity for no win.


<sub>Source: [`eval/builtins_mtl_darwin.go:583`](../eval/builtins_mtl_darwin.go#L583)</sub>

### `_mtlBufferAllocF32(count)` → `tuple`

_mtlBufferAllocF32(count) → (handle, err)


Allocates a zero-initialised GPU buffer big enough to hold `count` float32 values. Unlike _mtlBuffer (which marshals a kLex Array element-by-element) this skips the CPU-side data entirely — the Metal allocator zero-fills at the OS level for free. Use this for any output buffer that a kernel fully overwrites (reductions, FFT, matmul, etc.).


<sub>Source: [`eval/builtins_mtl_darwin.go:671`](../eval/builtins_mtl_darwin.go#L671)</sub>

### `_mtlBufferAllocU32(count)` → `tuple`

_mtlBufferAllocU32(count) → (handle, err)


uint32 counterpart of _mtlBufferAllocF32. Same underlying allocator (both are 4 bytes/element); separate builtin for API symmetry with _mtlBuffer / _mtlBufferU32.


<sub>Source: [`eval/builtins_mtl_darwin.go:697`](../eval/builtins_mtl_darwin.go#L697)</sub>

### `_mtlBufferFromTensor(t)` → `tuple`

_mtlBufferFromTensor(t) → (handle, err)


Zero-marshalling upload from a FrogPy f32 tensor straight to an MTLBuffer. The tensor's F32 slice is passed by pointer to mtl_buffer_create_f32 — the same C entry point as _mtlBuffer uses, but skipping the per-element kLex-Float-to-float32 loop (which dominates _mtlBuffer's cost for large arrays).


Measured ~5× faster than _mtlBuffer for 1024² f32 uploads.


Restrictions: tensor must be DType f32 and contiguous. Other dtypes error cleanly — convert via your own kernel first or use _mtlBuffer with an explicit Array conversion if you really need f64 / i64 on the GPU.


<sub>Source: [`eval/builtins_mtl_darwin.go:635`](../eval/builtins_mtl_darwin.go#L635)</sub>

### `_mtlBufferRelease(handle)` → `null`

_mtlBufferRelease(buffer) → null


Drops the bridge's retain. Idempotent.


<sub>Source: [`eval/builtins_mtl_darwin.go:898`](../eval/builtins_mtl_darwin.go#L898)</sub>

### `_mtlBufferU32(arr)` → `tuple`

_mtlBufferU32(ints) → (handle, err)


Same as _mtlBuffer but for uint32-typed buffers — needed for triangle index buffers in indexed acceleration structures. Accepts a kLex array of Integers; values are coerced to uint32 (must be non-negative and <= 4_294_967_295).


The bridge stores u32 buffers in the SAME handle table as f32 buffers — MTLBuffer is type-opaque at the Metal level. Kernel authors must declare the buffer with the right MSL type (`constant uint*` vs `constant float*`) or get silent corruption.


<sub>Source: [`eval/builtins_mtl_darwin.go:729`](../eval/builtins_mtl_darwin.go#L729)</sub>

### `_mtlClear(handle, color)`

_mtlClear(surface, [r, g, b, a]) → Channel of (null, err)


Submits a GPU clear-to-color command for the surface and returns a kLex Channel that emits ONE message — `(null, null)` on success or `(null, error_string)` on failure — then closes.


Async by design: the kLex thread keeps running while the GPU works. Wait for completion with `recv(ch)`. The Go bridge owns the goroutine that blocks on the MTLCommandBuffer; kLex never blocks on Metal directly.


Cancellation: closing the channel (`close(ch)`) causes the result to be dropped on the floor. The command buffer already on the GPU still runs to completion (Metal has no interrupt) — cancellation just means "don't deliver the result".


<sub>Source: [`eval/builtins_mtl_darwin.go:396`](../eval/builtins_mtl_darwin.go#L396)</sub>

### `_mtlDispatch(kernel, bindings, grid)` → `tuple`

_mtlDispatch(kernel, bindings, grid) → Channel of (null, err)


Submits one compute-shader dispatch. Returns a single-shot Channel that emits (null, null) on success or (null, err) on failure, then closes.


Arguments:


```lex
kernel    : int        — handle from _mtlKernel
bindings  : hash       — { "textures": [t0, t1, ...],
                           "buffers":  [b0, b1, ...],
                           "accels":   [a0, a1, ...] }
                         Textures bind to [[texture(i)]].
                         Buffers bind to [[buffer(i)]].
                         Acceleration structures bind to
                         [[buffer(buffer_count + i)]] — they
                         share the buffer slot space and
                         come immediately after the regular
                         buffers. Any key is optional.
grid      : array      — [x, y, z] thread count for the
                         dispatch. Use 1 for unused
                         dimensions, e.g. [n, 1, 1] for a
                         1-D pass over `n` elements.
```


Async via runAsyncSingle: the kLex thread keeps running while the GPU works. Wait for completion with `recv(ch)`. After a successful recv, any buffers/textures bound here are safe to read on the CPU side (shared storage + completion = visible).


<sub>Source: [`eval/builtins_mtl_darwin.go:1263`](../eval/builtins_mtl_darwin.go#L1263)</sub>

### `_mtlFFT(in_handle, out_handle, n, inverse)`

_mtlFFT(in_handle, out_handle, n, inverse) → Channel of (null, err)


1D complex-to-complex Fast Fourier Transform via MPSGraph. Both buffers hold 2n floats in interleaved (re, im) layout — same byte format as NumPy / SciPy complex64 arrays.


```lex
inverse=0  →  forward FFT
inverse=1  →  inverse FFT (caller divides by n for a true inverse)
```


Async via runAsyncSingle.


<sub>Source: [`eval/builtins_mtl_darwin.go:1148`](../eval/builtins_mtl_darwin.go#L1148)</sub>

### `_mtlInfo(…)` → `tuple`

_mtlInfo() → hash


Returns a hash describing the default Metal device. Used as the "is Metal available?" smoke test and the canonical place to read capability flags before code that depends on them runs.


Shape of returned hash:

```lex
{
  "name":                  "Apple M2 Pro",
  "registry_id":           4294968574,
  "has_unified_memory":    true,
  "supports_raytracing":   false,    // true on M3+
  "supports_apple7":       true,     // M1+/A14+ baseline
  "max_threads_per_group": 1024,
  "max_bind_slots":        8,        // MTL_MAX_BIND from the bridge
}
```


`max_bind_slots` is the hard cap on textures, buffers, and acceleration structures per dispatch — exposed so kLex code can validate kernel layouts at the call site instead of waiting for the bridge to reject the dispatch.


On older hardware where no Metal device is available, returns (null, error) — never a partially-filled hash.


<sub>Source: [`eval/builtins_mtl_darwin.go:240`](../eval/builtins_mtl_darwin.go#L240)</sub>

### `_mtlKernel(mslSource, fnName)` → `tuple`

_mtlKernel(msl_source, fn_name) → (handle, err)


Compiles MSL source code and produces a compute pipeline state for the named function. The returned handle is reusable across many dispatches — compile once, dispatch many.


MSL is Apple's Metal Shading Language: a C++14-ish dialect with GPU-specific qualifiers (`kernel`, `thread_position_in_grid`, `texture2d`, etc.). Compile errors come back with the same detail you'd see in Xcode — line/column + the message.


Sync because compilation is bounded (~10 ms for the kernels we have planned) and the result is needed before any dispatch can be set up. If we ever hit a kernel that takes >100 ms to compile we can move this to async — but speculative async for fast work would just add complexity for no win.


<sub>Source: [`eval/builtins_mtl_darwin.go:529`](../eval/builtins_mtl_darwin.go#L529)</sub>

### `_mtlKernelRelease(kernel)` → `null`

_mtlKernelRelease(kernel) → null


Drops the bridge's retain on the compiled pipeline state. Idempotent — unknown handles are no-ops.


<sub>Source: [`eval/builtins_mtl_darwin.go:560`](../eval/builtins_mtl_darwin.go#L560)</sub>

### `_mtlMatmulMPS(a, b, c, m, k, n)`

_mtlMatmulMPS(a_handle, b_handle, c_handle, m, k, n) → Channel of (null, err)


MPS-accelerated matrix multiply: C := A · B using Apple's MPSMatrixMultiplication. All three buffer handles must already exist (typically created via _mtlBuffer). C is overwritten in place.  Async via runAsyncSingle — channel emits one (null,err) when the GPU finishes.


For non-trivial sizes (256³+) this hits 10-20× the naive matmul kernel's throughput.


<sub>Source: [`eval/builtins_mtl_darwin.go:1104`](../eval/builtins_mtl_darwin.go#L1104)</sub>

### `_mtlReadBuffer(handle)` → `tuple`

_mtlReadBuffer(buffer) → (floats, err)


Copies the buffer's contents into a fresh kLex Array of floats. Sync because shared-storage buffers are CPU-visible directly — no GPU round-trip needed. The caller is responsible for ensuring any GPU work that writes to the buffer has finished (e.g. by awaiting the dispatch's completion channel) before calling this.


<sub>Source: [`eval/builtins_mtl_darwin.go:771`](../eval/builtins_mtl_darwin.go#L771)</sub>

### `_mtlReadBufferIntoTensor(handle, t)` → `tuple`

_mtlReadBufferIntoTensor(buffer, tensor) → (null, err)


Zero-allocation readback: copies a float32 MTLBuffer's contents directly into a pre-allocated FrogPy f32 tensor's underlying []float32 slice. Skips the per-element *Float construction that _mtlReadBuffer pays — for 1M cells that's ~7 ms saved on every readback. Pair with _mtlBufferAllocF32 / _mtlBufferFromTensor for a fully zero-marshalling round-trip through MPS or compute kernels.


Tensor must be:

```lex
- dtype f32 (the buffer's element type)
- contiguous (Strides == nil); strided views error cleanly
- sized exactly to the buffer's element count
```


Same synchronisation contract as _mtlReadBuffer: caller is responsible for ensuring any GPU work that writes to the buffer has finished before calling this.


<sub>Source: [`eval/builtins_mtl_darwin.go:857`](../eval/builtins_mtl_darwin.go#L857)</sub>

### `_mtlReadBufferU32(handle)` → `tuple`

_mtlReadBufferU32(buffer) → (ints, err)


uint32 counterpart of _mtlReadBuffer. Returns a kLex Array of Integer values. Used for reading back atomic-counter outputs (e.g. histogram bin counts).


<sub>Source: [`eval/builtins_mtl_darwin.go:807`](../eval/builtins_mtl_darwin.go#L807)</sub>

### `_mtlSurface(width, height)` → `tuple`

_mtlSurface(width, height) → (handle, err)


Allocates an offscreen RGBA8 Metal texture of the given size, retained on the bridge side under an integer handle. The handle is what every other `_mtl*` surface function expects.


`width` and `height` must be positive integers. The texture's storage mode is "shared" — zero-copy CPU read-back on Apple Silicon, a blit on Intel.


<sub>Source: [`eval/builtins_mtl_darwin.go:284`](../eval/builtins_mtl_darwin.go#L284)</sub>

### `_mtlSurfaceFromBytes(bytes, width, height)` → `tuple`

_mtlSurfaceFromBytes(bytes, width, height) → (surface, err)


Creates a fresh RGBA8 Metal surface of the requested dimensions and uploads `bytes` into it. `bytes` must be exactly width*height*4 bytes (RGBA8 row-major). The returned handle is the same kind of handle _mtlSurface returns and is freed with _mtlSurfaceRelease.


Use case: pull an in-memory RGBA image (e.g. a generated image from an SD endpoint) onto a Metal surface so a compute kernel can post-process it. Pair with _mtlSurfaceReadRgba on the output surface to read the processed pixels back.


<sub>Source: [`eval/builtins_mtl_darwin.go:314`](../eval/builtins_mtl_darwin.go#L314)</sub>

### `_mtlSurfaceReadRgba(handle)` → `tuple`

_mtlSurfaceReadRgba(surface) → (bytes, err)


Reads the surface's pixels back into a kLex bytes value (RGBA8 row-major, exactly width*height*4 bytes). Counterpart to _mtlSurfaceFromBytes — completes the round-trip for image post-processing pipelines.


On Apple Silicon (unified memory) this is a zero-copy view into the texture's shared storage; the kLex bytes are an independent copy so the caller can mutate them without affecting the texture.


<sub>Source: [`eval/builtins_mtl_darwin.go:356`](../eval/builtins_mtl_darwin.go#L356)</sub>

### `_mtlSurfaceRelease(handle)` → `null`

_mtlSurfaceRelease(surface) → null


Drops the bridge's retain on the surface so the GPU memory can be freed. Idempotent — calling with an unknown handle is a no-op. kLex has no destructors; callers must release surfaces explicitly. Forgotten releases leak GPU memory but don't crash.


<sub>Source: [`eval/builtins_mtl_darwin.go:501`](../eval/builtins_mtl_darwin.go#L501)</sub>

### `_mtlSurfaceSavePng(handle, path)` → `tuple`

_mtlSurfaceSavePng(surface, path) → Channel of (null, err)


Reads the surface pixels back to CPU memory (RGBA8), PNG-encodes them, and writes the result at `path`. Returns a single-shot channel that emits one (null, null) on success or (null, err) on failure, then closes.


The async path matters because (a) read-back from a GPU texture that's still rendering can stall until the GPU catches up, and (b) PNG encoding a 4K image is a few-millisecond CPU job that shouldn't block the kLex thread. Both happen in the goroutine.


<sub>Source: [`eval/builtins_mtl_darwin.go:449`](../eval/builtins_mtl_darwin.go#L449)</sub>

## Mtl Stub

### `_mtlAccel(…)`

_mtlAccel(vertex_buffer: int, vertex_count: int) -> channel


Build a primitive MTLAccelerationStructure from a flat triangle list. vertex_count must be a positive multiple of 3. Required for ray-tracing kernels. Note: sharing exact-equal vertex positions across triangles in an UNINDEXED AS breaks the BVH on some chips — use _mtlAccelIndexed instead if you have shared verts.


<sub>Source: [`eval/builtins_mtl_stub.go:136`](../eval/builtins_mtl_stub.go#L136)</sub>

### `_mtlAccelIndexed(…)`

_mtlAccelIndexed(vertex_buffer: int, vertex_count: int, index_buffer: int, index_count: int) -> channel


Indexed-triangle variant of _mtlAccel. index_buffer is a uint32 MTLBuffer; index_count must be a positive multiple of 3. Required when triangles share vertex positions.


<sub>Source: [`eval/builtins_mtl_stub.go:140`](../eval/builtins_mtl_stub.go#L140)</sub>

### `_mtlAccelRelease(…)` → `null`

_mtlAccelRelease(handle: int) -> null


Free an MTLAccelerationStructure. Note: the underlying vertex/index buffers are NOT auto-released — release them separately via _mtlBufferRelease.


<sub>Source: [`eval/builtins_mtl_stub.go:168`](../eval/builtins_mtl_stub.go#L168)</sub>

### `_mtlBatchBegin(…)`

_mtlBatchBegin() -> (batch: int, error)


Open a fresh batch command buffer. Subsequent _mtlBatchDispatch calls encode compute passes onto it WITHOUT committing. Pair every successful begin with either _mtlBatchCommit (run them) or _mtlBatchRelease (abandon them) — leaking a batch leaks an MTLCommandBuffer.


<sub>Source: [`eval/builtins_mtl_stub.go:152`](../eval/builtins_mtl_stub.go#L152)</sub>

### `_mtlBatchCommit(…)`

_mtlBatchCommit(batch: int) -> channel


Async — close the batch, submit its command buffer to the GPU, and wait. Returns a single-shot channel that emits (null, null) or (null, err). Releases the batch handle either way.


<sub>Source: [`eval/builtins_mtl_stub.go:160`](../eval/builtins_mtl_stub.go#L160)</sub>

### `_mtlBatchDispatch(…)`

_mtlBatchDispatch(batch: int, kernel: int, bindings: hash, grid: array) -> (null, error)


Encode one compute dispatch onto an open batch's command buffer. Same bindings/grid shape as _mtlDispatch. Synchronous — the GPU work doesn't run until _mtlBatchCommit.


<sub>Source: [`eval/builtins_mtl_stub.go:156`](../eval/builtins_mtl_stub.go#L156)</sub>

### `_mtlBatchRelease(…)` → `null`

_mtlBatchRelease(batch: int) -> null


Abandon a batch without committing. Discards any encoded dispatches and frees the underlying MTLCommandBuffer. Idempotent — releasing an unknown handle is a no-op. Pair with _mtlBatchBegin in error paths so a failed mid-batch dispatch doesn't leak the cb.


<sub>Source: [`eval/builtins_mtl_stub.go:164`](../eval/builtins_mtl_stub.go#L164)</sub>

### `_mtlBuffer(…)`

_mtlBuffer(arr: array) -> (handle: int, error)


Create an MTLBuffer of float32 elements from a kLex number array. Bytes are uploaded once at create time; the buffer is then bindable to compute dispatches.


<sub>Source: [`eval/builtins_mtl_stub.go:84`](../eval/builtins_mtl_stub.go#L84)</sub>

### `_mtlBufferAllocF32(…)`

_mtlBufferAllocF32(count: int) -> (handle: int, error)


Allocate an MTLBuffer of `count` zeroed float32 slots without an initial upload. Use for output buffers a kernel will write into.


<sub>Source: [`eval/builtins_mtl_stub.go:96`](../eval/builtins_mtl_stub.go#L96)</sub>

### `_mtlBufferAllocU32(…)`

_mtlBufferAllocU32(count: int) -> (handle: int, error)


Allocate an MTLBuffer of `count` zeroed uint32 slots without an initial upload. Use for output index / counter buffers.


<sub>Source: [`eval/builtins_mtl_stub.go:100`](../eval/builtins_mtl_stub.go#L100)</sub>

### `_mtlBufferFromTensor(…)`

_mtlBufferFromTensor(t: tensor) -> (handle: int, error)


Zero-marshalling MTLBuffer upload from a FrogPy f32 tensor's underlying []float32 slice. Skips the per-element Float-to-float32 conversion that _mtlBuffer does for kLex Arrays — ~5x faster at 1024^2. Tensor must be DType f32 and contiguous; other dtypes / strided views error cleanly.


<sub>Source: [`eval/builtins_mtl_stub.go:88`](../eval/builtins_mtl_stub.go#L88)</sub>

### `_mtlBufferRelease(…)` → `null`

_mtlBufferRelease(handle: int) -> null


Free an MTLBuffer. Idempotent — safe with unknown handles. Phase-F buffer pool recycles same-size buffers internally; release pushes the buffer back into the pool rather than freeing it outright.


<sub>Source: [`eval/builtins_mtl_stub.go:116`](../eval/builtins_mtl_stub.go#L116)</sub>

### `_mtlBufferU32(…)`

_mtlBufferU32(arr: array) -> (handle: int, error)


Same as _mtlBuffer but the buffer holds uint32 elements — for index buffers, atomic counters, packed flags, etc.


<sub>Source: [`eval/builtins_mtl_stub.go:92`](../eval/builtins_mtl_stub.go#L92)</sub>

### `_mtlClear(…)`

Async builtins: return a pre-resolved Channel so kLex code that does `result, err = recv(_mtlClear(...))` works identically on every platform.


<sub>Source: [`eval/builtins_mtl_stub.go:124`](../eval/builtins_mtl_stub.go#L124)</sub>

### `_mtlDispatch(…)`

_mtlDispatch(kernel: int, bindings: hash, grid: array) -> channel


Async compute dispatch. bindings is a hash with optional keys:\n  textures: [tex_handle, ...]  → bound to MSL [[texture(i)]]\n  buffers:  [buf_handle, ...]  → bound to MSL [[buffer(i)]]\n  accels:   [accel_handle, ...] → bound past the last buffer index\ngrid is [gx, gy, gz] thread count (Metal does the threadgroup math). Returns a single-shot channel that emits (null, null) or (null, err).


<sub>Source: [`eval/builtins_mtl_stub.go:132`](../eval/builtins_mtl_stub.go#L132)</sub>

### `_mtlFFT(…)`

_mtlFFT(in_handle, out_handle, n, inverse) -> Channel of (null, err)


1D complex-to-complex Fast Fourier Transform via MPSGraph.\nBoth buffers hold 2n floats in interleaved (re, im) layout —\nsame byte format as NumPy / SciPy complex64 arrays.\n\n  inverse=0  →  forward FFT\n  inverse=1  →  inverse FFT (caller divides by n for a true inverse)\n\nAsync via runAsyncSingle.


<sub>Source: [`eval/builtins_mtl_stub.go:148`](../eval/builtins_mtl_stub.go#L148)</sub>

### `_mtlInfo(…)`

Synchronous builtins: return tuple directly.


<sub>Source: [`eval/builtins_mtl_stub.go:56`](../eval/builtins_mtl_stub.go#L56)</sub>

### `_mtlKernel(…)`

_mtlKernel(mslSource: string, fnName: string) -> (handle: int, error)


Compile MSL source and create an MTLComputePipelineState for the named entry-point function. The returned handle is reusable across many dispatches.


<sub>Source: [`eval/builtins_mtl_stub.go:76`](../eval/builtins_mtl_stub.go#L76)</sub>

### `_mtlKernelRelease(…)` → `null`

_mtlKernelRelease(kernel) -> null


Drops the bridge's retain on the compiled pipeline state.\nIdempotent — unknown handles are no-ops.


<sub>Source: [`eval/builtins_mtl_stub.go:80`](../eval/builtins_mtl_stub.go#L80)</sub>

### `_mtlMatmulMPS(…)`

_mtlMatmulMPS(a: int, b: int, c: int, m: int, k: int, n: int) -> channel


Async MPSMatrixMultiplication: C := A · B. a, b, c are MTLBuffer handles (float32). A is [m × k] row-major, B is [k × n] row-major, C is overwritten in place. Apple's MPS kernels use simdgroup matrix instructions and beat naive matmul by 10-20× for n ≥ 256.


<sub>Source: [`eval/builtins_mtl_stub.go:144`](../eval/builtins_mtl_stub.go#L144)</sub>

### `_mtlReadBuffer(…)`

_mtlReadBuffer(handle: int) -> (array, error)


Read a float32 MTLBuffer's contents back into a kLex float array. Pair with _mtlBufferAllocF32 + _mtlDispatch for compute results.


<sub>Source: [`eval/builtins_mtl_stub.go:104`](../eval/builtins_mtl_stub.go#L104)</sub>

### `_mtlReadBufferIntoTensor(…)`

_mtlReadBufferIntoTensor(handle: int, t: tensor) -> (null, error)


Zero-allocation readback: copies a float32 MTLBuffer directly into a pre-allocated f32 FrogPy tensor's underlying []float32 slice. Skips the per-element Float construction _mtlReadBuffer pays — ~7ms saved per 1M cells. Tensor must be f32, contiguous, and sized exactly to the buffer's element count.


<sub>Source: [`eval/builtins_mtl_stub.go:112`](../eval/builtins_mtl_stub.go#L112)</sub>

### `_mtlReadBufferU32(…)`

_mtlReadBufferU32(handle: int) -> (array, error)


Read a uint32 MTLBuffer's contents back into a kLex int array.


<sub>Source: [`eval/builtins_mtl_stub.go:108`](../eval/builtins_mtl_stub.go#L108)</sub>

### `_mtlSurface(…)`

_mtlSurface(width: int, height: int) -> (handle: int, error)


Allocate a fresh offscreen RGBA8 Metal texture (\


<sub>Source: [`eval/builtins_mtl_stub.go:60`](../eval/builtins_mtl_stub.go#L60)</sub>

### `_mtlSurfaceFromBytes(…)`

_mtlSurfaceFromBytes(bytes: bytes, width: int, height: int) -> (handle: int, error)


Allocate a fresh RGBA8 surface and upload `bytes` (length must equal width*height*4) into it. Use to bring an in-memory image (e.g. a generated SD image) onto a Metal surface so a compute kernel can post-process it.


<sub>Source: [`eval/builtins_mtl_stub.go:68`](../eval/builtins_mtl_stub.go#L68)</sub>

### `_mtlSurfaceReadRgba(…)`

_mtlSurfaceReadRgba(handle: int) -> (bytes, error)


Read the surface's pixels back into a kLex bytes value (RGBA8 row-major, width*height*4 bytes). Counterpart to _mtlSurfaceFromBytes — completes the round-trip for image post-processing pipelines. On Apple Silicon (unified memory) the read is zero-copy (memcpy from shared storage).


<sub>Source: [`eval/builtins_mtl_stub.go:72`](../eval/builtins_mtl_stub.go#L72)</sub>

### `_mtlSurfaceRelease(…)` → `null`

_mtlSurfaceRelease(handle: int) -> null


Drop the bridge's retain on the surface so the GPU memory can be freed. Idempotent — safe to call with an unknown handle.


<sub>Source: [`eval/builtins_mtl_stub.go:64`](../eval/builtins_mtl_stub.go#L64)</sub>

### `_mtlSurfaceSavePng(…)`

_mtlSurfaceSavePng(handle: int, path: string) -> channel


Async — read the surface pixels, PNG-encode, write to disk. Returns a single-shot channel that emits (null, null) on success or (null, err) on failure.


<sub>Source: [`eval/builtins_mtl_stub.go:128`](../eval/builtins_mtl_stub.go#L128)</sub>

## Tensor

### `_tensor_abs(…)`

_tensor_abs(a) → tensor Element-wise abs(a). Returns a fresh tensor with the same shape and dtype as a. Supported for all dtypes (f32, f64, i64).


<sub>Source: [`eval/builtins_tensor.go:287`](../eval/builtins_tensor.go#L287)</sub>

### `_tensor_abs_inplace(…)`

_tensor_abs_inplace(a) → tensor In-place element-wise absolute value: a[i] = abs(a[i]). Returns a. All dtypes. For i64, INT64_MIN wraps to itself (matches NumPy).


<sub>Source: [`eval/builtins_tensor.go:490`](../eval/builtins_tensor.go#L490)</sub>

### `_tensor_add(…)`

_tensor_add(a, b) → tensor Element-wise a + b. Supports tensor+tensor (same dtype, broadcastable shapes) and tensor+scalar / scalar+tensor mixed forms. Allocates a fresh output tensor; use _tensor_add_inplace to avoid the allocation.


<sub>Source: [`eval/builtins_tensor.go:248`](../eval/builtins_tensor.go#L248)</sub>

### `_tensor_add_inplace(…)`

_tensor_add_inplace(a, b) → tensor In-place element-wise add: a[i] = a[i] + b[i]. Returns a. Avoids allocation for higher throughput than _tensor_add. Both tensors must have identical dtype and shape (no broadcasting for in-place ops).


<sub>Source: [`eval/builtins_tensor.go:413`](../eval/builtins_tensor.go#L413)</sub>

### `_tensor_arange(start, stop, step, dtype)`

_tensor_arange(start: number, stop: number, step: number, dtype: string) -> tensor


Returns a 1-D tensor of values from start up to (but not including) stop, separated by step. step must be non-zero. NumPy equivalent: np.arange(start, stop, step, dtype=dtype).


<sub>Source: [`eval/builtins_tensor.go:2413`](../eval/builtins_tensor.go#L2413)</sub>

### `_tensor_argmax(…)` → `int`

_tensor_argmax(t) → int Linear index of the maximum element (first occurrence on ties). Errors on empty tensor.


<sub>Source: [`eval/builtins_tensor.go:744`](../eval/builtins_tensor.go#L744)</sub>

### `_tensor_argmax_axis(…)`

_tensor_argmax_axis(t: tensor, axis: int) → tensor


Index of the maximum element along one axis. Indices refer to positions within the reduced axis. Output is i64 dtype regardless of input. First-occurrence on ties. Empty axis errors.


<sub>Source: [`eval/builtins_tensor.go:902`](../eval/builtins_tensor.go#L902)</sub>

### `_tensor_argmax_axis_keepdims(…)`

_tensor_argmax_axis_keepdims(t: tensor, axis: int) → tensor


<sub>Source: [`eval/builtins_tensor.go:911`](../eval/builtins_tensor.go#L911)</sub>

### `_tensor_argmin(…)` → `int`

_tensor_argmin(t) → int Linear index of the minimum element (first occurrence on ties). Errors on empty tensor.


<sub>Source: [`eval/builtins_tensor.go:725`](../eval/builtins_tensor.go#L725)</sub>

### `_tensor_argmin_axis(…)`

_tensor_argmin_axis(t: tensor, axis: int) → tensor


Index of the minimum element along one axis. Indices refer to positions within the reduced axis. Output is i64 dtype regardless of input. First-occurrence on ties. Empty axis errors.


<sub>Source: [`eval/builtins_tensor.go:879`](../eval/builtins_tensor.go#L879)</sub>

### `_tensor_argmin_axis_keepdims(…)`

_tensor_argmin_axis_keepdims(t: tensor, axis: int) → tensor


<sub>Source: [`eval/builtins_tensor.go:888`](../eval/builtins_tensor.go#L888)</sub>

### `_tensor_cast(t, dtype)`

_tensor_cast(t: tensor, dtype: string) -> tensor


Returns a new tensor with every element converted to the target dtype. When src and target dtypes are identical, this is equivalent to clone.


Conversion rules:

```lex
f32 → f64 : lossless widening
f64 → f32 : truncating (silent precision loss for large values)
i64 → f32 : best-effort (exact for |x| ≤ 2^24; silent rounding beyond)
i64 → f64 : near-lossless (exact for |x| ≤ 2^53)
f32 → i64 : truncate toward zero (IEEE floor-to-zero)
f64 → i64 : truncate toward zero
```


<sub>Source: [`eval/builtins_tensor.go:2217`](../eval/builtins_tensor.go#L2217)</sub>

### `_tensor_clip(t, lo, hi)`

_tensor_clip(t: tensor, lo: number, hi: number) -> tensor


Returns a new tensor with every element clamped to [lo, hi]. lo and hi must be numbers compatible with t's dtype (same rules as _tensor_full: Float rejects i64; Integer widens to f32/f64). lo <= hi is required; violating it errors cleanly.


<sub>Source: [`eval/builtins_tensor.go:2292`](../eval/builtins_tensor.go#L2292)</sub>

### `_tensor_clone(t)`

_tensor_clone(t: tensor) -> tensor


Returns a fresh independent copy of t. The backing data is fully copied — mutations to the result do not affect t and vice versa. Use this when you need an independent snapshot of a tensor that may otherwise be aliased by reshape / flatten / expand_dims views, or to materialise a strided view from _tensor_slice into a fresh contiguous tensor.


<sub>Source: [`eval/builtins_tensor.go:2038`](../eval/builtins_tensor.go#L2038)</sub>

### `_tensor_concatenate(tensors, axis)`

_tensor_concatenate(tensors: array, axis: int) -> tensor


Joins an array of tensors along an existing axis. All tensors must:

```lex
- have the same dtype
- have the same rank
- have identical shapes on every axis except `axis`
```

NumPy equivalent: np.concatenate([t1, t2, ...], axis=N).


<sub>Source: [`eval/builtins_tensor.go:2802`](../eval/builtins_tensor.go#L2802)</sub>

### `_tensor_contiguous(t)`

_tensor_contiguous(t: tensor) -> tensor


If t is already contiguous, returns t unchanged (no allocation — matches NumPy's np.ascontiguousarray fast path). If t is a strided view (e.g. produced by _tensor_slice) walks the multi- index and copies into a fresh contiguous tensor.


Required before passing a slice view to any kernel-based op (add, matmul, reductions, etc.) since those assume contiguous backing layout. Pair with _tensor_slice for the "view, then materialise" workflow.


<sub>Source: [`eval/builtins_tensor.go:2194`](../eval/builtins_tensor.go#L2194)</sub>

### `_tensor_cos(…)`

_tensor_cos(a) → tensor Element-wise cos(a). Float-only (f32/f64).


<sub>Source: [`eval/builtins_tensor.go:329`](../eval/builtins_tensor.go#L329)</sub>

### `_tensor_cos_inplace(…)`

_tensor_cos_inplace(a) → tensor In-place element-wise cos(a). Returns a. Float-only.


<sub>Source: [`eval/builtins_tensor.go:532`](../eval/builtins_tensor.go#L532)</sub>

### `_tensor_div(…)`

_tensor_div(a, b) → tensor Element-wise a / b. All dtypes. For i64, a pre-scan detects divide-by-zero and surfaces a clean kLex error with the offending index. Float dtypes follow IEEE 754 (n/0 = ±Inf, 0/0 = NaN) — no error raised.


<sub>Source: [`eval/builtins_tensor.go:367`](../eval/builtins_tensor.go#L367)</sub>

### `_tensor_div_inplace(…)`

_tensor_div_inplace(a, b) → tensor In-place element-wise divide: a[i] = a[i] / b[i]. Returns a. i64 div-by-zero errors cleanly; float dtypes follow IEEE 754.


<sub>Source: [`eval/builtins_tensor.go:438`](../eval/builtins_tensor.go#L438)</sub>

### `_tensor_dot(a, b)` → `float`

_tensor_dot(a: tensor, b: tensor) -> number


1-D inner product: sum(a[i] * b[i]) for i in [0, n). Both inputs must be 1-D, same dtype, same length, contiguous. Returns a scalar of the matching dtype (Float for f32/f64, Integer for i64). For higher-dimensional dot/matmul patterns, use _tensor_matmul.


Implemented as a fused C kernel (mul + sum in one pass) so no temporary tensor is allocated. NumPy equivalent: np.dot for the 1-D × 1-D case.


<sub>Source: [`eval/builtins_tensor.go:1068`](../eval/builtins_tensor.go#L1068)</sub>

### `_tensor_dtype(t)` → `string`

_tensor_dtype(t: tensor) -> string Returns the dtype name ("f32", "f64", "i64").


<sub>Source: [`eval/builtins_tensor.go:557`](../eval/builtins_tensor.go#L557)</sub>

### `_tensor_eq(…)`

_tensor_eq(a, b) -> tensor Element-wise equality: out[i] = 1 if a[i] == b[i] else 0. Output is always i64 regardless of input dtype. Operands may be tensor+tensor (broadcast-compatible) or tensor+scalar. NaN follows IEEE 754: any comparison with NaN is 0 (false). Pair with _tensor_where for mask-based selection. NumPy parallel: np.equal(a, b) cast to int64.


<sub>Source: [`eval/builtins_tensor.go:2595`](../eval/builtins_tensor.go#L2595)</sub>

### `_tensor_exp(…)`

_tensor_exp(a) → tensor Element-wise exp(a). Float-only (f32/f64); passing i64 errors cleanly.


<sub>Source: [`eval/builtins_tensor.go:295`](../eval/builtins_tensor.go#L295)</sub>

### `_tensor_exp_inplace(…)`

_tensor_exp_inplace(a) → tensor In-place element-wise exp(a). Returns a. Float-only (f32/f64); rejects i64.


<sub>Source: [`eval/builtins_tensor.go:498`](../eval/builtins_tensor.go#L498)</sub>

### `_tensor_expand_dims(t, axis)`

_tensor_expand_dims(t: tensor, axis: int) -> tensor


Returns a view of t with a new size-1 dimension inserted at the given axis. axis may be negative (counts from the end). Valid range is [-len(shape)-1, len(shape)] — i.e., the axis can be inserted at any existing position OR appended after the last.


NumPy equivalent: np.expand_dims.


```lex
t.expand_dims(t, 0)       // (3, 4) → (1, 3, 4)
t.expand_dims(t, 1)       // (3, 4) → (3, 1, 4)
t.expand_dims(t, -1)      // (3, 4) → (3, 4, 1)
```


<sub>Source: [`eval/builtins_tensor.go:1018`](../eval/builtins_tensor.go#L1018)</sub>

### `_tensor_eye(n, dtype)`

_tensor_eye(n: int, dtype: string) -> tensor


Returns an n×n identity matrix: 1 on the diagonal, 0 elsewhere. NumPy equivalent: np.eye(n, dtype=dtype).


<sub>Source: [`eval/builtins_tensor.go:2475`](../eval/builtins_tensor.go#L2475)</sub>

### `_tensor_from_array(data, dtype)`

_tensor_from_array(data: array, dtype: string) -> tensor


Converts a flat 1-D kLex array (of numbers) to a 1-D tensor of the given dtype. Element conversion is strict: Float64 elements convert losslessly to f64, are truncated to f32, and rejected for i64; Integer elements convert losslessly to i64 and as floats for f32/f64. Mixed-type arrays where some element doesn't fit the target dtype error cleanly.


For multi-dim construction in v1, build a 1-D tensor here then reshape (TBD). Keeps the v1 surface tight.


<sub>Source: [`eval/builtins_tensor.go:209`](../eval/builtins_tensor.go#L209)</sub>

### `_tensor_full(shape, value, dtype)`

_tensor_full(shape: array, value: number, dtype: string) -> tensor


Allocates a fresh contiguous tensor with every element set to `value`. Matches NumPy's np.full. Bypasses the from_array→reshape ceremony required to build a non-zero constant matrix — useful for benchmark setup, weight init, and constant operands.


Value conversion rules mirror _tensor_from_array:

```lex
- f32 / f64 accept Integer or Float
- i64 accepts Integer only; Float is rejected without explicit
  conversion (consistent with kLex's strict-typing policy)
```


<sub>Source: [`eval/builtins_tensor.go:67`](../eval/builtins_tensor.go#L67)</sub>

### `_tensor_ge(…)`

_tensor_ge(a, b) -> tensor Element-wise greater-than-or-equal: out[i] = 1 if a[i] >= b[i] else 0. Output is i64. NaN comparisons return 0. Same broadcasting + scalar rules as _tensor_eq.


<sub>Source: [`eval/builtins_tensor.go:2630`](../eval/builtins_tensor.go#L2630)</sub>

### `_tensor_get(t, idx)` → `float`

_tensor_get(t: tensor, idx: int) -> number Linear element access — treats the tensor as a flat 1-D view of its logical data. Used by stdlib/tensor.lex's preview/print helpers and by users who want a single scalar out without computing the multi-index. Bounds-checked.


Works on strided views (from _tensor_slice) too: the flat index is translated through the tensor's Strides to find the physical backing-slice offset.


<sub>Source: [`eval/builtins_tensor.go:590`](../eval/builtins_tensor.go#L590)</sub>

### `_tensor_gt(…)`

_tensor_gt(a, b) -> tensor Element-wise greater-than: out[i] = 1 if a[i] > b[i] else 0. Output is i64. NaN comparisons return 0. Same broadcasting + scalar rules as _tensor_eq.


<sub>Source: [`eval/builtins_tensor.go:2623`](../eval/builtins_tensor.go#L2623)</sub>

### `_tensor_le(…)`

_tensor_le(a, b) -> tensor Element-wise less-than-or-equal: out[i] = 1 if a[i] <= b[i] else 0. Output is i64. NaN comparisons return 0. Same broadcasting + scalar rules as _tensor_eq.


<sub>Source: [`eval/builtins_tensor.go:2616`](../eval/builtins_tensor.go#L2616)</sub>

### `_tensor_linspace(start, stop, n, dtype)`

_tensor_linspace(start: number, stop: number, n: int, dtype: string) -> tensor


Returns a 1-D tensor of n evenly spaced values from start to stop inclusive. n must be >= 1. For n == 1 the result is [start]. NumPy equivalent: np.linspace(start, stop, num=n).


<sub>Source: [`eval/builtins_tensor.go:2356`](../eval/builtins_tensor.go#L2356)</sub>

### `_tensor_log(…)`

_tensor_log(a) → tensor Element-wise natural log(a). Float-only (f32/f64). Negative inputs produce NaN silently (IEEE 754); use safe() if you want to detect domain errors.


<sub>Source: [`eval/builtins_tensor.go:304`](../eval/builtins_tensor.go#L304)</sub>

### `_tensor_log_inplace(…)`

_tensor_log_inplace(a) → tensor In-place element-wise log(a). Returns a. Float-only. Negative inputs produce NaN silently (IEEE 754).


<sub>Source: [`eval/builtins_tensor.go:507`](../eval/builtins_tensor.go#L507)</sub>

### `_tensor_lt(…)`

_tensor_lt(a, b) -> tensor Element-wise less-than: out[i] = 1 if a[i] < b[i] else 0. Output is i64. NaN comparisons return 0. Same broadcasting + scalar rules as _tensor_eq.


<sub>Source: [`eval/builtins_tensor.go:2609`](../eval/builtins_tensor.go#L2609)</sub>

### `_tensor_matmul(a, b)`

_tensor_matmul(a: tensor, b: tensor) -> tensor


Matrix multiplication C := A · B for 2-D tensors. Shape rules:


```lex
a: [m, k]   b: [k, n]   →   c: [m, n]
```


Dispatch:

```lex
- f32 on macOS: MPSMatrixMultiplication (GPU). MPS overhead
  loses to a naive CPU loop below ~256³; we accept that for
  v1 simplicity — the gap is sub-millisecond and the API
  stays predictable.
- f32 on Linux: CPU kernel (-O3 -ffast-math autovec).
- f64 / i64: CPU kernel on every platform. MPS is f32-only at
  the current bridge surface.
```


Both inputs must be 2-D, same dtype, both contiguous. No broadcasting; no batched matmul in v1.


<sub>Source: [`eval/builtins_tensor.go:1166`](../eval/builtins_tensor.go#L1166)</sub>

### `_tensor_max(…)` → `float`

_tensor_max(t) → number Maximum element. Returns a same-dtype scalar (Float for f32/f64, Integer for i64). Errors on empty tensor.


<sub>Source: [`eval/builtins_tensor.go:706`](../eval/builtins_tensor.go#L706)</sub>

### `_tensor_max_axis(…)`

_tensor_max_axis(t: tensor, axis: int) → tensor


Maximum along one axis. Output is same dtype as input. Empty axis errors.


<sub>Source: [`eval/builtins_tensor.go:856`](../eval/builtins_tensor.go#L856)</sub>

### `_tensor_max_axis_keepdims(…)`

_tensor_max_axis_keepdims(t: tensor, axis: int) → tensor


<sub>Source: [`eval/builtins_tensor.go:865`](../eval/builtins_tensor.go#L865)</sub>

### `_tensor_mean(…)` → `float`

_tensor_mean(t) → float Mean of all elements. Always returns Float regardless of dtype (matches NumPy's np.mean on integer arrays). Computed as sum/n in Go; precision follows the dtype's accumulator (f32 accumulates in float, f64 in double, i64 widened to float64). Errors on empty tensor.


<sub>Source: [`eval/builtins_tensor.go:667`](../eval/builtins_tensor.go#L667)</sub>

### `_tensor_mean_axis(…)`

_tensor_mean_axis(t: tensor, axis: int) → tensor


Mean along one axis. Always returns f64 dtype (matches scalar mean's policy and NumPy's np.mean on integer arrays). Empty axis errors cleanly.


<sub>Source: [`eval/builtins_tensor.go:811`](../eval/builtins_tensor.go#L811)</sub>

### `_tensor_mean_axis_keepdims(…)`

_tensor_mean_axis_keepdims(t: tensor, axis: int) → tensor


keepdims variant of _tensor_mean_axis. Reduced axis stays at size 1; useful for centring (x - mean(x, axis, keepdims=true)).


<sub>Source: [`eval/builtins_tensor.go:823`](../eval/builtins_tensor.go#L823)</sub>

### `_tensor_min(…)` → `float`

_tensor_min(t) → number Minimum element. Returns a same-dtype scalar (Float for f32/f64, Integer for i64). Errors on empty tensor.


<sub>Source: [`eval/builtins_tensor.go:687`](../eval/builtins_tensor.go#L687)</sub>

### `_tensor_min_axis(…)`

_tensor_min_axis(t: tensor, axis: int) → tensor


Minimum along one axis. Output is same dtype as input. Empty axis errors.


<sub>Source: [`eval/builtins_tensor.go:835`](../eval/builtins_tensor.go#L835)</sub>

### `_tensor_min_axis_keepdims(…)`

_tensor_min_axis_keepdims(t: tensor, axis: int) → tensor


<sub>Source: [`eval/builtins_tensor.go:844`](../eval/builtins_tensor.go#L844)</sub>

### `_tensor_mul(…)`

_tensor_mul(a, b) → tensor Element-wise a * b. Same dtype/shape rules as _tensor_add.


<sub>Source: [`eval/builtins_tensor.go:264`](../eval/builtins_tensor.go#L264)</sub>

### `_tensor_mul_inplace(…)`

_tensor_mul_inplace(a, b) → tensor In-place element-wise multiply: a[i] = a[i] * b[i]. Returns a.


<sub>Source: [`eval/builtins_tensor.go:429`](../eval/builtins_tensor.go#L429)</sub>

### `_tensor_ne(…)`

_tensor_ne(a, b) -> tensor Element-wise inequality: out[i] = 1 if a[i] != b[i] else 0. Output is i64. NaN comparisons: NaN != NaN is 1 (matches IEEE 754 and NumPy). Same broadcasting + scalar rules as _tensor_eq.


<sub>Source: [`eval/builtins_tensor.go:2602`](../eval/builtins_tensor.go#L2602)</sub>

### `_tensor_neg(…)`

_tensor_neg(a) → tensor Element-wise -a. Returns a fresh tensor with the same shape and dtype as a. Supported for all dtypes (f32, f64, i64).


<sub>Source: [`eval/builtins_tensor.go:278`](../eval/builtins_tensor.go#L278)</sub>

### `_tensor_neg_inplace(…)`

_tensor_neg_inplace(a) → tensor In-place element-wise negate: a[i] = -a[i]. Returns a. All dtypes.


<sub>Source: [`eval/builtins_tensor.go:481`](../eval/builtins_tensor.go#L481)</sub>

### `_tensor_numel(t)` → `int`

_tensor_numel(t: tensor) -> int Total element count (product of shape).


<sub>Source: [`eval/builtins_tensor.go:570`](../eval/builtins_tensor.go#L570)</sub>

### `_tensor_pow(…)`

_tensor_pow(a, b) → tensor Element-wise a ** b. All dtypes. Negative i64 exponents error cleanly (result would be fractional, not representable in i64 — use f32/f64). Float kernels handle negative exponents natively via libm pow.


<sub>Source: [`eval/builtins_tensor.go:339`](../eval/builtins_tensor.go#L339)</sub>

### `_tensor_pow_inplace(…)`

_tensor_pow_inplace(a, b) → tensor In-place element-wise power: a[i] = a[i] ** b[i]. Returns a. Negative i64 exponents error cleanly.


<sub>Source: [`eval/builtins_tensor.go:460`](../eval/builtins_tensor.go#L460)</sub>

### `_tensor_random(shape, dtype, seed)`

_tensor_random(shape: array, dtype: string, seed: int) -> tensor


Allocates a fresh contiguous tensor filled with pseudo-random values. Matches NumPy's np.random.rand for the float dtypes: uniform on [0, 1). For i64 a separate semantic applies — the full int64 range is used (consistent with NumPy's default int generator).


seed semantics:

```lex
seed > 0  → deterministic. Same seed + same shape produces
            identical output across runs. Use this for
            reproducible benchmarks and tests.
seed == 0 → time-seeded (non-reproducible). Use this when
            you want fresh entropy each run.
```


The PRNG is Go's math/rand (PCG-family). Not cryptographically secure — for crypto use stdlib/crypto.lex, not this.


<sub>Source: [`eval/builtins_tensor.go:149`](../eval/builtins_tensor.go#L149)</sub>

### `_tensor_reshape(t, newShape)`

_tensor_reshape(t: tensor, newShape: array) -> tensor


Returns a NEW Tensor with the requested shape, sharing the backing data slice with t (NumPy-style view). product(newShape) must equal t.Numel(); otherwise an error is returned.


v1 only handles contiguous → contiguous. Non-contiguous inputs (Strides != nil) error cleanly — they'd need materialisation first, which v1's storage layer doesn't yet expose.


Because the returned tensor shares storage, in-place ops on either alias mutate both. This matches NumPy's reshape-returns- view semantics. For an independent copy, callers can materialise via an element-wise op (e.g. add a zeros tensor) — clone is a later addition.


<sub>Source: [`eval/builtins_tensor.go:934`](../eval/builtins_tensor.go#L934)</sub>

### `_tensor_shape(t)` → `array`

_tensor_shape(t: tensor) -> array Returns a fresh kLex array of integers describing t's shape.


<sub>Source: [`eval/builtins_tensor.go:540`](../eval/builtins_tensor.go#L540)</sub>

### `_tensor_sin(…)`

_tensor_sin(a) → tensor Element-wise sin(a). Float-only (f32/f64).


<sub>Source: [`eval/builtins_tensor.go:321`](../eval/builtins_tensor.go#L321)</sub>

### `_tensor_sin_inplace(…)`

_tensor_sin_inplace(a) → tensor In-place element-wise sin(a). Returns a. Float-only.


<sub>Source: [`eval/builtins_tensor.go:524`](../eval/builtins_tensor.go#L524)</sub>

### `_tensor_slice(t, specs)`

_tensor_slice(t: tensor, specs: array) -> tensor


Returns a VIEW into t — a new tensor with its own Shape/Strides but sharing the same backing data slice. Mutations through the view affect t and vice versa.


`specs` is an array with one entry per axis of t (so its length must equal len(t.shape)). Each entry is either:


```lex
null                     — take all of this axis (== [null, null, null])
[start, stop, step]      — start/stop/step like Python slice
                           (each of start/stop/step may itself be null
                           to take the default: 0 / dim / 1)
```


Negative start/stop count from the end of the axis. step must be positive (negative-step / reversed slicing deferred to a later pass). Out-of-bounds start/stop clamp; if start >= stop after normalisation the corresponding axis becomes size 0.


Downstream kernels (add, matmul, reductions, etc.) require contiguous input — call `_tensor_contiguous(view)` to materialise before passing a slice to them. _tensor_get, _tensor_to_array, _tensor_clone, _tensor_shape/dtype/numel all work on views directly.


v1 limitations: source tensor must itself be contiguous (no slicing of slices in one step — call contiguous() in between).


<sub>Source: [`eval/builtins_tensor.go:2097`](../eval/builtins_tensor.go#L2097)</sub>

### `_tensor_sqrt(…)`

_tensor_sqrt(a) → tensor Element-wise sqrt(a). Float-only (f32/f64). Negative inputs produce NaN silently (IEEE 754).


<sub>Source: [`eval/builtins_tensor.go:313`](../eval/builtins_tensor.go#L313)</sub>

### `_tensor_sqrt_inplace(…)`

_tensor_sqrt_inplace(a) → tensor In-place element-wise sqrt(a). Returns a. Float-only. Negative inputs produce NaN silently (IEEE 754).


<sub>Source: [`eval/builtins_tensor.go:516`](../eval/builtins_tensor.go#L516)</sub>

### `_tensor_squeeze(t)`

_tensor_squeeze(t: tensor) -> tensor


Returns a view of t with all size-1 dimensions removed. If every dimension is 1, the result is a scalar tensor (shape `[]`, numel 1). Shares the backing data slice with t — in-place ops on either alias mutate both.


NumPy equivalent: np.squeeze. (NumPy's optional axis= argument is deferred — squeeze-all is the common case.)


<sub>Source: [`eval/builtins_tensor.go:980`](../eval/builtins_tensor.go#L980)</sub>

### `_tensor_stack(tensors, axis)`

_tensor_stack(tensors: array, axis: int) -> tensor


Joins an array of tensors along a NEW axis. All tensors must have identical shapes. The output has rank = input_rank + 1, with the new axis of size len(tensors) inserted at position `axis`. NumPy equivalent: np.stack([t1, t2, ...], axis=N).


<sub>Source: [`eval/builtins_tensor.go:2860`](../eval/builtins_tensor.go#L2860)</sub>

### `_tensor_sub(…)`

_tensor_sub(a, b) → tensor Element-wise a - b. Same dtype/shape rules as _tensor_add.


<sub>Source: [`eval/builtins_tensor.go:256`](../eval/builtins_tensor.go#L256)</sub>

### `_tensor_sub_inplace(…)`

_tensor_sub_inplace(a, b) → tensor In-place element-wise subtract: a[i] = a[i] - b[i]. Returns a.


<sub>Source: [`eval/builtins_tensor.go:421`](../eval/builtins_tensor.go#L421)</sub>

### `_tensor_sum(…)` → `float`

_tensor_sum(t) → number Sum of all elements. Returns a same-dtype scalar (Float for f32/f64, Integer for i64). Returns 0 for empty tensors (sum's identity element).


<sub>Source: [`eval/builtins_tensor.go:646`](../eval/builtins_tensor.go#L646)</sub>

### `_tensor_sum_axis(…)`

_tensor_sum_axis(t: tensor, axis: int) → tensor


Sum along one axis. Result has rank len(t.shape)-1. Output is same dtype as input. Empty axis returns zeros (sum's identity). NumPy parallel: np.sum(t, axis=N).


<sub>Source: [`eval/builtins_tensor.go:786`](../eval/builtins_tensor.go#L786)</sub>

### `_tensor_sum_axis_keepdims(…)`

_tensor_sum_axis_keepdims(t: tensor, axis: int) → tensor


Same as _tensor_sum_axis but keeps the reduced axis as size 1 in the output shape. Composes cleanly with broadcasting.


<sub>Source: [`eval/builtins_tensor.go:798`](../eval/builtins_tensor.go#L798)</sub>

### `_tensor_to_array(t)` → `array`

_tensor_to_array(t: tensor) -> array


Extracts all elements from t into a flat kLex array, in row-major logical order. Elements are returned as Float for f32/f64 tensors and as Integer for i64 tensors, matching the return convention of _tensor_get.


Works on strided views (from _tensor_slice): the multi-index is walked through Strides to fetch each element from the right physical position in the backing slice.


<sub>Source: [`eval/builtins_tensor.go:1983`](../eval/builtins_tensor.go#L1983)</sub>

### `_tensor_transpose(t)`

_tensor_transpose(t: tensor) -> tensor


2-D matrix transpose. Input must be a 2-D contiguous tensor of any supported dtype; result is a fresh contiguous tensor with axes swapped: shape [m, n] → shape [n, m], out[i, j] = in[j, i].


Materialising (allocating) rather than view-based — keeps the downstream kernels (matmul, element-wise) simple by not requiring stride-aware variants. Cost is one tensor allocation + a single-pass copy through the C kernel.


v1 is 2-D only. N-D transpose (NumPy's axis-permutation form) is deferred until a workload demands it.


<sub>Source: [`eval/builtins_tensor.go:1119`](../eval/builtins_tensor.go#L1119)</sub>

### `_tensor_where(mask, x, y)`

_tensor_where(mask: tensor, x: tensor|number, y: tensor|number) -> tensor


Element-wise conditional selection: out[i] = x[i] if mask[i] != 0 else y[i]. mask must be an i64 tensor (typically the output of a comparison op). x and y must have the same dtype; either may be a scalar number (broadcast to mask's shape). All shapes must match mask's shape after scalar promotion. NumPy equivalent: np.where(condition, x, y).


<sub>Source: [`eval/builtins_tensor.go:2641`](../eval/builtins_tensor.go#L2641)</sub>

### `_tensor_zeros(shape, dtype)`

_tensor_zeros(shape: array, dtype: string) -> tensor


shape is a kLex array of integers; dtype is one of "f32"/"float32", "f64"/"float64", "i64"/"int64". Allocates a fresh contiguous tensor with all elements set to zero (Go's slice-of-numeric-type default).


<sub>Source: [`eval/builtins_tensor.go:32`](../eval/builtins_tensor.go#L32)</sub>

## Ui Lifecycle Wasm

### `uiBegin(…)` → `null`

uiBegin() — reset per-frame UI state. Must be the first widget call in each draw frame. Mirrors the lifecycle the desktop uiBegin establishes.


<sub>Source: [`eval/builtins_ui_lifecycle_wasm.go:35`](../eval/builtins_ui_lifecycle_wasm.go#L35)</sub>

### `uiEnd(…)`

uiEnd() — close out the UI frame. For Phase 1a this is just the hover check: walk the element registry and update uiCore.hoveredID based on the live mouse position. Phase 3 adds dropdown / tooltip deferred draws here; Phase 1b will add toast rendering.


<sub>Source: [`eval/builtins_ui_lifecycle_wasm.go:77`](../eval/builtins_ui_lifecycle_wasm.go#L77)</sub>

## Ui Widgets

### `accordion(x, y, w, sections, openIdx, [size])`

── accordion(x, y, w, sections, openIdx, [size]) → int ──────────


<sub>Source: [`eval/builtins_ui_widgets.go:1107`](../eval/builtins_ui_widgets.go#L1107)</sub>

### `barChart(data, x, y, w, h)` → `null`

barChart(data, x, y, w, h, [min, max]) — bars with track column behind.


<sub>Source: [`eval/builtins_ui_widgets.go:1558`](../eval/builtins_ui_widgets.go#L1558)</sub>

### `button(label, x, y, w, h, [size])`

── button(label, x, y, w, h, [size]) → bool ─────────────────────


<sub>Source: [`eval/builtins_ui_widgets.go:457`](../eval/builtins_ui_widgets.go#L457)</sub>

### `checkbox(label, x, y, checked, [size])`

── checkbox(label, x, y, checked, [size]) → bool ────────────────


<sub>Source: [`eval/builtins_ui_widgets.go:570`](../eval/builtins_ui_widgets.go#L570)</sub>

### `colorPicker(x, y, w, r, g, b, a)`

── colorPicker(x, y, w, r, g, b, a) → [r, g, b, a] ────────────── Four RGBA drag sliders with a live preview swatch. Values are 0..1.


<sub>Source: [`eval/builtins_ui_widgets.go:2686`](../eval/builtins_ui_widgets.go#L2686)</sub>

### `contextMenu(x, y, items, visible, [size])` → `int`

── contextMenu(x, y, items, visible, [size]) → int ────────────── Returns selected item index, -1 if nothing clicked, -2 on outside-click (dismiss). Caller drives the `visible` arg from state they own (typically set on right-click).


<sub>Source: [`eval/builtins_ui_widgets.go:2562`](../eval/builtins_ui_widgets.go#L2562)</sub>

### `dropdown(label, items, x, y, w, [size])` → `string`

── dropdown(label, items, x, y, w, [size]) → string ───────────── Header shows the selected item; click to open a popup menu drawn last in uiEnd() so it sits on top of every other widget.


<sub>Source: [`eval/builtins_ui_widgets.go:2365`](../eval/builtins_ui_widgets.go#L2365)</sub>

### `getTypedChars(…)` → `string`

── getTypedChars() → string ───────────────────────────────────── Returns the printable characters typed this frame (Unicode-safe).


<sub>Source: [`eval/builtins_ui_widgets.go:2654`](../eval/builtins_ui_widgets.go#L2654)</sub>

### `image(img, x, y, w, h, [mode])` → `null`

── image(img, x, y, w, h, [mode]) → null ──────────────────────── Display a loadImage() result inside the UI. Mode is reserved for future "fit"/"fill"/"stretch" — Phase 1b honours "stretch" (the renderer's default) regardless of the argument.


<sub>Source: [`eval/builtins_ui_widgets.go:1399`](../eval/builtins_ui_widgets.go#L1399)</sub>

### `label(text, x, y, [size])` → `null`

── label(text, x, y, [size]) → null ─────────────────────────────


<sub>Source: [`eval/builtins_ui_widgets.go:549`](../eval/builtins_ui_widgets.go#L549)</sub>

### `lineChart(data, x, y, w, h)` → `null`

lineChart(data, x, y, w, h, [min, max]) — bg + axes + filled area + line.


<sub>Source: [`eval/builtins_ui_widgets.go:1484`](../eval/builtins_ui_widgets.go#L1484)</sub>

### `lineHeight(…)` → `int`

── lineHeight([scale]) → int ──────────────────────────────────── Pixel height of one line of widget text at `scale` (default 0.5). Lets kLex scripts compute vertically-centred text manually — e.g. `label(s, x, y + (h - lineHeight()) / 2)` to drop a label dead-centre inside an h-tall box.


<sub>Source: [`eval/builtins_ui_widgets.go:2666`](../eval/builtins_ui_widgets.go#L2666)</sub>

### `list(label, items, x, y, w, h, [size])` → `null`

── list(label, items, x, y, w, h, [size]) → selected item ───────


<sub>Source: [`eval/builtins_ui_widgets.go:1805`](../eval/builtins_ui_widgets.go#L1805)</sub>

### `listMulti(label, items, selected, x, y, w, h, [size])`

── listMulti(label, items, selected, x, y, w, h, [size]) → bool[]


<sub>Source: [`eval/builtins_ui_widgets.go:1981`](../eval/builtins_ui_widgets.go#L1981)</sub>

### `makeTheme(…)`

makeTheme() → array of 14 [r,g,b,a] arrays


<sub>Source: [`eval/builtins_ui_widgets.go:266`](../eval/builtins_ui_widgets.go#L266)</sub>

### `modal(title, message, buttons)` → `string`

── modal(title, message, buttons) → string ────────────────────── Full-screen dimmed overlay with a centred dialog. Returns the label of the clicked button, "" otherwise. Call AFTER all other widgets so it renders on top.


<sub>Source: [`eval/builtins_ui_widgets.go:1193`](../eval/builtins_ui_widgets.go#L1193)</sub>

### `numericStepper(label, x, y, w, value, min, max, [size])`

── numericStepper(label, x, y, w, value, min, max, [size]) → int


<sub>Source: [`eval/builtins_ui_widgets.go:1009`](../eval/builtins_ui_widgets.go#L1009)</sub>

### `pieChart(data, colors, cx, cy, radius)` → `null`

pieChart(data, colors, cx, cy, radius, [innerRadius])


<sub>Source: [`eval/builtins_ui_widgets.go:1623`](../eval/builtins_ui_widgets.go#L1623)</sub>

### `popDisabled(…)` → `null`

popDisabled() -> null


Pop the top disabled-state frame pushed by pushDisabled(). Every pushDisabled must have exactly one matching popDisabled.\n\nMust be called between uiBegin() and uiEnd().


<sub>Source: [`eval/builtins_ui_widgets.go:288`](../eval/builtins_ui_widgets.go#L288)</sub>

### `progressBar(x, y, w, h, value, min, max)` → `null`

── progressBar(x, y, w, h, value, min, max) → null ──────────────


<sub>Source: [`eval/builtins_ui_widgets.go:725`](../eval/builtins_ui_widgets.go#L725)</sub>

### `pushDisabled(disabled)` → `null`

pushDisabled(true|false) — push a disabled-state frame. All interactive widgets drawn between pushDisabled(true) and popDisabled() render at half opacity and ignore hover/click. Stack-based so nested forms can selectively re-enable sections.


<sub>Source: [`eval/builtins_ui_widgets.go:277`](../eval/builtins_ui_widgets.go#L277)</sub>

### `radio(label, x, y, value, groupValue, [size])` → `string`

── radio(label, x, y, value, groupValue, [size]) → string ─────── Returns `value` if this radio was clicked, else `groupValue`. Usage: chain `g = radio(...,g)` per option in the group.


<sub>Source: [`eval/builtins_ui_widgets.go:838`](../eval/builtins_ui_widgets.go#L838)</sub>

### `scrollArea(x, y, w, h, contentH)`

── scrollArea(x, y, w, h, contentH) → float ───────────────────── Returns the current scroll offset in pixels. Caller draws content translated by -scrollOff; pushClip/popClip the (x,y,w,h) viewport while drawing scrolled content.


<sub>Source: [`eval/builtins_ui_widgets.go:1736`](../eval/builtins_ui_widgets.go#L1736)</sub>

### `setTheme(name)` → `null`

setTheme(name) — install a preset theme. Valid names:

```lex
"nebula"        — deep violet + ice cyan (the original look)
"light"         — clean light theme, blue accent
"dark"          — modern dark theme, blue accent
"highContrast"  — accessibility — pure black/white + yellow accent
```

Sets both colour palette AND style tokens (radii, spacing, font base). Use uiTheme(palette) for fine-grained colour overrides.


<sub>Source: [`eval/builtins_ui_widgets.go:302`](../eval/builtins_ui_widgets.go#L302)</sub>

### `slider(label, x, y, w, value, min, max, [size])`

── slider(label, x, y, w, value, min, max, [size]) → float ──────


<sub>Source: [`eval/builtins_ui_widgets.go:634`](../eval/builtins_ui_widgets.go#L634)</sub>

### `sparkline(data, x, y, w, h)` → `null`

sparkline(data, x, y, w, h) — minimal line, no axes / bg.


<sub>Source: [`eval/builtins_ui_widgets.go:1453`](../eval/builtins_ui_widgets.go#L1453)</sub>

### `splitter(pos, x, y, length, orient, min, max, [thickness])`

── splitter(pos, x, y, length, orient, min, max, [thickness]) → int


<sub>Source: [`eval/builtins_ui_widgets.go:1302`](../eval/builtins_ui_widgets.go#L1302)</sub>

### `table(headers, rows, x, y, w, h, [size])` → `int`

── table(headers, rows, x, y, w, h, [size]) → selectedRow ───────


<sub>Source: [`eval/builtins_ui_widgets.go:3932`](../eval/builtins_ui_widgets.go#L3932)</sub>

### `tabs(x, y, w, items, activeIdx, [size])`

── tabs(x, y, w, items, activeIdx, [size]) → int ────────────────


<sub>Source: [`eval/builtins_ui_widgets.go:903`](../eval/builtins_ui_widgets.go#L903)</sub>

### `textArea(label, text, x, y, w, h, [size], [syntax])`

── textArea(label, text, x, y, w, h, [size], [syntax]) → string ────────── Multi-line text editor. Lines split on '\n'; Enter inserts a newline. Vertical scroll via wheel; cursor follows on edit. Horizontal scroll is intentionally NOT supported — wide lines are clipped on the right. Selection works across line boundaries. The two optional trailing args are order-independent: a number sets the font scale; the string "klex" enables kLex syntax highlighting (keyword/string/comment/number/operator/builtin colours derived from the theme background luminance).


<sub>Source: [`eval/builtins_ui_widgets.go:3344`](../eval/builtins_ui_widgets.go#L3344)</sub>

### `textInput(label, text, x, y, w, h, [size])`

── textInput(label, currentText, x, y, w, h, [size]) → string ───


<sub>Source: [`eval/builtins_ui_widgets.go:2942`](../eval/builtins_ui_widgets.go#L2942)</sub>

### `toast(message, [style], [duration])` → `null`

── toast(message, [style], [duration]) → null ─────────────────── Queue an ephemeral notification. Rendered bottom-right by uiEnd.


<sub>Source: [`eval/builtins_ui_widgets.go:2782`](../eval/builtins_ui_widgets.go#L2782)</sub>

### `toggle(label, x, y, on, [size])`

── toggle(label, x, y, on, [size]) → bool ───────────────────────


<sub>Source: [`eval/builtins_ui_widgets.go:766`](../eval/builtins_ui_widgets.go#L766)</sub>

### `tooltip(text)` → `null`

── tooltip(text) → null ───────────────────────────────────────── Attach hover text to the widget drawn immediately before this call. Appears after the cursor rests on that widget for 0.5s.


<sub>Source: [`eval/builtins_ui_widgets.go:2527`](../eval/builtins_ui_widgets.go#L2527)</sub>

### `treeView(x, y, w, h, labels, levels, expanded, [size])`

── treeView(x, y, w, h, labels, levels, expanded, [size]) → [selectedIdx, expanded[]]


<sub>Source: [`eval/builtins_ui_widgets.go:2164`](../eval/builtins_ui_widgets.go#L2164)</sub>

### `uiBeginCol(x, y, w, gap)` → `null`

Column cursor — advance top-to-bottom.


<sub>Source: [`eval/builtins_ui_widgets.go:420`](../eval/builtins_ui_widgets.go#L420)</sub>

### `uiBeginRow(x, y, h, gap)` → `null`

── Layout cursors ─────────────────────────────────────────────── Row cursor — advance left-to-right.


<sub>Source: [`eval/builtins_ui_widgets.go:381`](../eval/builtins_ui_widgets.go#L381)</sub>

### `uiColAdvance(h)` → `null`

uiColAdvance(h: float) -> null


Advance the column cursor down by h + gap (the gap set in uiBeginCol). Call once after each widget placed at uiColX(), uiColY().


<sub>Source: [`eval/builtins_ui_widgets.go:439`](../eval/builtins_ui_widgets.go#L439)</sub>

### `uiColW(…)`

uiColW() -> int


Returns the width of the active column. This value is constant for the lifetime of the column (set by uiBeginCol). Use it as the w argument for widgets that should span the full column.


<sub>Source: [`eval/builtins_ui_widgets.go:438`](../eval/builtins_ui_widgets.go#L438)</sub>

### `uiColX(…)`

uiColX() -> int


Returns the X position of the active column. This value is constant for the lifetime of the column (set by uiBeginCol).


<sub>Source: [`eval/builtins_ui_widgets.go:436`](../eval/builtins_ui_widgets.go#L436)</sub>

### `uiColY(…)`

uiColY() -> int


Returns the current Y position of the column cursor — where the next widget should be placed. Advances after each uiColAdvance() call.


<sub>Source: [`eval/builtins_ui_widgets.go:437`](../eval/builtins_ui_widgets.go#L437)</sub>

### `uiGetFocus(…)` → `string`

uiGetFocus() → string


<sub>Source: [`eval/builtins_ui_widgets.go:342`](../eval/builtins_ui_widgets.go#L342)</sub>

### `uiNextFieldID(…)` → `string`

uiNextFieldID() → string  — id the next textInput would receive


<sub>Source: [`eval/builtins_ui_widgets.go:337`](../eval/builtins_ui_widgets.go#L337)</sub>

### `uiResetFont(…)` → `null`

uiResetFont() -> null


Revert widget text back to the embedded monospace font. Call after uiSetFont() when you want to stop using a custom font for subsequent widgets.


<sub>Source: [`eval/builtins_ui_widgets.go:374`](../eval/builtins_ui_widgets.go#L374)</sub>

### `uiRowAdvance(w)` → `null`

uiRowAdvance(w: float) -> null


Advance the row cursor right by w + gap (the gap set in uiBeginRow). Call once after each widget placed at uiRowX(), uiRowY().


<sub>Source: [`eval/builtins_ui_widgets.go:402`](../eval/builtins_ui_widgets.go#L402)</sub>

### `uiRowH(…)`

uiRowH() -> int


Returns the height of the active row. This value is constant for the lifetime of the row (set by uiBeginRow). Use it as the h argument for widgets placed in the row.


<sub>Source: [`eval/builtins_ui_widgets.go:401`](../eval/builtins_ui_widgets.go#L401)</sub>

### `uiRowX(…)`

uiRowX() -> int


Returns the current X position of the row cursor — where the next widget should be placed. Advances after each uiRowAdvance() call.


<sub>Source: [`eval/builtins_ui_widgets.go:399`](../eval/builtins_ui_widgets.go#L399)</sub>

### `uiRowY(…)`

uiRowY() -> int


Returns the Y position of the active row. This value is constant for the lifetime of the row (set by uiBeginRow).


<sub>Source: [`eval/builtins_ui_widgets.go:400`](../eval/builtins_ui_widgets.go#L400)</sub>

### `uiSetFocus(id)` → `null`

uiSetFocus(id) — write activeID directly


<sub>Source: [`eval/builtins_ui_widgets.go:347`](../eval/builtins_ui_widgets.go#L347)</sub>

### `uiSetFont(font)` → `null`

uiSetFont(font) / uiResetFont() — pick the proportional font. On WASM the renderer ignores activeFont (Canvas2D always uses CSS font directly) so this is a no-op visual change there until font support is wired into the renderer interface.


<sub>Source: [`eval/builtins_ui_widgets.go:363`](../eval/builtins_ui_widgets.go#L363)</sub>

### `uiTheme(palette)` → `null`

uiTheme(palette) — apply a 14-slot palette to uiCore.theme


<sub>Source: [`eval/builtins_ui_widgets.go:320`](../eval/builtins_ui_widgets.go#L320)</sub>

