# compiled binary
klex
klex.exe
/froglsp
build_releases.sh

# claude project mantra / claude specific stuff
CLAUDE.md
.claude/
.vscode/
.claude/memory/
.claude/memory_manifest.json
/stuff/
snowball/frogMcp

# Claude specific tool binaries (root-only — source under tools/ is tracked)
/bin/
/wasm

# WASM binary — canonical build lives in bin/klex.wasm (gitignored via /bin/).
# This entry prevents local copies in test/example dirs from being committed.
klex.wasm

# VM specific files
vmcoverage

# generatally ignored files
cache.db
kv_test.txt
projects/
releases/

# Index files and things not commited to git
snowball/frogMcp/index.json
snowball/froglsp/froglsp
snowball/frogMcp/indexer/frogMcp-indexer

# Generated WASM stdlib embed (regenerated by tools/stdlibgen / build_releases.sh)
cmd/wasm/embeddedstdlib_gen.go

# Go toolchain artifacts
*.test
*.out

# macOS
.DS_Store

# python bytecode cache (stdlib/python helpers and bridge scripts)
__pycache__/
*.pyc
*.pyo
